# Product Decisions — DealFlow AI

**Append-only decision log.** Captures the "why" behind product and technical choices so AI agents and future developers don't re-litigate settled decisions.

---

## Entry format

```markdown
### [YYYY-QN] <Short decision title>
**Category**: Architecture / Security / Payments / Marketplace / Design / DevOps / Process / Data Model / API
**Status**: Active / Superseded / Deferred / Cancelled
**Context**: What situation required the decision.
**Decision**: The actual decision.
**Rationale**: Why this choice over alternatives.
**Alternatives considered**: Options rejected and why.
```

---

## Architecture & Stack

### [2026-Q2] Tech stack selected
**Category**: Architecture
**Status**: Active
**Context**: v5 onboarding stack selection.
**Decision**: claudomat baseline (applied as default) — Turborepo/pnpm monorepo, NestJS + Next.js 15 (TypeScript), PostgreSQL + Drizzle, SuperTokens auth, Railway hosting (bring-your-own), GitHub Actions CI, Biome, Vitest + Playwright. Payments (Stripe) deferred — no MVP payments. See stack-decisions.md.
**Rationale**: No stack signal in v0 docs or founder requests; product is a standard data-heavy TypeScript/React web app the baseline fits directly. Applied silently per always-on rule 17 and announced to the founder.
**Alternatives considered**: None raised by founder.
**Cascading updates**: None on the default path. v6 architecture additionally specifies project-driven pieces not in the generic baseline: a transactional-email + event-tracking provider (required for compliant outreach), a tamper-evident append-only audit-log store, an LLM provider for matching/drafting, and deal-source/enrichment data providers.

## Authentication & Security

### [2026-Q2] Formal compliance-regime classification = none (features still built)
**Category**: Security
**Status**: Active
**Context**: v6b project.yaml consolidation. The `compliance_regime` enum (gdpr/hipaa/pci/soc2/none) has no FINRA/SOX option; asked the founder how to classify for ongoing reviews.
**Decision**: `stack.compliance_regime: none` — keep the formal regulatory classification light for now. **This does NOT reverse compliance-first**: the compliance FEATURES (tamper-evident audit log, pre-send approval gate, suppression/disclaimer rules, recordkeeping export) remain MVP-core and are fully architected (v6 security/databases branches). What "none" changes is only that future changes are NOT gated by a formal SOC2/GDPR regime review; the founder can raise the classification later.
**Rationale**: Founder chose "keep it light for now." For an internal/pilot tool, building the audit/recordkeeping features matters more right now than formally binding the project to a regulatory regime label. Reversible.
**Alternatives considered**: soc2 (SOX/FINRA-aligned controls) — not chosen now; GDPR — only if EU data enters scope.

### [2026-Q2] Build compliance-first into the MVP (not deferred)
**Category**: Security
**Status**: Active
**Context**: The founder's brief listed "compliance-first architecture (SOX, FINRA readiness)" as an MVP must-have, but also framed DealFlow AI as an internal tool — which normally defers formal compliance to a later horizon. Surfaced at v1 onboarding.
**Decision**: Treat compliance as MVP-scope (H1). The first shippable version includes tamper-evident audit logs, communication recordkeeping, and FINRA/SOX-minded outreach controls as core to the outreach loop — not a later milestone.
**Rationale**: M&A outreach lives under recordkeeping/communications rules; an outreach tool without defensible audit trails is unusable for live mandates regardless of matching quality. Compliance is therefore a precondition for the core loop being usable and a wedge vs ungoverned point tools (Hunter.io / RocketReach). Founder explicitly chose "build compliance-first now" over deferring.
**Alternatives considered**: (a) Audit trail now, formal SOX/FINRA controls later — rejected as it leaves live-mandate outreach under-governed at pilot. (b) Minimal compliance, decide regime at planning — rejected as too risky for regulated outreach with a pilot partner.

## Payments & Finance

_(empty)_

## Marketplace & Product

_(empty)_

## UI & Design

### [2026-Q2] Design direction approved
**Category**: Design
**Status**: Active
**Context**: v7 onboarding design direction, anchored on the Dashboard.
**Decision**: Clean, light-mode interface — calm neutral slate/grey scale (near-white backgrounds → near-black text) with an emerald-green accent for positive/status states; left sidebar navigation; dashboard composed of active-mandate cards, AI match fit-scores, outreach-activity, and a compliance queue. Restrained, credible, data-dense-but-legible — Linear/Stripe spirit, deliberately not a heavy enterprise CRM. Emotional anchors: credible · calm · precise · fast · trustworthy. Light default (dark-mode-feasible token structure, deferred).
**Rationale**: Fits a time-pressured M&A advisory audience working dense deal/financial data who need to trust an audit-grade tool; beats incumbent (DealCloud) density/bloat while matching its credibility. Founder approved on first generation (no revisions needed).
**Artifacts**: `design/direction.html`, `process/session/onboarding/v7-direction-brief.md`

### [2026-Q2] Design system built + approved
**Category**: Design
**Status**: Active
**Context**: v8 onboarding design system generation, on the approved v7 direction + locked module list.
**Decision**: `design/DESIGN-SYSTEM.md` populated — neutral zinc/grey scale + emerald accent + a 5-value status palette; Inter typography with tabular-nums for financial data; spacing/radius/shadow/motion tokens; and a component kit (standard primitives + DealFlow-specific: MatchCard with FitScore, StatusBadge set, ComplianceCheckPanel, AuditLogRow with integrity indicator, MandateCard). Light default, dark-mode-feasible token structure. Visual showcase at `design/staging/design-system.html`.
**Rationale**: Aligned to approved direction (v7) + locked module list (v6b). Founder approved first build.
**Artifacts**: `design/DESIGN-SYSTEM.md`, `design/staging/design-system.html`

### [2026-Q2] Per-page designs complete (20 screens)
**Category**: Design
**Status**: Active
**Context**: v9 onboarding per-page design generation; founder reviewed in 4 grouped batches and approved all.
**Decision**: 20 screens designed + approved (canonical at `design/<page>.html`): dashboard, mandates-list, mandate-detail, mandate-new, matches-shortlist, buyer-universe, outreach-composer, pipeline, sourcing-workspace, companies-contacts, templates-library, compliance-queue, audit-log-export, compliance-settings, admin-integrations, admin-users, admin-workspace-settings, login, accept-invite, reset-password.
**Consistency note**: Pages were generated independently and a ui-designer audit found chrome drift (mixed icon libraries, per-page nav variation, a few off-palette accents). The egregious structural cases (matches-shortlist, outreach-composer, audit-log-export) were regenerated against the canonical shell. Remaining minor drift is ACCEPTED as directional — true cross-page consistency is enforced at build time by a single shared `<AppShell>` (Sidebar + TopBar) + the design-system contract added at DESIGN-SYSTEM.md §10 (lucide-only icons, emerald-600 primary, zinc+emerald+5-status palette, canonical sidebar item set). B-block must implement chrome once, not per page.
**Artifacts**: `design/<page>.html` ×20, `command-center/product/per-page-pd/<page>.md` (annotated), DESIGN-SYSTEM.md §10.

## DevOps & Deploy

### [2026-Q2] CI + canary + PR conventions (defaults)
**Category**: DevOps
**Status**: Active
**Context**: v13 onboarding CI-PRINCIPLES + CI workflow authoring.
**Decision**: GitHub Actions CI (lint / typecheck / test-with-real-Postgres / `pnpm audit --audit-level=high` / build; every job `timeout-minutes` + least-privilege `permissions`). Canary = Standard profile (error_rate 1.0%, p95 +200ms, 15min), runs once real-user DAU crosses 1000. PR conventions = AI-attribution footer ON, auto-merge OFF, squash. Applied silently as technical defaults (rule 17).
**Rationale**: Standard canary fits pilot-customer; conservative human-merge fits a solo/internal team; the audit gate reflects the compliance-first posture.
**Alternatives considered**: Strict/lenient canary; auto-merge — not chosen for a pilot.

## Testing

### [2026-Q2] Test accounts — auto-provision via signup
**Category**: Testing
**Status**: Active
**Context**: v13 onboarding test-account seeding.
**Decision**: Test accounts are created through the project's own signup flow at the first UI wave's B-5 (one local-dev + one prod-fixture per v3 persona: advisor / analyst / compliance / admin). `project.yaml: test_users.local_dev` = `[]` until then.
**Rationale**: No credentials needed at handoff; avoids vendor lock-in to provider tooling. T-5 / T-8 won't block — accounts exist before first needed.
**Next action**: First UI wave's B-5 scripts the signup endpoint.

## Process & Workflow

### [2026-Q2] Launch stage = pilot-customer (upgraded from self-use-mvp)
**Category**: Process
**Status**: Active
**Context**: The brief declared founder stage `self-use-mvp` (founder is first user inside her own firm). At v1 onboarding the founder clarified that one friendly outside advisory firm is planned as a design partner shortly after launch.
**Decision**: Set founder stage to `pilot-customer`. Build for the founder's firm first, but keep a near-term external design partner in mind for security, data isolation, and compliance scoping.
**Rationale**: A design partner outside the founder's own firm changes the trust/security bar from "internal only" to "another firm's data flows through this." Combined with the compliance-first decision, this raises the v6 security branch and v10 compliance milestone to H1.
**Alternatives considered**: Keep self-use-mvp — rejected because it would under-scope security/data-isolation for the imminent external partner.

## Data Model Decisions

### [2026-Q2] Roadmap authored — 12 themed milestones (v10)
**Category**: Process
**Status**: Active
**Context**: v10 planning; founder approved the roadmap shape.
**Decision**: Authored 12 themed milestones (DB, all `status='todo'`, zero child tasks — decomposition is per-wave). H1/MVP (7): M1 Foundation, M2 Compliance backbone, M3 Deal sourcing & data, M4 Mandates & buyer universe, M5 AI matching, M6 Compliant outreach & pipeline, M7 Admin & settings. H2 (3): M8 Pilot-partner workspace, M9 Integrations & insight, M10 Advanced compliance artifacts. H3 (2): M11 Multi-tenant SaaS + billing, M12 Deal network + predictive models. The broad v1 seed milestone ("H1 — Integrated sourcing…") was CANCELLED (superseded by this build-ordered themed set). M2 (compliance backbone) is H1 per the compliance-first override; formal SOX/FINRA artifacts (M10) stay H2.
**Rationale**: Build-order themes with platform-foundation first (M1/M2), then the product loop (M3→M6), then admin (M7). Every MVP page/feature/module/SDK is covered in some milestone's Scope prose. M6 is the "one live mandate end-to-end" proof of the core bet.
**Alternatives considered**: Trim to MVP-only — founder chose full roadmap. Reshape — not requested.

### [2026-Q2] v6b architecture cross-branch conflict resolutions
**Category**: Architecture / Data Model
**Status**: Active
**Context**: v6b integration; architect-reviewer found 19 cross-branch items. `_library.md` is authoritative; resolutions below applied (architect-recommended winners adopted).
**Decisions**:
1. Recordkeeping is a real H1 module with an `export_jobs` status table (async export, fire-and-query).
2. Add `notifications` + `notification_preferences` tables (Databases was missing them).
3. Buyer-universe is a real backend module + `buyer_universe` schema (canonical backend module roster reconciled).
4. Templates is its own module (split from Outreach) with `template_versions` — needed so editing a message after approval invalidates the approval (compliance version-binding).
5. **Audit integrity = HMAC-SHA256 with a Railway-secret key (+ `chain_version`)**, NOT bare SHA-256 — DB-write access alone must not be able to forge the chain. (Compliance crown jewel.)
6. Audit append-only enforced by BOTH an INSERT-only DB grant AND a BEFORE UPDATE/DELETE trigger (defense-in-depth).
7. Audit table = `audit_log_entries`, PK `sequence_number` (Databases is physical-schema authority); fold in `actor_role` + `chain_version`; keep `content_hash` (chain hash) and `payload_hash` (payload hash) as distinct fields.
8. Compliance tables use Databases names: `compliance_approvals` (not `compliance_queue`), `suppression_list`, `disclaimer_templates`, `compliance_rules`.
9. **Pipeline: fixed stage enum for MVP** (shortlisted→contacted→engaged→diligence→offer→closed/withdrawn) + a separate append-only `pipeline_events` table for notes/transitions (feeds audit log). Advisor-configurable per-mandate stages deferred to H2. *(Product-flavored default — reversible; revisit if the founder wants custom stages.)*
10. **Data-source credentials live in Railway env vars only** (`data_source_connections` holds metadata + `provider_key`, no encrypted secrets in DB) — matches the project secrets convention. Consequence: adding a NEW data source needs an ops/env change (no in-app dynamic credential entry) — acceptable for pilot's few fixed sources; in-app source management deferred to H2. *(Logged consequence.)*
11. SuperTokens runs against its OWN Railway Postgres instance, separate from the app DB (`SUPERTOKENS_*` URL never aliases `DATABASE_URL`).
12. App DB env var standardized to `DATABASE_URL` (drop `POSTGRES_URL`).
13. Email webhook secret = provider-agnostic `EMAIL_WEBHOOK_SECRET`; email API key provider-agnostic too (pluggable email provider; Resend is a candidate, not locked).
14. Background queue = BullMQ + Redis (confirmed — Outreach per-domain send rate-limiting depends on it); adds a Redis service.
15. CI test DB = real Postgres container (Docker Compose / testcontainers), NOT pg-mem — the audit append-only grant tests (SQLSTATE 42501) need real role grants.
16. Realtime = SSE for MVP (no WebSocket/Socket.IO gateway); DevOps `api` label corrected.
17. Add `pnpm audit --audit-level=high` as a CI gate (compliance posture).
18. Playwright Chrome binary must be installed host-side before the first UI wave's T-5 (env gap; flagged in test.md + devops.md + competitive-benchmarks/INDEX.md).
**Rationale**: All are UNAMBIGUOUS winners or technical defaults; the four compliance-core invariants (append-only audit, hash-chain integrity, non-bypassable pre-send gate, sender≠approver separation of duties) were consistent across branches — conflicts were mechanism/naming detail only.

---

## [2026-07-02] M1 — Foundation: promoted todo → in_progress (N-1 wave-1 close-out)

**Decision**: M1 (Foundation: auth, roles, app shell, data model, CI) promoted `todo → in_progress`.

**Trigger**: N-1 survey found zero `in_progress` milestones — M1 was never promoted during greenfield bootstrap (wave-1 seed was hand-seeded under a `todo` milestone). Highest-tier `todo` (T1, platform-foundation, required-by all milestones) → promoted per slot-promotion rule (Action 8a).

**Consequence**: M1 is now the active milestone. Wave-1 shipped only scaffold+CI of M1's scope; auth/RBAC/AppShell/data-model/auth-screens remain → decomposition fires for the next foundation bundle (see next entry). M1 NOT closed (scope not shipped).

---

## [2026-07-02] M1 — Foundation: bundle authored — 3 tasks (auth vertical slice)
- caller: N-1-next-bundle
- decomposed by: milestone-decomposer sub-agent
- Slice: end-to-end auth vertical (DB + API + UI). Seed "Integrate SuperTokens auth + user/role data model" (SuperTokens on its own Postgres per #11, users/roles/invites additive Drizzle schema, invite-only, role session claim) + siblings "Build invite-only auth API: signup, session, reset" (NestJS auth endpoints + role-aware guard primitive) and "Wire login, accept-invite, reset-password screens end-to-end" (three Next.js 15 auth pages, Playwright E2E to authenticated placeholder). AppShell (DESIGN-SYSTEM.md section 10), role-aware dashboard shell, and full per-route RBAC enforcement deliberately deferred to a FOLLOW-UP M1 bundle to keep this slice within the size rubric.

---

## [2026-07-03] M1 (M1 — Foundation: auth, roles, app shell, data model, CI): bundle authored — 3 tasks (AppShell + role-aware dashboard shell + per-route RBAC)
- caller: N-1-next-bundle
- decomposed by: milestone-decomposer sub-agent
- Slice: the M1 shell + RBAC vertical explicitly deferred by the 2026-07-02 auth-vertical bundle. Seed "Build shared AppShell chrome + role-aware dashboard shell" (single shared `<AppShell>` = `<Sidebar>` + `<TopBar>` + base primitives per DESIGN-SYSTEM.md section 10 — zinc/emerald, lucide-react only, 4px grid; replaces the wave-2 dashboard placeholder with a role-aware landing reading the SuperTokens role claim for the 4 roles advisor/analyst/compliance/admin) + siblings "Enforce per-route RBAC across API and web routes" (extends the wave-2 guard primitive to per-route API 403s + web route protection off one canonical route→allowed-roles mapping) and "Make AppShell navigation role-aware for the 4 roles" (sidebar items render per role off the SAME mapping — no drift between shown nav and reachable route). Vertical slice (UI + API + guard wiring); closes M1's "land on a role-aware dashboard shell" + per-route RBAC success-metric gap. N-1 judgment override: two existing open follow-ups (test-fixture typing bfadcec1; auth-hardening 6fe232e3) left in queue — they do not advance M1's defining unshipped scope.
- N-1 judgment note: seed-candidate guard count returned 0 (both open follow-ups carry a wave_id); authored per N-1 Action 7 re-ordering allowance, not the standard empty-queue trigger.

## Wave 3 — canonical dashboard route + RBAC route matrix (P-4 remediation, 2026-07-03)
**Decision:** The role-aware dashboard shell mounts at **`/`** (authed; unauthenticated → redirect `/login`), per user-journey-map row 4 ("Dashboard, `/`, All role-aware"). This supersedes the wave-2 placeholder `/dashboard` and the wave-1 public `/` health-landing (health remains served by the api `GET /health`). Public (ungated) allowlist = `/auth/*` + `GET /health` only. Login/accept-invite success redirects to `/`.
**Why:** Resolves a spec-vs-journey-map drift jenny caught at P-4 (plan had used `/dashboard` + a public `/`). The product's front door is the authed app; a compliance-first M&A tool has no public marketing landing in the MVP journey map. Follows the canonical journey map rather than the stale placeholder.
**RBAC route matrix:** pinned to the journey-map persona columns (single `roleRoutes` source of truth in `@dealflow/shared`; drives both nav visibility and `@Roles()` enforcement; nav⊆RBAC contract-tested). Enforced live this wave on `/` (all roles) + `/compliance/summary` (compliance, admin); the remaining route→role rows are pinned now as the enforcement contract for M3+ pages.

## [2026-07-03] M1 → done; M2 → in_progress (N-1 milestone disposition, wave-3 close)
**Decision:** M1 (Foundation: auth, roles, app shell, data model, CI) closed `in_progress → done`. M2 (Compliance backbone: tamper-evident audit log + rules engine) promoted `todo → in_progress`.
**Trigger:** N-1 Action 6 closure check (wave-3 close). M1 open_count=3 but ALL 3 open rows are non-core follow-ups (test-fixture typing bfadcec1; auth-hardening rate-limit/CSRF 6fe232e3; AppShell placeholder pages d7f716b4). M1's core `## Scope` (monorepo, NestJS+Next.js, Postgres+Drizzle, SuperTokens invite-only auth, RBAC 4 roles, shared AppShell, auth screens, role-aware dashboard shell, CI) is delivered across the 7 done tasks, and M1's `## Success metric` (invited user → set password → sign in → role-aware dashboard shell; CI green real-Postgres; Railway dev) is SHIPPED + live-verified in wave-3 (935b847, E2E 7/7). None of the 3 open follow-ups is required by the success metric — they are polish/hardening. Judged shipped → closed.
**M1 follow-up disposition:** the 3 open follow-ups re-parented `milestone_id M1 → M2` (they retain their original `wave_id`; parent_task_id NULL) so they stay claimable and get re-triaged under the now-active compliance milestone rather than orphaned. They are candidate seeds for a future wave but do not gate M2's own scope.
**M2 promotion rationale (Action 8a):** M2 is the highest-tier `todo` — `## Tier T1`, `## Class platform-foundation`, `## Bet source Compliance-first outreach wedge`, `## Required by M6, M10`, `## Why now` founder compliance-first override (audit/recordkeeping MVP-core H1, the defensible wedge). Oldest `created_at` in the todo queue and highest tier (vs M3 T2). Advancing the roadmap to the actual product wedge.
**Consequence:** M2 is the active milestone; its queue has no fresh top-level seed (the 3 re-parented rows all carry a wave_id) → N-1 Action 7 fires milestone-decomposition against M2 for the next wave's first bundle.

[2026-07-03] M2 (Compliance backbone: tamper-evident audit log + rules engine): bundle authored — 4 tasks (first vertical slice: stand up the tamper-evident HMAC-SHA256 hash-chained audit-log service — immutable table + append service + integrity verifier + compliance-settings integrity view; rules engine / suppression / disclaimer / pre-send check deferred to a later M2 bundle since they depend on this audit log existing first)
- caller: N-1-next-bundle
- decomposed by: milestone-decomposer sub-agent

## [2026-07-03] M2 (Compliance backbone: tamper-evident audit log + rules engine): bundle authored — 4 tasks (compliance rules engine + callable pre-send gate)
- caller: N-1-next-bundle
- decomposed by: milestone-decomposer sub-agent
- Slice: M2's second (rules-engine) vertical, unblocked now that wave-4 shipped the audit-log half (AuditService.append a8b2b5a2 exists to write to). The prior M2 bundle log explicitly deferred exactly this. Vertical DB→service→UI. Seed "Build compliance rules engine schema + non-bypassable pre-send compliance gate service" (seed 0595a835) = the 4 rules-engine tables (compliance_rules, suppression_list, disclaimer_templates jurisdiction-keyed, compliance_approvals) + the single ComplianceGateService.evaluate() that is the SOLE send-eligibility authority and MUST write its pass/block verdict via AuditService.append in-transaction — non-bypassability is a stated acceptance criterion (no send path can skip the gate + log). Siblings: "Enforce suppression-list + approval-SoD checks inside the pre-send gate" (95adac6c — recipient suppression re-check at send time + sender≠approver / approver-is-compliance SoD assertion, reusing M1 RolesGuard), "Enforce jurisdiction disclaimers + approval-version binding in the pre-send gate" (034463b1 — required per-jurisdiction disclaimer presence + content-hash approval-version match so post-approval edits re-block), and "Wire compliance-settings screen to manage rules, suppression, and disclaimers" (34cb1d18 — compliance-role CRUD on the existing compliance-settings shell 031d79fc, every change audited). Closes M2's success metric "suppression/disclaimer/approval rules are configurable and enforced by a callable pre-send check used by outreach (M6)". Anchored to architecture/security.md §"Outreach compliance controls" + §"Audit-log security" + §RBAC-SoD (one gate service = sole send-eligibility authority; non-bypassable choke point re-run at send time). Est. net LOC ~3,000–4,000, ≤~40 files. P-4 note: compliance-critical + non-bypassable gate → security-scope-tightened + SoD/RBAC gate expected at P-4 next wave; acceptance criteria authored to demand non-bypassability + SoD, gate itself not built here.
- N-1 judgment note: seed-candidate guard count returned 0 before authoring; the 3 pre-existing M1-follow-up rows (d7f716b4 AppShell-polish, 6fe232e3 auth-hardening, bfadcec1 test-fixture typing — all carry a wave_id, all parent_task_id NULL) were left untouched and are NOT part of this bundle.

## [2026-Q3] SoD approver is the compliance role only; admin cannot approve a send
**Category**: Security
**Status**: Active
**Context**: The wave-5 pre-send compliance gate enforces separation of duties (sender ≠ approver). The plan draft allowed the approver role to be either `compliance` or `admin`. jenny's P-4 iteration-1 review flagged this as a CRITICAL drift from `security.md` §RBAC-SoD, which specifies the approver must be `compliance` only, with the principle "no super-role shortcut around separation of duties."
**Decision**: The gate's SoD evaluator accepts an approval ONLY when the approver holds the `compliance` role and is a different person than the sender; `admin` is explicitly excluded as an approver. Settings-CRUD access on the compliance rules/suppression/disclaimer tables remains `compliance, admin` — admin manages configuration, which is a distinct authority from approving an individual send.
**Rationale**: A compliance separation-of-duties control is only meaningful if no single privileged role can both create and approve outreach. Allowing admin to approve would let an admin who also authored a send self-clear it, defeating the control and creating FINRA/SOX recordkeeping and audit exposure. Distinguishing "authority to configure" (admin) from "authority to approve a send" (compliance-only) preserves the control while keeping config management operable. Remediated doc-level at P-4 before B-block; live-verified at C-2 (admin-approver blocked, `sod/invalid-approver-role`).

## [2026-07-03] M2 → done; M3 → in_progress (N-1 milestone disposition, wave-5 close)
**Decision:** M2 (Compliance backbone: tamper-evident audit log + rules engine) closed `in_progress → done`. M3 (Deal sourcing & company/contact data) promoted `todo → in_progress`.
**Trigger:** N-1 Action 6 closure check (wave-5 close). M2 open_count=3 but ALL 3 open rows are the re-parented M1 non-core follow-ups (test-fixture typing bfadcec1; auth-hardening rate-limit/CSRF 6fe232e3; AppShell placeholder pages d7f716b4 — each retains an original wave-1/2/3 `wave_id`, parent_task_id NULL). They are NOT M2-scope work. M2's own `## Scope` names three components and ALL THREE are delivered across the 8 done tasks: (1) tamper-evident append-only audit-log service — HMAC-SHA256 hash-chain, INSERT-only grant + BEFORE UPDATE/DELETE trigger, content/payload hash distinct (wave 4: ec1f279d, a8b2b5a2, e6a4cbfe); (2) compliance rules engine — suppression_list, disclaimer_templates per-jurisdiction, compliance_rules, approval-gating + the callable non-bypassable pre-send check (wave 5: 0595a835, 95adac6c, 034463b1); (3) compliance-settings screen (031d79fc, 34cb1d18). M2's `## Success metric` is fully met: audit writes are hash-chained + integrity-verifiable with tamper detection (UPDATE/DELETE blocked); suppression/disclaimer/approval rules are configurable and enforced by a callable pre-send check. Wave 5 shipped LIVE (13e55ef), all gates APPROVED, real-browser 33/33, SoD admin-approver-BLOCKED live-verified. The success-metric clause "used by outreach (M6)" is an M6 forward dependency — the gate is a callable contract the M6 send-path will invoke (M6 `## Depends on: M2, M5`), NOT M2's own delivery. Judged shipped → closed.
**M2 follow-up disposition:** the 3 open M1-follow-ups re-parented `milestone_id M2 → M3` (they retain their original `wave_id`; parent_task_id NULL) so they stay claimable backlog and get re-triaged under the now-active M3 rather than orphaned or force-cancelled. After re-parenting, M2's own child set is fully terminal (0 open / 8 done) — closure invariant 3 satisfied honestly rather than by force-closing over open children.
**M3 promotion rationale (Action 8a):** M3 is the correct next active milestone — it is the front of the deal-flow pipeline (sourcing → mandates(M4) → matching(M5) → outreach(M6)) with NO `## Depends on`, i.e. the earliest un-blocked milestone. Although M6 carries `## Tier T1` (vs M3/M4/M5 at T2), M6 is dependency-blocked (`## Depends on: M2, M5`; M5 depends on M4 depends on M3) and cannot start now. With the compliance foundation (M1+M2) shipped, M3 advances the roadmap from "compliance backbone built" to the actual deal-flow product. M3 is `## Class product-feature`, so its wave-1 (wave 6) P-0 will run mvp-thinner.
**Consequence:** M3 is the active milestone; its queue has no fresh top-level seed (the 3 re-parented rows all carry a wave_id) → N-1 Action 7 fires milestone-decomposition against M3 for wave 6's first bundle.

## [2026-07-03] M3 (Deal sourcing & company/contact data): bundle authored — 4 tasks (first vertical slice: the deal-sourcing data spine — connection store + pluggable adapter → on-demand ingestion into raw_companies → deterministic dedupe into canonical companies+contacts with provenance → companies-contacts view/clean screen)
- caller: N-1-next-bundle
- decomposed by: milestone-decomposer sub-agent
- Slice: M3's wave-6 (first) vertical. M3 is freshly promoted (0 done tasks). The seed slice is the data backbone every other M3 scope item depends on: nothing (search UI, scheduled sync, enrichment fan-out) can be built before the connection store, staging table, canonical companies/contacts store, and pluggable adapter interface exist. True DB→service→job→API→UI vertical, NOT a horizontal layer. Seed "Stand up data-source-connections store + pluggable DataSourceAdapter interface" (ff378a95) = Drizzle schemas for data_source_connections + sync_runs + raw_companies staging + canonical companies/contacts + provenance joins (company_data_sources/contact_data_sources, non-null source_connection_id + ingested_at per databases.md invariant 3) + dedupe_candidates, the IDataSourceProvider interface in @dealflow/shared with ≥2 DI-registered adapters (provider_key → Railway-env credentials, never in-DB), and admin-only connection CRUD (M1 RolesGuard, every mutation audited via M2 AuditService). Siblings: "Build ingestion/ETL service + on-demand SourceSyncJob into raw_companies staging" (0241222b — IngestionService + BullMQ SourceSyncJob, idempotent upsert on (connection_id, external_id), ETL-module multi-provider fan-out across ≥2 sources, schema-drift quarantine); "Build dedupe engine promoting raw_companies to canonical companies+contacts with provenance" (db274731 — DedupeService deterministic-then-fuzzy entity resolution, non-destructive merge with merged_into_id, provenance rows per contributing source; ≥2-source dedupe → one canonical record with both provenance rows = the metric's "import deduped records with provenance" proof); "Build companies-contacts screen to view, filter, and clean deduped records" (f5771d13 — journey-map page 13; list/detail/provenance-trail + dedupe_candidates accept/reject/merge + archive/suppress, shared-Zod DTOs, design-system tokens → D-block runs). Together they demonstrate M3's full success metric end-to-end in one path (import from ≥2 sources → deduped provenance-tracked records → view/clean on companies screen).
- Deliberate deferrals to a later M3 bundle (kept out to hold this as one coherent vertical, ~4,000–4,800 net LOC, ≤~55 files, 1 seed + 3 siblings): the sourcing-workspace search-and-trigger page (page 12), scheduled/cron SourceSyncJob, and contact-ENRICHMENT via an external provider (email verification, decision-maker discovery). The seed's adapters may be sandbox/fixture-backed for this first bundle if real vendor SDK access is not yet provisioned — the load-bearing acceptance criterion is the pluggable interface + ≥2-provider fan-out + provenance from the start, not the specific vendor; a real vendor integration triggers P-3 SDK-research per external-sdk-integration-rules.md.
- Anchored to architecture/databases.md §companies_contacts + §data_source_connections + invariants 2/3/5, architecture/modules.md #3 (ingestion) + #4 (dedupe/enrichment) + #19 (admin data-source credentials) + §Conventions (adapter pattern, fan-out is ETL responsibility), architecture/sdks.md §3 (Deal-Source Data APIs) + §Conventions (Pluggable Provider Interface + Secrets via Railway Env). Reuses M1 RolesGuard (RBAC) + M2 AuditService (every connection/merge/clean mutation audited, append last-in-txn). P-4 note: M3 is ## Class product-feature → wave-6 P-0 runs mvp-thinner; credentials-handling (provider_key/env, pgcrypto for admin credential storage) touches secrets → security-scope-tightened gate expected at P-4; UI wave → D-block runs.
- N-1 judgment note: seed-candidate guard count returned 0 before authoring, exactly 1 after; the 3 pre-existing re-parented M1-follow-up rows (d7f716b4 AppShell-polish, 6fe232e3 auth-hardening, bfadcec1 test-fixture typing — all carry a wave_id, all parent_task_id NULL) were left untouched and are NOT part of this bundle (backlog re-triaged under M3 by future N-2, not siblings of this seed).

## Wave 6 — deal-sourcing schema reconcile + contact-level provenance preserved (P-4 remediation, 2026-07-03)
**Decision:** The deal-sourcing data spine adopts a staging(raw_companies)→canonical(companies/contacts) two-tier schema with company- AND contact-level provenance tables, superseding the pre-implementation databases.md §companies_contacts/§data_source_connections sketch (reconciled as-built there). Contact-level provenance (`contact_provenance`) is PRESERVED (not deferred) to honor databases.md reusability principle 3 — feature #9 (the data-quality screen shipping this wave) is a named consumer.
**Why:** The two-tier + dual provenance is architecturally stronger (non-destructive re-ingest, non-lossy cross-source dedup, traceable lineage) and is the correct foundation M4 (mandates) / M5 (matching) read. Deferred (documented): sync_runs history, real provider SDKs, scheduled sync, contact enrichment, sourcing-workspace page — none needed for M3's first-bundle metric (a deduped, provenance-tracked company universe visible + cleanable in the screen). SoD/compliance unaffected (external-party data; provider secrets Railway-env only).

[2026-07-04] M3 (Deal sourcing & company/contact data): bundle authored — 2 tasks (sourcing-workspace page /sourcing to search connected sources + trigger ingestion [SEED], plus first real DataSourceAdapter behind the existing pluggable interface + ETL fan-out [SIBLING] — the vertical slice that makes the M3 "run a search across ≥2 connected sources" success metric verifiable end-to-end)
- caller: N-1-next-bundle (wave 6 → wave 7 handoff, automatic mode)
- decomposed by: milestone-decomposer sub-agent
- note: real-adapter sibling is gated on the deal-source vendor-selection spend decision + SDK-research (sdks.md §3 Risk 1 + external-sdk-integration-rules.md) which routes to BOARD/ceo-agent at P-3 before any adapter code; no vendor presumed here. Fixture adapter remains a valid source so the seed is demonstrable even if the real-adapter gate defers. Pre-existing M1-follow-up rows (d7f716b4, 6fe232e3, bfadcec1) left untouched — not part of this bundle.

## [2026-07-04] M3 → done; M4 → in_progress (N-1 milestone disposition, wave-7 close — BOARD 7/7 APPROVE)
**Decision:** M3 (Deal sourcing & company/contact data) closed `in_progress → done`. M4 (Mandates & buyer universe) promoted `todo → in_progress`. Milestone-disposition routed to BOARD under automatic mode (decision-slug `N-1-milestone-disposition-M3-wave-7`); **7/7 APPROVE, no hard-stop veto** — clears both the 4+/7 default and 6+/7 strict thresholds.
**Trigger:** N-1 Action 6 closure check (wave-7 close). Substantively-complete judgment: M3's `## Success metric` (analyst runs a sourcing search across ≥2 connected sources → import deduped records with provenance → view/clean on companies screen) is proven END-TO-END on LIVE deployed state (waves 6+7, head-verifier V-3 APPROVED, real post-hydration DOM, HMAC-SHA256 audit chain intact — NOT mocked). All buildable, non-vendor-gated `## Scope` shipped across 5 done tasks: pluggable DataSourceAdapter interface + connections store (ff378a95), ingestion/ETL + on-demand sync (0241222b), dedupe engine raw→canonical with provenance (db274731), companies-contacts screen (f5771d13), sourcing-workspace /sourcing page (dfa5bd56). The only unshipped scope (a concrete REAL provider adapter + real contact-enrichment SDK) is vendor/spend-gated and founder-blocked, cleanly isolated behind the adapter seam. BOARD consensus (risk-manager, architect-reviewer): this is honest fixture-backed completion, NOT the "Hallucinated Milestone Completion" anti-pattern — the compliance-load-bearing property (dedupe provenance + audit chain) shipped and was live-verified. Keeping M3 open would stall the loop on a founder-only vendor decision with zero buildable seed.
**Child disposition (closure invariant 3 satisfied honestly — all 6 remaining children now terminal-or-removed, 0 open):**
- `345dfbc6` (first real DataSourceAdapter) → re-homed `milestone_id M3 → M9` (Integrations & insight), `parent_task_id` cleared to NULL (clean future M9 seed). Remains founder-blocked on vendor selection + account-issued API key + spend gate; re-enters the buildable queue when M9 activates AND the founder returns the already-surfaced vendor decision. The implied real contact-enrichment provider SDK travels with it.
- `b9141490` (in-page dedupe-preview modal) → `status='cancelled'` (WON'T-DO, redundant): the shipped `/sourcing/companies` review queue already delivers the accept/reject/merge clean workflow (per wave-7 P-1 split).
- `bfadcec1` (test-fixture typing) / `6fe232e3` (auth hardening) / `d7f716b4` (AppShell placeholder pages) → re-homed `milestone_id M3 → M4` (precedent-consistent with the M1→M2 and M2→M3 carries; retain original `wave_id`, `parent_task_id` NULL). Backlog re-triaged under the now-active M4 rather than orphaned under a closed milestone; NOT M4-scope seeds and do not gate M4.
**M4 promotion rationale (Action 8a):** M4 is the next un-blocked pipeline stage (sourcing M3 → mandates M4 → matching M5 → outreach M6), no `## Depends on`, `## Tier T2`, `## Class product-feature`. M6 is T1 but dependency-blocked (`Depends on: M2, M5`; M5 depends on M4). BOARD (founder-proxy, ceo-reviewer, competitive-analyst) concur M4 is next. M4 is product-feature → wave-8 P-0 runs mvp-thinner; UI wave (mandates-list/mandate-detail/mandate-new/buyer-universe) → D-block runs.
**Consequence:** M4 is the active milestone with no fresh top-level seed (only the 3 re-homed follow-ups, all carry a wave_id) → N-1 Action 7 fires milestone-decomposition against M4 for wave-8's first bundle.

[2026-07-04] M4 (Mandates & buyer universe): bundle authored — 3 tasks (first vertical slice: the mandate data spine + create/configure/list/detail lifecycle — the load-bearing container every other M4 scope item and M5 matching reads; buyer-universe builder deferred to a later M4 bundle)
- caller: N-1-next-bundle (wave 7 → wave 8 handoff, automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice: M4's wave-8 (first) vertical. M4 is freshly promoted (0 done tasks). The seed slice is the mandate spine every other M4 scope item depends on: buyer-universe assembly, list/detail, and submission to matching (M5) all read the mandate's seller/target profile + buyer criteria + compliance profile, so none can be built before the mandate exists. True DB→service→API→UI vertical, NOT a horizontal layer. Seed "Build mandate data spine + create/configure sell-side mandate flow" (ba0edebf) = Drizzle schema for mandates + mandate_buyer_criteria + mandate_compliance_profile (jurisdiction + disclaimer template + suppression scope, FK into the M2 compliance_rules/disclaimer_templates/suppression_list tables), MandateService.create/configure (all three written in one txn), POST mandate API guarded by M1 RolesGuard (advisor) with every mutation audited via M2 AuditService.append last-in-txn, shared-Zod contracts, and the mandate-new create form page. Compliance profile is stored/captured only this bundle — enforcement is the M6 pre-send gate, which will later read the profile persisted here. Siblings: "Build mandates-list page with status filters and list endpoint" (c070ca23 — GET list endpoint + mandates-list page, the advisor's home surface and the entry point into detail) and "Build mandate-detail page with profile, criteria, and compliance view" (50227055 — GET detail + PATCH configure endpoints, audited; mandate-detail page reviewing/editing all three parts, plus a labelled placeholder anchor for the not-yet-built buyer universe so it is the stable mount point for the next M4 bundle). Together they deliver the create→list→review/edit mandate lifecycle end to end — the first half of M4's success metric ("an advisor can create a sell-side mandate with buyer criteria + a compliance profile").
- Deliberate deferral to a later M4 bundle (kept out to hold this as one coherent ~3,400–4,300 net-LOC mandate-lifecycle vertical, ≤~45 files, 1 seed + 2 siblings): the buyer-universe builder + buyer-universe page (assemble candidate buyers from the M3 canonical companies store + prior deals, filter by criteria, enrich contacts, flag gaps, submit to matching) — the second, larger M4 vertical that reads FROM this mandate spine and the M3 company universe. It is the second half of M4's success metric ("an analyst can assemble and enrich a buyer universe for it that is ready to rank") and mandate-detail ships a placeholder anchor for it. Per the ritual size rubric, combining it with the mandate spine would overflow the wave.
- Anchored to architecture/services.md (mandate service module) + architecture/databases.md (mandate tables + FKs into M2 compliance tables), journey-map pages mandates-list / mandate-detail / mandate-new. Reuses M1 RolesGuard (RBAC advisor) + M2 AuditService (every mandate mutation audited, append last-in-txn) + M2 compliance_rules/disclaimer_templates/suppression_list (the compliance profile references them); buyer criteria dimensions align to the M3 canonical company fields the deferred buyer-universe builder will filter on. P-4 note: M4 is Class product-feature → wave-8 P-0 runs mvp-thinner; UI wave (mandate-new/mandates-list/mandate-detail) → D-block runs; compliance-profile handling touches the M2 compliance surface → SoD/compliance-scope attention expected at P-4 (profile stored not enforced this bundle).
- N-1 judgment note: seed-candidate guard count returned 0 before authoring, exactly 1 after; the 3 pre-existing re-homed M1-follow-up rows (d7f716b4 AppShell-polish, 6fe232e3 auth-hardening, bfadcec1 test-fixture typing — all carry a wave_id, all parent_task_id NULL) were left untouched and are NOT part of this bundle (backlog re-triaged under M4 by a future N-2, not siblings of this seed).

[2026-07-04] M4 (Mandates & buyer universe): bundle authored — 3 tasks (second and FINAL M4 vertical slice: the buyer-universe builder — assemble candidate buyers from the M3 canonical companies store, filter by the mandate's stored buyer criteria, enrich contacts + flag gaps, and mark ready-to-rank for the M5 matching handoff; the mandate spine having already shipped wave-8)
- caller: N-1-next-bundle (wave 8 → wave 9 handoff, automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice: M4's wave-9 (second, final) vertical, delivering the second half of M4's success metric ("an analyst can assemble and enrich a buyer universe for it that is ready to rank"). The wave-8 mandate spine (mandates + mandate_buyer_criteria + mandate_compliance_profile + MandateService + POST/PATCH/GET /mandates + mandate-new/mandates-list/mandate-detail) is done; mandate-detail already ships labelled placeholder anchors (Buyer Engine / Ranked Candidates / Pipeline — "coming in a later step", D6 decision) as the stable mount point for this bundle. True DB→service→API→UI vertical tied to one analyst workflow. Seed "Build buyer-universe data spine + assemble-and-filter service" (92a8ff3f) = ADDITIVE Drizzle schema for a buyer_universe container (one per mandate, FK→mandates) + buyer_universe_candidates (FK→M3 canonical companies + FK→buyer_universe, membership status + provenance), plus BuyerUniverseService that assembles candidates from the M3 canonical companies store (REUSED, not rebuilt) and filters them against the mandate's mandate_buyer_criteria (industry/sector, geo, size_band, deal_type persisted wave-8), exposed as shared-Zod API endpoints. RBAC per the metric: analyst is the primary actor (analyst can assemble), advisor + admin also permitted; every mutation audited via M2 AuditService.append last-in-txn; getUserWithRole actor id (wave-5 lesson). Siblings: "Build /buyer-universe page to assemble, filter, and review candidates" (394a60ba — the analyst UI mounting on the mandate-detail D6 anchors + the standalone /buyer-universe page named in M4 Scope; assemble → criteria-filter → review/include-exclude over the seed's persisted state; web-read-schema + SSR-hydrate reads per wave-6/7) and "Enrich buyer contacts, flag universe gaps, and mark ready-to-rank for matching handoff" (c907731f — contact-enrichment from the M3 contact tables, gap-flagging for candidates missing contacts/criteria data, and submit-to-matching that flips the buyer_universe to ready-to-rank + persists the candidate rows M5 consumes; extends the sibling-1 UI). Together they deliver assemble → filter → enrich → flag → ready-to-rank end to end for one mandate.
- M4/M5 boundary (HARD, reject-if-crossed): "submit to matching" here is ONLY the ready-to-rank status flip / handoff marker + the persisted buyer_universe rows M5 will read — it produces NO fit-scores, NO ranking, NO rationale, and makes NO Anthropic/LLM call. M5 owns deterministic pre-score + LLM ranked matching. No ranking column, no scoring, no LLM anywhere in this bundle.
- Reuse contract (reject-if-crossed): reuses M3 canonical companies + contact tables (source of candidate buyers + enrichment — no new company store invented), M4 mandate_buyer_criteria (filter dims), M1 RolesGuard (RBAC — analyst primary per metric, advisor/admin too), M2 AuditService.append (every buyer-universe mutation audited last-in-txn). All new schema ADDITIVE (new buyer_universe + buyer_universe_candidates tables FK→mandates + M3 companies; no destructive ORM ops, no refactor of the mandate spine / mandates-list / M3 surfaces).
- Sizing: est. net LOC ~3,000–4,500, ≤~50 files, 1 seed + 2 siblings (clean spine-bundle shape). Both siblings tightly coupled to the seed (same buyer-universe module), not horizontal jumps. P-4 note: M4 is Class product-feature → wave-9 P-0 runs mvp-thinner; UI wave (/buyer-universe + mandate-detail anchor wiring) → D-block runs; enrichment reuses M3 contact data (no external SDK gate this bundle — the vendor-gated real contact-enrichment provider travelled to M9 at wave-7 close and is NOT presumed here).
- N-1 judgment note: seed-candidate guard count re-confirmed 0 before authoring, exactly 1 after; the 3 pre-existing re-homed M1-follow-up rows (d7f716b4 AppShell-polish, 6fe232e3 auth-hardening, bfadcec1 test-fixture typing — all carry a wave_id, all parent_task_id NULL) were left untouched and are NOT part of this bundle (backlog re-triaged under M4 by a future N-2, not siblings of this seed). With the mandate spine (bundle 1) + buyer-universe builder (this bundle 2) both authored, M4's full ## Scope is now decomposed — this is the final M4 bundle.

[2026-07-04] M4 → done; M5 → in_progress; 3 non-M4 backlog tasks re-homed (N-1 milestone disposition, wave-9 close — BOARD 7/7 APPROVE, automatic mode)
- M4 (Mandates & buyer universe): in_progress → done. Success metric ("an advisor can create a sell-side mandate with buyer criteria + a compliance profile, and an analyst can assemble and enrich a buyer universe for it that is ready to rank") FULLY shipped + live-verified across two waves: wave-8 mandate spine (create/configure/list/detail + compliance profile, live e57be83) + wave-9 buyer-universe builder (assemble candidates from M3 canonical companies → filter by mandate_buyer_criteria → enrich contacts → flag gaps → submit ready-to-rank, live 937ae18, V-3 APPROVED, C-2 first-try, all 9 T-layers + V-block + L-block APPROVED, CHANGELOG 0.9.0). Honest close: metric live-verified end-to-end in real headless-chromium (not tickets-closed); ready-to-rank is a real persisted state (buyer_universe.status='submitted' + submit guards + M5-consumable candidate rows), NOT a stub — Hallucinated-Milestone-Completion explicitly avoided. M4 children at close: 6 done + 0 open (the 3 formerly-todo re-homed below before close, satisfying the 0-open-children closing invariant).
- Re-home (BEFORE M4 close): the 3 M1/M2-era foundation backlog tasks previously parked under M4 (NOT M4-scope) re-homed to their proper future milestones and cleared to clean seeds (wave_id → NULL, parent_task_id NULL): 6fe232e3 (auth hardening: rate-limiting, input validation, logout anti-CSRF) → milestone_id M4 → M10 (Advanced compliance & recordkeeping); d7f716b4 (AppShell polish: placeholder pages for role-nav items that 404) → M4 → M7 (Admin & settings); bfadcec1 (test-fixture typing in wave-1 health tests) → M4 → M7. Nothing dropped; each is a clean future todo seed under its new milestone. This ends the M1→M2→M3→M4 carry chain by routing to the milestones where the work actually belongs.
- M5 (AI buyer-seller matching, ranked + rationale): todo → in_progress. Roadmap-sequence next (sourcing M3 → mandates M4 → matching M5 → outreach M6); consumes wave-9's ready-to-rank buyer_universe natively; the flagship differentiator per the live bet "Integrated platform beats stitched-together tools for M&A" (AI ranked sell-side matching is the core value the integrated workflow exists to deliver). Single-active invariant held (M4 done before M5 promoted; verified 0 in_progress between the two flips). M5 has 0 child tasks post-promotion → N-1 Action 7 fires milestone-decomposition for wave-10's first bundle.
- BOARD (automatic mode, slug N-1-milestone-disposition-wave-9, strict Tier-3 6+/7 bar applied): 7/7 APPROVE, 0 HARD-STOP, 0 REJECT/ABSTAIN. Bench: founder-proxy, strategist, realist, user-advocate, industry-expert, risk-officer, counter-thinker. Full record: process/waves/wave-9/escalations/board-N-1-milestone-disposition-wave-9.md (archived with wave-9). Mirrors the wave-7 M3→done + M4→in_progress BOARD-routed precedent.
- Non-blocking dissent flags carried for action:
  * SECURITY-DEBT FLAG (counter-thinker + risk-officer + user-advocate): auth-hardening (6fe232e3, /auth/* rate-limiting + input validation + logout anti-CSRF) has now been carried untouched across M1→M2→M3→M4 and re-homed to M10 — a compliance-first product must NOT let this perpetually park. Founder-visible security-debt flag: acceptable to defer ONLY while signup stays invite-only (random 32-byte tokens) + 0 pilot users. The moment a public/pilot auth surface goes live, the /auth/* brute-force gap becomes a live exposure and 6fe232e3 must pull forward ahead of M10's normal schedule.
  * AppShell 404 (user-advocate): d7f716b4 → M7 must resolve before first real pilot login; nav⊆RBAC-by-construction holds the interim (unbuilt-route nav items only, 0 DAU).
  * WAVE-10 M5 carry-forwards (strategist / industry-expert / risk-officer / realist — enforce at wave-10 P-3/P-4): (a) the Anthropic/Claude LLM SDK is an external SDK → wave-10 P-3 runs external-sdk-integration-rules; (b) the LLM abstraction/gateway (provider-agnostic routing layer, Claude behind a NestJS interface) MUST land in the SAME wave that introduces the SDK — no hardcoded SDK calls (vendor-lock-in guard); (c) LLM must enter via a zero-retention enterprise DPA endpoint only (no CIM/seller-financial payloads to consumer LLM surfaces; no proprietary relationship-graph/matching-criteria into training sets); (d) explainable rationale generated over RBAC-scoped, single-mandate context only and logged as an audit read event (verify at M5 P-4, not deferrable to M6); (e) load-test + explainability evidence required at M5 P-4 (latency / degradation paths / black-box auditability / per-decision cost); (f) LLM API SPEND surfaces as a founder Tier-3 (money) decision at wave-10 P-0/P-4 (likely a cost-ceiling, not a vendor pick — Claude is the established engine). All NON-BLOCKING for this disposition: promoting M5 is a status flip that commits no spend and integrates no SDK today (grep-confirmed: no Anthropic SDK in the tree yet); those gates land inside wave-10 with their own verdicts.

[2026-07-04] M5 (AI buyer-seller matching, ranked + rationale): bundle authored — 3 tasks (first M5 vertical slice: the deterministic match spine + rule-based pre-score service, deferring the LLM-assisted ranking + explainable rationale to a later M5 bundle)
- caller: N-1-next-bundle (wave 9 → wave 10 handoff, automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice: M5's wave-10 (first) vertical. M5 is freshly promoted (0 done tasks; seed_candidates 0 → 1 after authoring). M5's full success metric = deterministic pre-score + LLM ranked matching + explainable rationale + accept/reject/flag shortlist — too big for one wave. This first slice ships the DETERMINISTIC match spine only, with ZERO Anthropic/Claude/LLM dependency and ZERO API spend — the load-bearing container every later M5 piece reads and enriches. This directly executes the wave-9-close BOARD carry-forwards (a–f): the LLM SDK + provider-agnostic gateway + zero-retention DPA + rationale-as-audit-read + load-test/explainability evidence + LLM-spend Tier-3 all land in the LATER M5 bundle that introduces the SDK, NOT here. Seed "Build deterministic match spine + rule-based pre-score service" (47ed7ddd) = ADDITIVE Drizzle schema for a match_run container (FK→mandates + FK→buyer_universe, one run per submitted buyer_universe) + match_candidates (FK→buyer_universe_candidates + FK→match_run, integer fit_score 0-100, structured deterministic score-breakdown/component-provenance jsonb, disposition status pending/accepted/rejected/flagged), plus a MatchingService that reads a SUBMITTED buyer_universe (its 'included' candidates + attached M3 contacts + the mandate's mandate_buyer_criteria industry/geo/size_band/deal_type + contact-completeness) → computes a DETERMINISTIC rule-based integer fit_score (dimensions absent on M3 companies scored only where present, unsupported-dimension provenance — graceful degradation mirroring wave-9's unsupportedDimensions) → persists ranked match_candidates. Shared-Zod API, RBAC advisor-primary via getUserWithRole (wave-5 actor-id lesson), every mutation audited via M2 AuditService.append last-in-txn. Siblings: "Build /matches-shortlist ranked-list page with fit-scores and disposition" (fb82d339 — the /matches-shortlist page named in M5 Scope + mount on the mandate-detail D6 "Ranked Candidates" anchor; ranked list ordered by fit_score with the per-dimension deterministic score breakdown + accept/reject/flag controls; web read-schema passthrough + SSR-hydrate per wave-6/7) and "Persist accept/reject/flag shortlist with ready-for-outreach handoff marker" (f74dce45 — disposition persistence audited last-in-txn + build-shortlist roll-up of accepted candidates + a ready-for-outreach status flip on match_run that M6 will consume; extends the sibling-1 UI). Together they deliver rank → view/explain → accept/reject/flag → build shortlist → mark ready-for-outreach end to end for one mandate, on deterministic scores alone.
- HARD BOUNDARIES (reject-if-crossed, enforced in every task description): (1) NO Anthropic/Claude/LLM call, NO BullMQ async job, NO rationale-text generation this bundle — fit_score is a deterministic rule-based integer 0-100 only; the LLM-assisted ranking + explainable rationale is a LATER M5 bundle carrying the wave-9 BOARD gates a–f. (2) M5/M6 boundary: shortlist→outreach is ONLY a ready-for-outreach status flip / handoff marker + persisted accepted rows M6 reads — NO outreach send, NO email, NO compliance pre-send gate (that is M6). (3) All new schema ADDITIVE (match_run + match_candidates FK→existing buyer_universe/candidates/mandates; no destructive ORM, no refactor of the shipped M4/M3 surfaces; rollback note = drop the two new tables + enums, no existing table altered). (4) Reuse M4 buyer_universe + M3 companies/contacts + mandate_buyer_criteria + M1 RBAC + M2 AuditService — no new stores.
- Deliberate deferral to a later M5 bundle: the LLM-assisted ranked matching + explainable rationale over this deterministic pre-score (Anthropic/Claude behind a provider-agnostic NestJS gateway, async BullMQ job, zero-retention enterprise DPA endpoint, RBAC-scoped single-mandate context, rationale logged as an audit read event, load-test + explainability + per-decision cost evidence at P-4, LLM spend as a founder Tier-3 money decision). Held out to keep this first slice free of the external-SDK + money gate and shippable/verifiable with zero spend; it reads FROM the match_run/match_candidates spine this bundle persists.
- Sizing: est. net LOC ~2,800–4,200, ≤~50 files, 1 seed + 2 siblings (clean spine-bundle shape; both siblings tightly coupled to the seed's matching module, not horizontal jumps). P-4 note: M5 is Class product-feature → wave-10 P-0 runs mvp-thinner; UI wave (/matches-shortlist + mandate-detail anchor wiring) → D-block runs; NO external SDK gate this bundle (deterministic-only — the Anthropic SDK + external-sdk-integration-rules + LLM-spend Tier-3 travel with the LATER LLM-rationale bundle per BOARD carry-forwards a/f).
- N-1 judgment note: seed-candidate guard count returned 0 before authoring, exactly 1 after. M5 had 0 pre-existing child rows at promotion, so this bundle is the sole M5 backlog. M5's ## Scope is NOT yet fully decomposed — the LLM-rationale second vertical remains for a future M5 bundle.

[2026-07-04] M6 (Compliant outreach & pipeline — one live mandate, end-to-end): bundle authored — 3 tasks (first M6 vertical slice: versioned template library + outreach composer + the NON-BYPASSABLE server-side pre-send compliance gate — the load-bearing container the rest of M6 reads; compliant send + email SDK + tracking, approval-queue screen, pipeline, and recordkeeping-export all deferred to later M6 bundles)
- caller: N-1-next-bundle (wave 10 → wave 11 handoff, automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice: M6's wave-11 (first) vertical, executing the BOARD 7/7-approved sequencing on its binding conditions. M6 freshly promoted (0 done, 0 open tasks; seed_candidates 0 → 1 after authoring). M6's full ## Scope (templates + composer + pre-send gate + compliant send + tracking + approval queue + audit/recordkeeping-export + pipeline) is far too big for one wave. This first slice ships the templates + composer + pre-send-gate container ONLY — a true DB→service→API→UI vertical for the pre-send-gated outreach path. Seed "Build versioned template library spine + templates-library page" (102a2f00) = ADDITIVE Drizzle outreach_templates + outreach_template_versions (content_hash + approval-version binding + required-compliance-block FK into M2 disclaimer_templates) + TemplateService (create/draftNewVersion/requestApproval; editing an approved body mints a new version and invalidates approval — version-binding invariant mirroring M2 disclaimer approval-version binding, product-decisions #4), RBAC via M1 RolesGuard (approver-is-compliance-role), every mutation audited via M2 AuditService.append last-in-txn (getUserWithRole actor id), + the templates-library page. Siblings: "Build outreach composer + non-bypassable server-side pre-send compliance gate" (e90a4a99 — OutreachComposerService.composeFromTemplate reading an approved-and-hash-matched template version + populating recipients from the M5 DETERMINISTIC shortlist [match_run.ready_for_outreach + accepted match_candidates, live @ 0075a20]; the ComplianceGateService.evaluate structurally non-bypassable + SERVER-SIDE, consuming the shipped M2 rules engine [ComplianceGateService/compliance_rules/suppression_list/disclaimer_templates, wave-5 live] and writing its pass/block verdict to the M2 audit log in-txn; the outreach-composer page; STOPS at "gate-passed, send-eligible" — no send) and "Enforce sender!=approver SoD + template version-binding at compose/send-eligibility" (2601ba33 — SoD asserted server-side reusing M1 RolesGuard + the wave-5 SoD precedent [admin cannot self-approve], version-binding refusal of stale/edited approvals at compose + gate, composer/library UI surfacing approver-identity + version-match state; all audited last-in-txn). Together: draft template → version + require compliance blocks → request compliance approval (SoD-enforced) → compose from an approved-hash-matched version against the deterministic shortlist → non-bypassable server-side pre-send compliance check writing to the audit log → gate-passed/send-eligible, end to end for one mandate.
- Compliance-critical acceptance demanded AT SPEC TIME (compliance-first DealFlow, per binding constraint 3): (a) pre-send gate structurally non-bypassable + enforced SERVER-SIDE in NestJS, consuming M2 rules engine, writing verdict to M2 audit log in-txn (last-in-txn actor-id, wave-5 lesson); (b) Separation of Duties sender≠approver asserted server-side reusing M1 RolesGuard + wave-5 SoD precedent (admin cannot self-approve); (c) template version-binding — editing an approved template invalidates its approval (content-hash approval-version match, mirroring M2 disclaimer approval-version binding, product-decisions #4); (d) every mutation audited via M2 AuditService.append last-in-txn (getUserWithRole actor id). All three siblings carry these in their task descriptions.
- HARD BOUNDARIES (reject-if-crossed at N-2 re-gate + wave-11 build, enforced in every task description): (1) VERTICAL slice — DB→service→API→UI for the pre-send-gated outreach path, NOT a horizontal layer. (2) NO ghost dependency on the UNBUILT M5 LLM-rationale half or any unmerged PR — buildable on ALREADY-SHIPPED M2 rules engine (wave-5) + M2 AuditService (HMAC-SHA256 hash-chain, INSERT-only, wave-4) + M5 DETERMINISTIC shortlist (wave-10, live @ 0075a20); ZERO live Anthropic/Claude call, ZERO Anthropic SDK — the "AI-assisted drafting" in M6 ## Scope is a LATER M6 bundle carrying the SAME founder LLM-spend Tier-3 gate M5's LLM bundle carries; drafting here is manual/structured only. (3) SDK-free + spend-free — the transactional-email provider SDK carries its own external-SDK + spend gate in a later M6 send bundle; this bundle stops at "gate-passed, send-eligible". (4) All new schema ADDITIVE (outreach_templates + outreach_template_versions + outreach_messages/draft + compliance_gate_verdicts, FK into M2 compliance tables + M5 match_run/match_candidates + M4 mandates + M1 users); NO destructive ORM, NO refactor of shipped surfaces; rollback = drop the new tables.
- Deliberate deferral to SEPARATE later M6 bundles (kept out to hold this as one coherent ~3,000–4,500 net-LOC / ≤~50-file single-session vertical, 1 seed + 2 siblings): the compliant SEND + transactional-email-provider SDK + webhook ingestion for opens/clicks/replies/bounces (own external-SDK + spend gate); the standalone compliance approval QUEUE screen (compliance-queue page — the SoD ENFORCEMENT lands this bundle, the dedicated queue UI is later); pipeline/deal-stage tracking (pipeline page); audit-log + recordkeeping-export screens.
- Sizing: est. net LOC ~3,000–4,500, ≤~50 files, 1 seed + 2 siblings (clean spine-bundle shape; both siblings tightly coupled to the seed's templates/composer/gate module, not horizontal jumps). P-4 note (for wave-11 P-4, NOT run now): compliance-critical → security-scope-tightened + SoD/RBAC gate expected at P-4; a concurrent-send P99 latency LOAD-TEST on the synchronous non-bypassable pre-send gate is REQUIRED at P-4 (realist/risk-officer BOARD dissent — the gate is on every message's critical path); M6 Class product-feature → wave-11 P-0 runs mvp-thinner; UI wave (templates-library + outreach-composer pages) → D-block runs.
- N-1 judgment note: seed-candidate guard count returned 0 before authoring, exactly 1 after. M6 had 0 pre-existing child rows at promotion, so this bundle is the sole M6 backlog. M6's ## Scope is NOT yet fully decomposed — the AI-assisted drafting half, the compliant send + tracking, the approval-queue screen, pipeline tracking, and recordkeeping-export all remain for future M6 bundles.

---

[2026-07-04] M5 → blocked (founder LLM-spend hold); M6 → in_progress; wave-11 seeded on M6 (N-1 milestone disposition, wave-10 close — BOARD 7/7 APPROVE, automatic mode, path b)
- **M5 (AI buyer-seller matching, ranked + rationale): in_progress → blocked.** M5 is NOT complete — its ## Success metric requires "explainable rationale per buyer" and the flagship LLM-assisted ranked-matching half (Anthropic/Claude behind a provider-agnostic gateway, async BullMQ, explainable rationale) is genuinely UNBUILT (verified: all 3 M5 done tasks are deterministic — pre-score service 47ed7ddd, shortlist page fb82d339, persist-handoff f74dce45; zero LLM/rationale/BullMQ/Anthropic task exists). M5 explicitly NOT closed (Hallucinated-Milestone-Completion avoided — the deterministic half shipping does not satisfy the metric). Blocked status = "external hold" per roadmap-lifecycle: the M5 LLM bundle carries a build-time FOUNDER money gate (per wave-9-close carry-forwards (b) the LLM gateway must land IN the same wave as the SDK, and (e) load-test + explainability evidence at P-4 ⇒ a live Claude call is needed in-wave ⇒ the recurring LLM API spend must be committed up-front; under automatic mode a money commitment routes to the FOUNDER). Blocking (vs leaving in_progress) is required by the single-active invariant — only one milestone may be in_progress, and M6 becomes active; blocked keeps M5 + its pending LLM bundle ON the roadmap, tracked and re-surfaceable, NOT dropped.
- **M6 (Compliant outreach & pipeline — one live mandate, end-to-end): todo → in_progress.** The highest-tier (T1) buildable milestone and the "one live mandate end-to-end" proof of the integrated-platform bet. Now unblocked: ## Depends on M2 (done) + M5 — and the DEPENDENCY M6's outreach actually consumes is the M5 DETERMINISTIC shortlist (match_run.ready_for_outreach + accepted match_candidates), which SHIPPED wave-10 and was live-verified @ 0075a20 (handoff-guard 400/2xx/idempotent, HMAC audit chain ok:true). M6's first bundle (templates + composer + non-bypassable server-side pre-send compliance gate) is buildable WITHOUT the founder LLM-spend decision and with ZERO Anthropic/email SDK. Single-active invariant verified: 0 in_progress between the M5→blocked and M6→in_progress flips; exactly 1 in_progress (M6) after.
- **BOARD (automatic mode, slug N-1-milestone-disposition-wave-10, strict Tier-3 6+/7 bar applied): 7/7 APPROVE, 0 HARD-STOP, 0 REJECT/ABSTAIN** on path (b) — promote M6, keep M5 (blocked) with its LLM bundle pending, surface the LLM-spend to the founder non-blocking. Bench: founder-proxy, strategist, risk-officer, counter-thinker, industry-expert, user-advocate, realist. Load-bearing verified facts across seats: (i) M6's shortlist dependency reads the DETERMINISTIC scorer output (match_candidates.score_breakdown is structured JSONB, NOT rationale TEXT — enforced boundary test matching.scorer.spec.ts) → M6's send-path has ZERO LLM coupling, no rework/deadlock (risk-officer, counter-thinker, realist); (ii) vertical precedent blesses shipping compliant outreach before AI rationale — DealCloud built its franchise on process-compliance/RBAC/audit with AI intro-drafting layered years later (industry-expert); (iii) the shortlist is already explainable via structured deterministic drivers (portfolio overlap, geo, deal-size fit) so outreach-before-LLM-prose is not a black-box gap (user-advocate); (iv) M6 is genuine Leverage not a Placebo dodge — the compliance-first outreach wedge is a live founder bet (strategist, founder-proxy).
- Convergent NON-BLOCKING dissent flags carried for wave-11 action (NOT blocking this disposition): (1) the M6 first-bundle pre-send compliance gate MUST be server-side non-bypassable + SoD (sender≠approver) + audited-in-txn, and a concurrent-send P99 latency LOAD-TEST on the synchronous gate is REQUIRED at M6 P-4 (realist/risk-officer/user-advocate/industry-expert — the gate is on every message's critical path); (2) M6's B-block must run the security-scope-tightened + SoD/RBAC gate at P-4 (compliance-critical, not optional). Both are already written into the M6 first-bundle task descriptions as P-4 flags.

[2026-07-04] FOUNDER DECISION SURFACED (non-blocking) — LLM matching-rationale API spend ceiling — Status: Deferred (awaiting founder)
- **Decision type:** Tier-3 money commitment. Routed to the FOUNDER under automatic mode (money commitments route to the founder; BOARD resolves sequencing but not spend).
- **What it is:** DealFlow's flagship AI matching-rationale feature (the "why this buyer" explainable ranking on top of the shipped deterministic shortlist) needs a live Anthropic/Claude API call to build and verify. Claude is DealFlow's ESTABLISHED engine, so this is very likely a bounded monthly cost-ceiling to set, NOT a from-scratch vendor pick (per wave-9-close carry-forward (f)). The founder is asked to set a monthly spend ceiling for the AI-matching-rationale feature.
- **Why non-blocking / why the loop continues RUNNING:** there IS buildable, bet-aligned, spend-free work — wave 11 ships M6's compliant-outreach vertical (templates + composer + non-bypassable pre-send gate), which delivers real end-to-end value while this decision awaits an answer. The loop does NOT hard-pause (always-on rule 13 — no measured pause trigger fired; a concrete buildable seed exists).
- **Re-surface trigger (guard against Premature-Archival / silent orphaning of the flagship differentiator, per counter-thinker/strategist/risk-officer dissent):** when the founder returns the spend ceiling, M5 flips blocked → in_progress and its LLM-rationale bundle is decomposed + seeded (carrying wave-9 BOARD carry-forwards a–f: external-sdk-integration-rules at P-3, provider-agnostic gateway landing WITH the SDK, zero-retention enterprise DPA endpoint only, RBAC-scoped single-mandate context, rationale-as-audit-read verified at P-4, load-test + explainability + per-decision-cost evidence at P-4). If M6 fully ships before the founder answers, N-1 MUST re-surface this decision at that wave's close rather than let M5 sit blocked indefinitely — the LLM rationale is the flagship differentiator and must not silently orphan.

---

[2026-07-05] M6 (Compliant outreach & pipeline — one live mandate, end-to-end): bundle authored — 3 tasks (pipeline/deal-stage tracking vertical: pipeline+pipeline_events DB spine + PipelineService, pipeline board API+RBAC+page, per-deal event timeline feeding the audit log)
- caller: N-1-next-bundle (wave-11 close; automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice rationale: M6's SECOND vertical (after wave-11's templates+composer+non-bypassable-pre-send-gate foundation, live @ b0be1c4). Chosen over the remaining M6 ## Scope items because it is the strongest BUILDABLE, no-new-credential, spend-free vertical: it manages ALREADY-PERSISTED `outreach` send_eligible records + accepted `match_candidates` through fixed pipeline stages WITHOUT sending email or ingesting webhooks. Advances the M6 ## Success metric phrase "buyers advance through the pipeline" — the pipeline STRUCTURE + manual stage transitions half. The AUTOMATED "replies/opens advance buyers" half stays deferred with the send/webhook bundle (credential-gated).
- Implements product-decision #137: FIXED stage enum (shortlisted→contacted→engaged→diligence→offer→closed/withdrawn) + a SEPARATE append-only `pipeline_events` table for notes/transitions feeding the M2 audit log; advisor-configurable per-mandate stages remain H2-deferred. The `pipeline` page is an already-designed+approved screen (product-decision #80; canonical at design/pipeline.html).
- Bundle: seed "Build pipeline + pipeline_events spine + PipelineService (enroll + stage transitions)" (07989285-7e64-4f26-a3de-1954ba89a5c7 — additive Drizzle pipeline + pipeline_events tables FK into mandates/match_candidates/outreach/users, fixed `pipeline_stage` enum, PipelineService enroll+transition, every mutation audited via M2 AuditService.append last-in-txn, RBAC via M1 getUserWithRole). Siblings: "Ship pipeline board API + RBAC + interactive pipeline page" (d1940142-e962-48cd-b1eb-26d0c79e98dd — NestJS controller + M1 RolesGuard + the stage-columned pipeline board matching design/pipeline.html; server-side illegal-transition + role rejection) and "Ship per-deal pipeline event timeline (notes + transition history, audited)" (45b259e1-d0d4-40b1-b09b-aeab25971700 — PipelineService.addNoteAsActor appending pipeline_events + mirroring to the M2 audit log last-in-txn; append-only notes; RBAC-guarded timeline read + UI panel on the deal view).
- Buildability confirmation: buildable WITHOUT any founder credential — NO email-provider API key, NO LLM/Anthropic spend, NO new external SDK, NO webhook ingestion, NO send path. All new schema additive (rollback = drop the two new tables + the new enum). Builds only on shipped-and-live surfaces: outreach send_eligible records, match_run/match_candidates, mandates, M2 audit log (audit.service.ts HMAC chain), M1 RBAC.
- Deferral note: the DEFERRED M6 ## Scope items remain founder-gated and were correctly NOT seeded — AI-assisted template drafting (LLM-spend, awaiting founder per founder-decision-llm-matching-spend.md) and compliant SEND via transactional-email provider + webhook ingestion for opens/clicks/replies/bounces (account-issued email-provider API key + EMAIL_WEBHOOK_SECRET, product-decision #141). The standalone compliance approval-QUEUE screen and the audit-log/recordkeeping-EXPORT screens remain buildable-without-credential and are candidates for the next M6 bundle.

[2026-07-05] M6 (Compliant outreach & pipeline — one live mandate, end-to-end): bundle authored — 3 tasks (audit-log + recordkeeping-EXPORT vertical: filtered read + hash-chain integrity-verify API [SEED], verifiable FINRA/SOX export-package generator, and the /compliance/audit-log page)
- caller: N-1-next-bundle (wave-12 close; automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice rationale: M6's THIRD vertical (after wave-11 templates+composer+non-bypassable-pre-send-gate and wave-12 pipeline+PipelineService+board+timeline). Chosen per product-decision #306's buildable-without-credential disposition: the audit-log/recordkeeping-EXPORT screen is the STRONGEST remaining spend-free, no-new-credential vertical and the compliance wedge's defensibility proof — it reads the EXISTING M2 immutable hash-chain (audit.service.ts HMAC-SHA256, AuditVerifier.verifyChain) plus pipeline_events/outreach and adds a compliance recordkeeping review + verifiable export surface. Advances the M6 ## Success metric phrase "compliance can export a verifiable recordkeeping package". The compliance approval-QUEUE screen (also buildable-without-credential) was deliberately NOT bundled to avoid bloat — it is a distinct enforcement-review surface and stays the candidate for the next M6 bundle.
- Bundle: seed "Build audit-log recordkeeping API: filtered read + hash-chain integrity verify" (36a17c81-3778-4594-a7d1-4b1977e5b5a0 — NestJS filterable/paginated read over the immutable audit_log + GET verify endpoint wrapping the EXISTING AuditVerifier.verifyChain; RBAC via M1 RolesGuard + getUserWithRole; READ-ONLY over the hash-chain). Siblings: "Generate verifiable FINRA/SOX recordkeeping export package (mandate/time-scoped)" (20c479db-d8ba-4ae3-9a64-cd3cc7874a27 — export service+endpoint assembling scoped entries + hashes + verifyChain result + manifest into a deterministic download; export action recorded last-in-txn via M2 AuditService.append; export restricted to compliance/admin) and "Ship audit-log & recordkeeping-export page (/compliance/audit-log)" (10ee0ec4-c34d-4899-b39f-43aed12b9616 — Next.js page per design/audit-log-export.html: filterable immutable-log table + integrity badge + export panel; role-scoped rendering; read-only over the log).
- Buildability confirmation: buildable WITHOUT any founder credential — ZERO email-provider API key, ZERO EMAIL_WEBHOOK_SECRET, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO send path, ZERO webhook ingestion. Read/export surface only; the sole write is an APPEND of the export event via M2 AuditService (never an edit/re-order of the hash-chain). All new schema (if any) additive (rollback = drop added index/manifest table). Builds only on shipped-and-live surfaces: M2 audit log + AuditVerifier, M1 RBAC (RolesGuard, getUserWithRole), wave-11 outreach foundation, wave-12 pipeline + pipeline_events.
- Deferral note: DEFERRED M6 ## Scope items remain founder-gated and were correctly NOT seeded — compliant SEND + event tracking (transactional-email-provider API key + EMAIL_WEBHOOK_SECRET, product-decision #141), automated reply/open-driven pipeline stage-advance (depends on send/webhook), AI-assisted template drafting (LLM-spend Tier-3 gate). The standalone compliance approval-QUEUE screen stays the next buildable-without-credential candidate.

[2026-07-06] M6 (Compliant outreach & pipeline — one live mandate, end-to-end): bundle authored — 3 tasks (compliance-hardening vertical: mandate-derivation real-DB e2e for the scoped recordkeeping export [SEED], producer-side gate mandate-attribution, and the /compliance/queue approval-queue screen)
- caller: N-1-next-bundle (wave-13 close; automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Slice rationale: M6's FOURTH vertical (after wave-11 templates+composer+non-bypassable-pre-send-gate, wave-12 pipeline+PipelineService+board+timeline, wave-13 audit-log+recordkeeping-EXPORT). This is the buildable-without-credential M6 compliance-hardening remainder that closes two wave-13-surfaced follow-ons AND ships the last spend-free M6 UI surface. Advances the ## Success metric phrases "send a compliance-checked, APPROVED, tracked outreach" (the /compliance/queue screen) and "compliance can export a verifiable recordkeeping package" (making the mandate-scoped export trustworthy via the seed e2e + gate mandate-attribution). All three read only shipped-and-live surfaces (M2 immutable audit log + AuditVerifier hash-chain, M1 RBAC/SoD, wave-11 composer + non-bypassable pre-send gate + sender!=approver SoD, wave-12 pipeline, wave-13 recordkeeping-export API + /compliance/audit-log page).
- Bundle: seed "Add mandate-derivation real-DB e2e for the recordkeeping scoped-export" (07bd1e1a-d71b-4e31-bc75-95de5a48aeef — HARD-GATED per V-2 wave-13 DEV-2: seed multi-producer audit rows under one mandate against a REAL database, assert the scoped export captures ALL mandate-derivable producers AND excludes gate-evaluate rows [resourceType='outreach-template-version', cross-mandate reusable], reuse the wave-12 race-safe shared migrate helper; the mandate-scoped export must NOT back a live regulator request until this lands). Siblings: "Record mandate/outreach context on the compliance gate audit row" (487b0f0c-bc4b-49f3-980f-07fd4f3503bc — wave-13 B-6 H1 producer-side fix: gate-evaluate rows carry mandate/outreach context so a mandate-scoped export includes the allow/block decision without over-capturing other mandates'; additive audit-metadata only, no chain-format break, non-bypassable gate behavior unchanged) and "Build the compliance approval-queue page (/compliance/queue)" (f5074df8-bd4e-4e39-864c-94574fecd9be — role-scoped compliance/approver screen listing pending-approval outreach with approve/reject honoring the shipped sender!=approver SoD, non-approver RBAC-blocked, each action audited last-in-txn via M2 AuditService.append; the last M6 ## Scope Pages item — compliance-queue — not yet shipped).
- Buildability confirmation: buildable WITHOUT any founder credential — ZERO email-provider API key, ZERO EMAIL_WEBHOOK_SECRET, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO send path, ZERO webhook ingestion. All writes are additive audit-metadata + APPENDs via M2 AuditService (never edits/re-orders of the hash-chain); any new schema is additive (rollback = drop the added column/index). Builds only on shipped-and-live surfaces.
- Deferral note: DEFERRED M6 ## Scope items remain founder-gated and were correctly NOT seeded — compliant SEND + event tracking (transactional-email-provider API key + EMAIL_WEBHOOK_SECRET, product-decision #141), automated reply/open-driven pipeline stage-advance (depends on send/webhook), AI-assisted template drafting (LLM-spend Tier-3 gate, awaiting founder per founder-decision-llm-matching-spend.md). With this bundle, the buildable-without-credential M6 remainder is essentially exhausted — the next M6 disposition (once these ship) is likely closure-pending-founder-credential rather than another buildable bundle.

---

[2026-07-06] M6 (a068dc3d): in_progress → blocked; M7 (08d3053a): todo → in_progress — roadmap disposition at wave-14 close (N-1)
- caller: N-1 (head-next, N-block gate) · mode: automatic · decision-slug: N-1-milestone-disposition-wave-14 · routed to BOARD (Tier-3: next-milestone-priority is a product/taste call per always-on rule 17)
- BOARD outcome: **7/7 APPROVE, 0 HARD-STOP** (clears Tier-3 6+/7 bar). Full record: process/waves/wave-14/escalations/board-N-1-milestone-disposition-wave-14.md
- M6 → BLOCKED (external hold): all 12 M6 child tasks done; the 5 ## Scope Pages (templates-library, outreach-composer, compliance-queue, audit-log-export, pipeline) ALL shipped and live. M6 ## Success metric (send→track→advance end-to-end) is UNMET — the remaining scope is 100% founder-gated: (a) compliant SEND + webhook event-tracking (transactional email-provider API key + EMAIL_WEBHOOK_SECRET, product-decision #141), (b) reply/open-driven pipeline auto-advance (depends on webhook), (c) AI-assisted template drafting (LLM-spend, awaiting founder per founder-decision-llm-matching-spend.md). The brain CANNOT author these (account-issued credential + spend commitment per always-on rule 6). Hallucinated-Milestone-Completion explicitly AVOIDED — M6 is NOT falsely marked done; `blocked` = the honest "external hold" state (mirrors the wave-10 M5→blocked precedent). M6 + its founder-gated remainder stay ON the roadmap, tracked and reversible (flip back to in_progress when #141 lands).
- M7 → IN_PROGRESS (promotion): M7 "Admin & settings" is the highest-tier todo (H1/T3 vs M8 H2/T4, M10 H2/T4), same H1 horizon as M6, and its ## Scope (sending-domain DKIM/SPF/DMARC verification, data-source connection mgmt, user mgmt, workspace/sending-identity settings) is on the CRITICAL PATH to eventually unblocking M6's compliant send. Serves both live founder bets (integrated platform; compliance-first outreach wedge). Also re-homes wave-9-close AppShell-404 polish + test-fixture-typing debt per that milestone's prior routing.
- BOARD dissent folded into M7 decomposition + P-block: (1) M7 first bundle scoped to BUILDABLE-WITHOUT-CREDENTIAL slices (Firm Profile CRUD, Default Compliance Profile cascade, user-management/RBAC, credential-management form UI); do NOT seed DKIM/SPF/DMARC record-GENERATION or live-verify (credential-seamed → would re-import the #141 stockout) — the paste-key management form is buildable, the ESP-minted record generation is deferred with #141. (2) M7 P-block MUST run security-scope-tightened + SoD/RBAC gate; audit/compliance tables append-only (WORM). (3) Composer must honestly surface not-yet-sendable state (no dead Send button) once M7 exposes sending settings.
- Founder deferrals re-surfaced (non-blocking, loudly framed): email-provider credential #141 is now the TRUE M6-completion blocker (its re_surface_trigger fired — M6 buildable scope exhausted); LLM-spend still pending. Both carried to process/session/updates/ + .last-wave-completed pending_founder_decisions with a wave-count watchdog on M6's BLOCKED state.

---

[2026-07-06] M7 (Admin & settings): bundle authored — 4 tasks (M7 FIRST vertical: user-management admin surface [invite/assign-role/deactivate + server-side last-admin guard + SoD, SEED], workspace + firm-profile + default-compliance-profile settings, data-source connection admin UI + encrypted-at-rest credential form, and the re-parented AppShell placeholder/TopBar-title polish)
- caller: N-1-next-bundle (wave-14 close; automatic mode — M7 freshly promoted this wave)
- decomposed by: milestone-decomposer sub-agent
- Slice rationale: M7's FIRST vertical (M7 promoted todo→in_progress at wave-14 close, 0 done tasks). Directly executes product-decision #331 — first bundle scoped to the BUILDABLE-WITHOUT-CREDENTIAL M7 slices only (user-management/RBAC, Firm Profile CRUD, Default Compliance Profile cascade, credential-management FORM UI). User management is the load-bearing seed: workspace + integration settings are only meaningful once a firm manages its own users/roles, and it ships the live role-mutation path d7f716b4 flagged as previously untestable. All four build ONLY on shipped-and-live surfaces — M1 RBAC (RolesGuard + @Roles, AuthRepository.getUserWithRole, SessionGuard), M2 immutable audit log (AuditService.append HMAC-SHA256 hash-chain, last-in-txn WORM), the wave-2 users/roles/invites schema (invites table already exists — reused, not re-created), the shipped M3 data_source_connections store + DataSourceAdapter registry (admin CRUD over an existing store, NOT a new store), and the shipped AppShell + role-nav. Advances M7 ## Success metric clauses "invite users and assign roles" (seed) and "workspace / firm profile / default compliance profile" (sibling 1). Additive-only schema across all four (rollback = drop the added column/table/index; no destructive ORM, no refactor of shipped surfaces).
- Bundle: seed "Build user-management admin vertical: invite, assign-role, deactivate (last-admin guard, SoD)" (82ec8724-3f9e-45bd-8e81-e4e3fab8872d — admin-users page + UserManagementService; SERVER-SIDE last-admin guard [race-safe locking count, refuses any transition dropping active-admin count to zero], SoD [no self-deactivate/self-demote below threshold, reusing wave-5 admin-cannot-self-approve precedent], every mutation audited last-in-txn; invite mints a hashed single-consume token but does NOT email — surfaces the not-yet-emailed state honestly). Siblings: "Build workspace + firm-profile settings with default compliance profile cascade" (648a86a6-024b-4fce-9212-1e637ee16765 — admin-workspace-settings page + firm-profile CRUD + default-compliance-profile selector cascading into the M2 disclaimer/suppression/jurisdiction tables mandates read; sending-domain verification shown as a labelled not-yet-available placeholder, no dead Verify/Send button per #331 (3)); "Add data-source connection admin management UI + encrypted-at-rest credential form" (41c017f7-0665-4fca-b95f-82fbf8962178 — admin-integrations page over the shipped M3 data_source_connections store; add/enable/disable + a paste-key credential form stored encrypted-at-rest [app-layer envelope encryption, Railway-env key, masked "set" state, never echoed, never in the audit row], NO live connection-TEST); and the RE-PARENTED carry-forward "AppShell polish: placeholder pages for role-nav items + TopBar per-route title" (d7f716b4-d451-4095-8b43-9fbe4e85fcf8 — moved from parent_task_id NULL to a sibling under the seed, because the admin-users / admin-workspace-settings pages this bundle ships replace exactly the "Team"/"Settings" role-nav placeholders it describes, and the seed's live role-mutation flow is the path d7f716b4 flagged for the RBAC re-verify black-box test).
- Re-parent note: d7f716b4 re-parented under the seed (rides with the admin pages that replace its "Team"/"Settings" nav 404s). bfadcec1 ("Tighten test-fixture typing in wave-1 health tests") LEFT UNTOUCHED (parent_task_id NULL) — its own note defers it to a testing-infra wave; NOT M7 feature scope. Seed-candidate guard: 2 non-feature carry-forward rows pre-existed (both low-priority debt, not a substantive M7 feature seed) — N-1 fired decomposition to author the first substantive M7 vertical; count now 2 unparented (bfadcec1 debt + the new feature seed 82ec8724).
- Buildability confirmation: buildable WITHOUT any founder credential — ZERO email-provider API key, ZERO EMAIL_WEBHOOK_SECRET, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO send path, ZERO webhook ingestion, ZERO live DKIM/SPF/DMARC record generation or domain-verify. The credential-management FORM (paste-key, encrypted-at-rest) is buildable; the ESP-minted DNS-record generation + live-verify is NOT and was correctly NOT seeded (credential-seamed with #141). Every write is additive schema + APPENDs via M2 AuditService (never edits/re-orders of the hash-chain); all new schema additive (rollback = drop added column/table/index). Builds only on shipped-and-live surfaces (M1 RBAC/SoD, M2 audit, wave-2 users/roles/invites, M3 connection store, AppShell).
- Deferral note: DEFERRED M7 ## Scope items were correctly NOT seeded — sending-domain DKIM/SPF/DMARC DNS-record GENERATION + live domain-verify polling (needs the ESP credential, deferred with product-decision #141, same stockout that blocks M6 send), and the live data-source connection-TEST against an external source (needs that source's own credential). The invite EMAIL delivery is likewise #141-gated (mint+persist+surface-in-app only this bundle). These remain M7's credential-seamed remainder for a later bundle once #141 lands.
- P-4 note (for wave-15 P-4, NOT run now): compliance-critical admin/RBAC/user-management/sending-identity surface → security-scope-tightened + SoD/RBAC gate REQUIRED (product-decision #331 (2)); audit/compliance tables append-only (WORM); last-admin guard + SoD must be black-box-proven live; M7 Class product-feature → wave-15 P-0 runs mvp-thinner; UI wave (admin-users + admin-workspace-settings + admin-integrations pages) → D-block runs.

[2026-07-06] M7 (Admin & settings): bundle expanded — +1 sibling (admin activity / audit-oversight view over existing immutable audit log), total 6 specs (1 seed + 5 siblings)
- caller: P-1-expand-current-bundle (RESCOPE-AUTO-MERGE; wave-16 bundle was 5 specs / ~1,200 est LOC, below the multi-spec floor of >2,500 LOC OR ≥6 specs — one coherent BUILDABLE sibling added to clear the floor by count)
- new sibling: "Admin activity view: read-only recent-admin-actions page + endpoint" (8bb0a22f-66a4-4b37-8cbb-dae7ad637767 — parent_task_id 904a3c25, milestone_id M7, wave_id NULL, status todo). Read-only admin-only GET /admin/activity endpoint + /admin/activity page. Filters the EXISTING immutable audit_log (M2) to the six wave-15 admin auditActionEnum actions (user-invite, role-change, deactivate, workspace-settings-update, data-source-conn-upsert, data-source-conn-toggle), firm-scoped, newest-first, paginated. Reuses apps/api/src/modules/audit/audit.repository.ts read path + packages/shared audit.ts schema pattern + existing admin route guard + AppShell admin nav + design-system table/badge. NO schema change, NO migration, NO new audit action, NO write path (log stays append-only/immutable). ~200–350 LOC.
- rationale: closes a real M7 admin-oversight gap — the four shipped admin write-surfaces (data-source connections, user invite/role/deactivate, workspace/firm-profile settings) had no in-app way for an admin to review WHO did WHAT; every one already writes an immutable audit entry, so a read-only oversight view is a same-surface, additive, fully-buildable companion to the wave-16 hardening bundle. Buildable NOW: no founder credential, no external SDK, no LLM, no email/DKIM path (correctly does NOT touch the #141-gated sending-domain verification / email-send remainder). Does not duplicate any existing open M7 task (verified against the 4 prior siblings + standalone bfadcec1).
- decomposed by: milestone-decomposer sub-agent

---

[2026-07-06] M8 (Pilot-partner workspace (data isolation)): bundle authored — 4 tasks (M8 FIRST vertical: workspaces anchor + workspace_id scoping column across tenant tables [SEED], deny-by-default Postgres row-level security, per-request workspace GUC propagation, and the cross-tenant negative-read isolation proof)
- caller: N-1-next-bundle (wave-16 close; automatic mode — M8 freshly promoted this wave)
- decomposed by: milestone-decomposer sub-agent
- Slice rationale: M8's FIRST vertical (promoted todo→in_progress at wave-16 close, 0 done + 0 open tasks). M8 scope = lightweight workspace_id scoping + DB row-level security over EXISTING tables for ONE external design-partner advisory firm — NOT full multi-tenant SaaS (that is H3/M11). The load-bearing seed is the workspaces anchor + additive workspace_id column backfilled across tenant-scoped tables; nothing else in M8 is meaningful until that column exists. The three siblings are the isolation mechanism + its proof: (1) deny-by-default Postgres RLS keyed to a per-session tenant GUC, (2) request-pipeline propagation that SET LOCALs the authenticated user's workspace onto the DB transaction (transaction-local so it never leaks across the pool), (3) an adversarial cross-tenant negative-read integration test against the real RLS-enforced path. All build ONLY on shipped-and-live surfaces (M1 Auth&RBAC user/session + server-side re-verify, M2 immutable audit log WORM guarantees untouched, M7 admin-workspace-settings, the shipped Drizzle/Postgres tenant tables). Additive-only schema across all four (rollback = drop the added column/workspaces table + DISABLE RLS/drop policies).
- Bundle: seed "Add workspaces anchor + workspace_id scoping column across tenant tables" (0db154ff-31f1-45c4-85cd-71d34d65c437 — additive workspaces table + one seed workspace for the internal firm + one for the incoming partner; NOT-NULL workspace_id FK backfilled to default across mandates/companies/contacts/matches/outreach/pipeline; every write stamps workspace_id server-side from the authed user, never client input). Siblings: "Enforce deny-by-default Postgres row-level security scoped by workspace_id" (e45ba68c-80f3-475e-a240-54c23ea9ccb2 — ENABLE RLS + deny-by-default USING/WITH CHECK policy keyed to current_setting('app.current_workspace_id'); no-workspace session sees ZERO rows; M2 audit_log WORM untouched); "Propagate authenticated user workspace into every request-scoped DB session" (96026365-77b2-4763-bf57-705fbf340ba8 — NestJS request lifecycle resolves the user's workspace [re-verified server-side like the role claim] + SET LOCAL app.current_workspace_id transaction-local so it never leaks across pooled connections; unauth = no workspace = reads nothing); and "Prove cross-tenant isolation with a negative-read integration test" (df2f3b2f-6e7d-4f39-a6ab-7ca49020e967 — real-DB test seeds two workspaces, asserts workspace-A user reads ONLY workspace-A rows + ZERO workspace-B rows via the RLS-enforced path, asserts deny-by-default empty result, asserts a forged client-supplied workspace_id cannot override the session-derived one; reuses the wave-12 race-safe migrate helper; gates the M8 isolation claim backing partner go-live).
- Validation note (Step 1 check 3): M8 ## Success metric header carries the `_TBD by founder_` token BUT is followed by a concrete qualitative target ("the design-partner firm operates in an isolated workspace with no cross-firm data visibility"), and ## Scope is richly enumerated (workspace_id scoping + RLS over existing tables, named modules) with usable ## References (architecture/security.md exists + detailed, product-decisions.md). The scope-too-vague guard (TBD metric AND one-line scope AND no references) did NOT fire — only the quantitative metric target is founder-deferred, which is a metric-refinement concern, not a decomposition blocker; the qualitative target is present + testable and maps directly to sibling 3's negative-read proof. Bundle authored.
- BOARD build-time guardrail carried forward (for whoever builds this — NOT a decomposition blocker): RLS must be DB-row-level (not app-layer only), deny-by-default, with a cross-tenant negative read test. Sibling 1 (deny-by-default RLS) + sibling 3 (negative-read proof) encode exactly this.
- Buildability confirmation: buildable WITHOUT any founder credential — ZERO email-provider API key, ZERO EMAIL_WEBHOOK_SECRET, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO send path, ZERO webhook ingestion. All schema additive (rollback = drop added column/workspaces table + DISABLE RLS/drop policies). Builds only on shipped-and-live surfaces (M1 Auth&RBAC, M2 audit log, M7 admin-workspace-settings, shipped Drizzle/Postgres tenant tables). Does not duplicate any existing open task (M8 had 0 open tasks).
- P-block note (for the next wave's P-4, NOT run now): tenant-isolation / RLS / session-scoped data-access surface → security-scope-tightened gate REQUIRED; the cross-tenant negative-read test (sibling 3) is a hard acceptance criterion for the isolation claim; M8 Class product-feature → next P-0 runs mvp-thinner; backend/DB-isolation wave (no new UI beyond admin-workspace-settings already shipped) → D-block likely skipped, confirm at P-block.

---

[2026-07-06] M7 (Admin & settings, 08d3053a): in_progress → blocked; M8 (Pilot-partner workspace / data isolation, 9ed98c3c): todo → in_progress — roadmap disposition at wave-16 close (N-1)
- caller: N-1 (head-next, N-block gate) · mode: automatic · decision-slug: N-1-milestone-disposition-M7-wave-16 · routed to BOARD (Tier-3: milestone disposition + next-milestone-theme advance is a product/taste call per always-on rule 17)
- BOARD outcome: **7/7 endorse the forward motion (park M7, advance to M8), 0 HARD-STOP** (clears the Tier-3 strict 6+/7 bar). Two seats voted "REJECT as framed" — both explicitly endorsed the M8 advance and objected only to framing details (counter-thinker+founder-proxy: M7 must be `blocked` not `done`; realist: M8 metric `_TBD`). Full record: process/waves/wave-16/escalations/board-N-1-milestone-disposition-M7-wave-16.md
- M7 → BLOCKED (external hold, NOT done): M7's 2 substantive ## Success-metric legs are SHIPPED & LIVE — connect a data source, and invite users + assign roles (10 child tasks done across waves 14/15/16, V-blocks APPROVED, LIVE @d72d7cb + a18fc82). The 3rd leg — verify a sending domain (DKIM/SPF/DMARC) for compliant outreach — is UNSHIPPED and 100% founder-gated on the email-provider/DKIM credential (product-decision #141, the SAME gate that put M6 into `blocked` at wave-14). The brain CANNOT author it (account-issued credential per always-on rule 6). Per the M5→blocked (wave-10) and M6→blocked (wave-14) precedent, a milestone whose ## Success metric is unmet ONLY because of a #141 founder-gated credential goes to `blocked` (honest external-hold), NEVER `done` — Hallucinated-Milestone-Completion explicitly AVOIDED (dispositive: founder-proxy + counter-thinker). M7 + its remainder stay ON the roadmap, tracked and reversible (flip blocked→in_progress when #141 lands, then decompose the sending-domain verify bundle). The 4 open M7 tasks stay parented to M7: 3 V-2 wave-16 Low hardening gaps (widen invite advisory-lock key space / opaque admin-activity cursor / constrain data-source config fieldMapping — all Low severity, all on already-in-prod surfaces, surfaced at every future P-0 walk) + the wave-1 test-fixture-typing carryover (bfadcec1, testing-infra debt). risk-officer note: prioritize the fieldMapping free-value slot early when M7 reopens.
- M8 → IN_PROGRESS (promotion): M8 "Pilot-partner workspace (data isolation)" is the oldest todo (H2/T4) and the roadmap's designated pilot-partner successor — workspace_id scoping + Postgres row-level security over EXISTING tables for ONE external design-partner advisory firm (NOT full multi-tenant SaaS; that is H3/M11). ## Why now: the external partner arrives shortly after MVP. In M&A, client-confidentiality / information-walls are table-stakes and a procurement gate (industry-expert: Mizuho/Virtu fines for failing to TECHNICALLY enforce barriers; user-advocate: confidentiality dominates felt value for the arriving firm). Serves both live founder bets (integrated platform; the imminent pilot partner). Front-loading RLS isolation BEFORE the second firm's data enters is far safer than retrofitting (risk-officer).
- M8 first bundle authored (decomposition-complete; milestone-decomposer, commit 1234f41): seed "Add workspaces anchor + workspace_id scoping column across tenant tables" (0db154ff) + 3 siblings — deny-by-default Postgres RLS scoped by workspace_id (e45ba68c), propagate authenticated user workspace into every request-scoped DB session (96026365), prove cross-tenant isolation with a negative-read integration test (df2f3b2f). Clean vertical slice (DB spine → RLS policy → request-scope middleware → adversarial cross-tenant test), ~2,000–3,200 LOC, ≤~25 files, additive-only (rollback = drop column/table + DISABLE RLS), builds only on shipped M1/M2/M7 surfaces. BOARD build-time guardrails ENCODED in the bundle: DB-row-level RLS (not app-layer), deny-by-default, cross-tenant negative-read test (counter-thinker + industry-expert + risk-officer). realist's `_TBD`-metric blocker resolved at the ritual level: the scope-too-vague guard is a CLUSTER (TBD metric AND one-line scope AND no references) that did not fire — M8's ## Scope is richly enumerated with live ## References (architecture/security.md), and the qualitative target ("no cross-firm data visibility") anchors the negative-read proof; only the QUANTITATIVE metric is founder-deferred.
- Founder deferrals re-surfaced (non-blocking, loudly framed): (1) M8 ## Success metric quantitative target is `_TBD by founder_` — carried to the founder digest for tightening (roadmap-planning can revisit); build proceeds against the qualitative "no cross-firm data visibility" target + negative-read test. (2) email-provider/DKIM credential #141 is now the TRUE blocker for BOTH M6 (compliant send) AND M7's sending-domain leg — its wave-count watchdog fires again this wave. (3) LLM-spend still pending (M5). All carried to process/session/updates/ + .last-wave-completed pending_founder_decisions.


---

[2026-07-06] M8 (Pilot-partner workspace / data isolation, 9ed98c3c): in_progress → done; M9 (Integrations & insight, 099cee10): todo → in_progress — roadmap disposition at wave-17 close (N-1)
- caller: N-1 (head-next, N-block gate) · mode: automatic · decision-slug: N-1-milestone-disposition-M8-wave-17 · routed to BOARD (Tier-3: milestone closure disposition + next-milestone-theme advance is a product/taste call per always-on rule 17)
- BOARD outcome: **7/7 APPROVE-A, 0 HARD-STOP** (clears the Tier-3 strict 6+/7 bar decisively). Option A = call M8 done on its shipped pilot scope + advance to M9; Option B = keep M8 open for the 3 H3-forward/process gaps. Seats: ceo-reviewer, architect-reviewer, ux-researcher, risk-manager, founder-proxy, competitive-analyst, product-manager — all APPROVE-A, independent fresh-context votes. Full record: process/waves/wave-17/escalations/board-N-1-milestone-disposition-M8-wave-17.md
- M8 → DONE (honest scope-complete, NOT Hallucinated-Milestone-Completion): M8's written scope is explicitly pilot-scoped to ONE external design-partner firm ("workspace_id scoping + RLS over EXISTING tables — NOT full multi-tenant SaaS; that is H3/M11") and its qualitative success metric ("the design-partner firm operates in an isolated workspace with no cross-firm data visibility") is SHIPPED & LIVE-VERIFIED end-to-end: deny-by-default FORCE Postgres row-level security scoped by workspace_id, proven via a [RLS-GUARD] boot-assertion + a CI negative-read integration suite executed AS the non-superuser dealflow_app runtime role (not superuser). Existing firm data backfilled into the default workspace; reads not over-blocked. 5 tasks done (4 core + 1 reconciled triage). Reads (the cross-firm-visibility vector) are RLS-protected & live — that satisfies the metric on its own. Live @591b3f8, V-block APPROVED.
- The 3 open M8 tasks (all wave-17 V-2/jenny triage follow-ups) were RE-HOMED before close (0-open-children closure invariant): 
  (1) GAP-2 write-path fail-closed (2867d087) → **M11 Multi-tenant SaaS + billing**, wave_id cleared → clean future seed. Flagged in-task as a BLOCKING M11 pre-requisite: M11 MUST NOT introduce any 2nd-workspace provisioning path until this repo-wide (14+ files) `workspaceId ?? DEFAULT_WORKSPACE_ID` INSERT-fallback sweep is replaced with explicit null-GUC deny + a no-GUC-write-denied test; M11 cannot exit until closed. This honors the mandatory condition risk-manager + architect-reviewer both attached to their APPROVE-A. Topologically INERT for the pilot (one workspace only; reads RLS-protected regardless); load-bearing only multi-firm.
  (2) GAP-4 standing populated-DB migration-proof AC (fd8f2860) → **M10 Advanced compliance & recordkeeping**, wave_id cleared. Testing-process debt born from migration 0014 colliding with the WORM trigger on 328 populated prod rows the empty CI never exercised.
  (3) GAP-5 RLS connection-split doc + coupled rollback (1a1c5855) → **M10**, wave_id cleared. Ops/devops documentation debt.
- Architect-reviewer verification (recorded, load-bearing for the containment argument): there is exactly ONE `INSERT INTO workspaces` in the entire repo — the migration seed — and no application/API/admin/script path creates a 2nd workspace. A 2nd workspace requires a deliberate human-authored migration, so the single-firm containment ("only the default workspace exists") CANNOT be silently violated by normal pilot operation. GAP-2 is therefore genuine defense-in-depth deferrable to M11, not a live pilot exposure.
- BOARD-recommended non-blocking control (planned into M11, noted on GAP-2): a hard runtime/boot assertion that refuses workspace-count > 1 until the fail-closed write-path sweep lands — converts early-2nd-firm onboarding from a silent cross-firm write mis-placement into a loud blocking failure.
- M9 → IN_PROGRESS (promotion): M9 "Integrations & insight" is the oldest todo (H2/T4) and the roadmap's designated successor — CRM integration adapters (Salesforce/DealCloud/Affinity) via the data-source adapter interface + advanced analytics/reporting (mandate throughput, outreach response rates, advisor productivity) + multi-channel outreach + seller intent signals + matching feedback loop. Directly advances the top founder bet (integrated sourcing→matching→compliant-outreach beats stitched tools) and serves the pilot advisor with felt value (CRM interoperability + productivity analytics are table-stakes in M&A deal-tech per competitive-analyst: DealCloud/Affinity/Datasite/Intapp). Single-active invariant held (0 in_progress between the M8→done and M9→in_progress flips).
- Founder deferrals re-surfaced (non-blocking, loudly framed, carried to digest + .last-wave-completed): (a) the M8-disposition + the GAP-2 H3-hardening carry (a REAL write-path fail-closed gap, inert for the pilot but that MUST land before any 2nd firm); (b) M8 ## Success metric quantitative target was `_TBD by founder_` — pilot met on the qualitative target + negative-read proof; quantitative tightening still open for roadmap-planning; (c) LLM-spend still pending (M5 blocked); (d) email-provider/DKIM credential #141 still the true blocker for M6 (compliant send) + M7 (sending-domain leg).

---

[2026-07-06] M9 (Integrations & insight): bundle authored — 3 tasks (M9 FIRST buildable vertical: advisor-insights analytics aggregation service [workspace-scoped, RLS-honoring, SEED] → shared-Zod-typed RBAC-scoped analytics API endpoint(s) → /insights Next.js 15 advisor analytics dashboard)
- caller: N-1-next-bundle (wave-17 close; automatic mode — M9 freshly promoted this wave, 0 done + 1 pre-existing DEFERRED task under M9)
- decomposed by: milestone-decomposer sub-agent
- Slice rationale: M9's FIRST vertical (promoted todo→in_progress at wave-17 close). M9 ## Scope contains two separable halves — (a) a founder-gated integrations half (CRM adapters Salesforce/DealCloud/Affinity, multi-channel outreach LinkedIn/phone, seller-intent signals, matching-feedback model retraining) that each require a VENDOR-SELECTION SPEND decision + account-issued credential + external SDK/LLM spend (barred by always-on rule 6); and (b) a fully BUILDABLE, credential-free, spend-free analytics/insight half ("advanced analytics & reporting — mandate throughput, outreach response rates, advisor productivity" + the READ/REPORT facet of the matching feedback loop over existing match_candidates.disposition). This bundle authors the buildable half as one clean VERTICAL SLICE: aggregation service (seed) → shared-Zod API endpoint → /insights dashboard page. It is the reporting half of the M9 ## Success metric ("advisors ... see response/throughput analytics"); the CRM-sync half of that metric is the founder-gated remainder.
- Bundle: seed "Build advisor-insights analytics aggregation service (workspace-scoped, RLS-honoring)" (a5ba8068-2e1b-48ea-83d9-6da739a41e2b — new NestJS AnalyticsService aggregating SHIPPED-AND-LIVE tables only [M4 mandates+buyer_universe, M5 match_run/match_candidates incl. disposition, wave-11 outreach, wave-12 pipeline/pipeline_events, M3 companies/contacts, M2 audit_log READ-ONLY] into the four metric families; every read runs through the M8 request-scoped GUC so deny-by-default RLS confines it to the caller's workspace — no superuser/GUC-bypass path; match-disposition is the READ facet only, NO model retraining [that is LLM-spend, deferred]). Siblings: "Expose shared-Zod-typed analytics API endpoint(s) with RBAC-scoped read" (9e05828b-38dd-475c-9f82-cd5ac4565fff — GET-only analytics routes, NEW packages/shared analytics.ts Zod contract, M1 RolesGuard/@Roles + server-side role re-verify, workspace-scoped by inherited M8 GUC); "Build the /insights advisor analytics dashboard page (Next.js 15)" (4b014689-8e12-4560-95c9-5b0ae4d2f4fc — /insights route on shipped AppShell + design-system [zinc/emerald, lucide-react, 4px grid], renders the four metric families via the shared-Zod schema, HONEST not-yet-tracked states for any founder-gated signal, role-gated + workspace-scoped, WCAG 2.2).
- Seed-candidate guard reasoning (Step 1.4): the literal seed-candidate count was 1, NOT 0 — one pre-existing row (345dfbc6, "Implement first real DataSourceAdapter") matches the seed-candidate predicate (todo / wave_id NULL / parent_task_id NULL). BUT that row is explicitly DEFERRED [wave-7 P-0 RESCOPE-AUTO-SPLIT]: externally blocked on a founder VENDOR-selection spend decision + an account-issued API key (always-on rule-6 exception), "Not autonomously completable." N-1 therefore correctly judged the M9 queue to have no CLAIMABLE/usable seed and fired decomposition (identical precedent: the M7 wave-14 authoring, where 2 non-feature carry-forward rows [bfadcec1 test-fixture debt] pre-existed and were not treated as a substantive feature seed). Treating 345dfbc6 as a satisfied seed candidate would deadlock M9 indefinitely, since it can never clear without a founder decision out of scope for autonomous flow. Bundle authored on the buildable analytics vertical; 345dfbc6 LEFT UNTOUCHED (parent_task_id NULL, unclaimable, awaiting founder vendor decision) — NOT duplicated, NOT seeded, NOT depended on.
- Buildability confirmation: buildable WITHOUT any founder credential — ZERO CRM/vendor selection, ZERO account-issued API key, ZERO email-provider key / EMAIL_WEBHOOK_SECRET, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO send path, ZERO webhook ingestion, ZERO multi-channel-outreach provider, ZERO model retraining. Pure read/report over existing live tables. All schema additive (default = on-the-fly aggregation; any performance cache = additive summary table, rollback = drop the summary table + refresh path; new API route + new shared Zod schema, rollback = remove them; new /insights page + nav entry, rollback = remove them). NO mutation to any existing table; M2 HMAC-SHA256 audit hash-chain is READ-ONLY. Builds ONLY on shipped-and-live surfaces (M2 audit, M3 sourcing, M4 mandates+buyer_universe, M5 match_candidates.disposition, wave-11 outreach, wave-12 pipeline/pipeline_events, M8 workspace_id+RLS+GUC, shipped AppShell + design system + M1 RBAC + packages/shared Zod). Does NOT duplicate any existing open task (M9 had 1 open task, the DEFERRED 345dfbc6, which this bundle explicitly avoids).
- Deferred-scope note: the founder-gated M9 ## Scope items were correctly NOT seeded — CRM integration adapters (vendor spend + account key; already tracked by DEFERRED 345dfbc6's sibling class), multi-channel outreach LinkedIn/phone (external provider creds + #141 ESP class), seller-intent signals + matching-feedback MODEL RETRAINING (LLM-spend, same gate as M5-blocked). These remain M9's credential/spend-seamed remainder for later bundles once the founder resolves the vendor decision / #141 / LLM-spend.
- P-block note (for the next wave's P-4, NOT run now): UI wave (/insights dashboard) → D-block + head-designer gate run; M9 Class product-feature → next P-0 runs mvp-thinner; analytics reads touch the M2 audit log + M8 RLS-scoped tenant data → confirm read-only/no-GUC-bypass at P-block; honest not-yet-tracked states are a hard acceptance criterion (no fabricated metrics).

## [2026-07-07] M9 (Integrations & insight): bundle authored — 4 tasks (matching feedback-loop / score-calibration insight vertical)
- caller: N-1-next-bundle (automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Scope thread chosen: **matching feedback loop (learn from accept/reject)** — the single best BUILDABLE, credential-free slice of M9 ## Scope. Chosen over multi-channel outreach (needs LinkedIn/phone provider creds + ESP class #141 — founder-credential/spend gated), seller-intent signals (needs external signal provider or LLM-spend), and CRM adapters (DEFERRED 345dfbc6 — vendor spend + account key). Mirrors the just-shipped analytics vertical (a5ba8068 → 9e05828b → 4b014689): shared-Zod contract → RLS-scoped aggregation service → RBAC-gated API → /insights dashboard section.
- Bundle: seed "Build match-calibration feedback aggregation service (workspace-scoped, RLS)" (5568ad44-3702-46d5-809a-40c1de0a2035 — new read-only aggregation over SHIPPED-AND-LIVE match_candidates.disposition + fit_score + score_breakdown joined to match_run; computes acceptance-rate by fit-score band, per-dimension accepted-vs-rejected lift, reviewed-vs-pending coverage; every read via getDb→FORCE RLS + set_config('app.workspace_id') exactly like AnalyticsRepository; NO writes to match_candidates, NO LLM/Anthropic import [matching hard boundary], NO model retraining [that is LLM-spend, deferred]). Siblings (parent 5568ad44): "Add shared-Zod match-feedback contracts to packages/shared" (69387b56 — new match-feedback.ts Zod schemas, single source for API DTO + page props); "Expose RBAC-scoped match-feedback API endpoint (advisor+admin read)" (e206a56a — SessionGuard+RolesGuard @Roles(advisor,admin) fail-closed, analyst/compliance 403, workspace-scoped RLS end-to-end); "Add score-calibration feedback section to the /insights dashboard" (077974a2 — extends apps/web/app/(app)/insights/page.tsx with acceptance-by-band + dimension-lift + coverage using zinc/emerald + lucide-react + 4px grid, empty-state safe, WCAG 2.2).
- Dependency verification: reads ONLY data already built + live. match_candidates.disposition (pending|accepted|rejected|flagged), fit_score (INTEGER CHECK 0-100), score_breakdown (ScoreBreakdown JSONB: sectorMatch/contactCompleteness/tieBreak), match_run, mandates, buyer_universe — all present in apps/api/src/db/schema/matching.ts, all workspace_id-scoped + RLS-enforced (wave-17). NO ghost dependency on unbuilt schema, NO dependency on DEFERRED CRM seed 345dfbc6, NO unmerged PR. Distinct from shipped analytics getMatchDisposition (raw counts only) — this correlates disposition×score for calibration insight, a genuinely new vertical, not a duplicate.
- Seed-candidate guard (Step 1.4): literal seed-candidate count was 1 (345dfbc6 matches todo/wave_id-NULL/parent_task_id-NULL) BUT that row is explicitly DEFERRED [wave-7 P-0 RESCOPE-AUTO-SPLIT] — externally blocked on a founder VENDOR-selection spend decision + account-issued API key (rule-6 exception), "Not autonomously completable." N-1 correctly judged M9 to have no CLAIMABLE seed and fired decomposition; treating 345dfbc6 as satisfied would deadlock M9 indefinitely. 345dfbc6 LEFT UNTOUCHED (not seeded, not duplicated, not depended on). 1d95cac0 (V-2 process follow-up, wave_id set) is a current-wave artifact per bundle anti-pattern #6, not a milestone seed — also not duplicated.
- Founder-credential/spend guard HELD: buildable WITHOUT any founder credential — ZERO CRM/vendor selection, ZERO account-issued API key, ZERO email/ESP key, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO model retraining. Pure read-only aggregation + report over existing live tables. New shared Zod schema + new API route + new /insights section, all additive; rollback = remove them. NO mutation to any existing table.

## [2026-07-07] M9 (Integrations & insight): bundle authored — 4 tasks (internal multi-channel outreach-activity tracker vertical)
- caller: N-1-next-bundle (automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Scope thread chosen: **multi-channel outreach (LinkedIn/phone tasks)** — the INTERNAL manual outreach-activity/task-tracking carve-out (create/track call/email/LinkedIn touches as internal records with channel + status + due date + optional deal-target link; NO actual external send). Chosen over seller-intent-signals (thread 5, also credential-free but a pure read-derivation like the two already-shipped read-only verticals) because thread 4's internal-tracker slice needs a NEW MUTABLE TABLE — giving a genuine full-stack DB→service→contract→API→UI vertical with a real write surface that engages the M2 HMAC audit chain (higher coverage than another read-only report). Actual LinkedIn/phone/ESP API sending stays founder-credential-gated and is explicitly OUT of scope. CRM adapters remain the DEFERRED 345dfbc6 (vendor spend + account key).
- Bundle: seed "Add outreach_activity table + additive migration (workspace-scoped, RLS, WORM-free)" (d45c73b5-39bc-4a8a-a5c3-65d12b0cb5eb — new MUTABLE outreach_activity table via ONE additive Drizzle migration: workspaceId RLS FK, new outreach_activity_channel [call|email|linkedin|other] + outreach_activity_status [planned|completed|cancelled] pgEnums, subject/body/dueAt/completedAt, 0-or-1 deal-target link [outreachId|matchCandidateId|pipelineId] + nullable mandate scope, createdBy FK, workspace + (workspace,status,due_at) indexes; table is NOT under the M2 WORM regime but every mutation APPENDS to the M2 HMAC-SHA256 audit chain; deterministic-test-spec-first [empty+populated migration proof vs the GAP-4 WORM-collision lesson + RLS negative-read] required; rollback = DROP TABLE + DROP the two TYPEs, additive-only, no existing table altered). Siblings (parent d45c73b5): "Build outreach-activity service with RLS + audit-logged mutations (workspace-scoped)" (5c12ac3a — NestJS OutreachActivityService, getDb→FORCE RLS→set_config app.workspace_id like AnalyticsRepository, create/update/complete/cancel each in a txn that appends to the M2 audit chain, 0-or-1-link + status-transition validation, RLS negative-read + audit-append + invalid-transition tests first); "Add shared-Zod outreach-activity contracts to packages/shared" (c3776cac — new outreach-activity.ts Zod schemas, single source for API DTO + page props, enums mirror the DB pgEnums, 0-or-1-link + non-empty-subject validation); "Expose RBAC-scoped outreach-activity API + build the outreach-touches UI panel" (b2acf4ce — GET/POST/PATCH routes SessionGuard+RolesGuard @Roles(advisor,admin) fail-closed [analyst/compliance 403] workspace-scoped RLS end-to-end, + an outreach-touches panel on the shipped AppShell + design system [zinc/emerald, lucide-react, 4px grid] with create form + complete/cancel actions + HONEST empty state + WCAG 2.2).
- Dependency verification: reads/writes ONLY surfaces already built + live. FK targets outreach / match_candidates / pipeline / mandates / users / workspaces all present + workspace_id-scoped + RLS-enforced (wave-17); enum-naming + workspaceId + index conventions mirrored from apps/api/src/db/schema/outreach.ts. NO ghost dependency on unbuilt schema, NO dependency on DEFERRED CRM seed 345dfbc6, NO unmerged PR. Genuinely new surface (a mutable internal activity ledger — distinct from the outreach compose/gate table and from pipeline stage events).
- Seed-candidate guard (Step 1.4): literal seed-candidate count was 1 (345dfbc6 matches todo/wave_id-NULL/parent_task_id-NULL) BUT that row is explicitly DEFERRED [wave-7 P-0 RESCOPE-AUTO-SPLIT] — externally blocked on a founder VENDOR-selection spend decision + account-issued API key (rule-6 exception), "Not autonomously completable." N-1 correctly judged M9 to have no CLAIMABLE seed and fired decomposition; treating 345dfbc6 as satisfied would deadlock M9 indefinitely. 345dfbc6 LEFT UNTOUCHED (not seeded, not duplicated, not depended on). 1d95cac0 (V-2 process follow-up, wave_id set) is a current-wave artifact, not a milestone seed — also not duplicated.
- Founder-credential/spend guard HELD: buildable WITHOUT any founder input — ZERO CRM/vendor selection, ZERO account-issued API key, ZERO email/ESP key (#141 untouched), ZERO LinkedIn/phone/external-send integration, ZERO external intent-data provider, ZERO LLM/Anthropic spend, ZERO new external SDK, ZERO webhook ingestion. Internal manual-touch records only. All schema additive (rollback = drop the new table + two enum types); new shared Zod schema + new API routes + new UI panel, all additive (rollback = remove them). NO mutation to any EXISTING table; the M2 audit chain is APPENDED to (its correct use), not altered. Sizing ~2,500–4,000 net LOC, ≤~30 files, 1 seed + 3 siblings.

## [2026-07-07] M9 → blocked; M10 → in_progress — milestone disposition at wave-23 close (N-1, BOARD 7/7)
- M9 (Integrations & insight, 099cee10): in_progress → **blocked**. Buildable scope EXHAUSTED at wave-23 — 4 credential-free insight verticals shipped (analytics w18 / calibration w19 / outreach-log w20 / seller-intent w23; 17 done tasks, CI-verified LIVE @6c22919). The only remaining child 345dfbc6 (blocked) is a real CRM/DataSourceAdapter gated on a founder data-vendor selection + account-issued API key; rest of ## Scope (external send/ESP #141, matching-model retraining/LLM-spend) likewise founder-credential/spend-gated. Parked as HONEST external-hold — NOT done (## Success metric still _TBD by founder), NOT cancelled (17 verticals shipped; resume when founder clears the gate). Matches the M5/M6/M7 founder-gated-remainder → blocked precedent.
- M10 (Advanced compliance & recordkeeping SOX/FINRA, 033f97e0): todo → **in_progress** (N-1 Action 8a promotion; slot vacated by M9). Grounded in LIVE founder bet c541045c "Compliance-first outreach is a durable wedge". Depends on M2 (done — the tamper-evident HMAC audit backbone).
- Decision: **BOARD 7/7 UNANIMOUS APPROVE Option (a)** — DEFER/park M9, promote M10, keep the loop producing on credential-free work. Slug N-1-roadmap-planning-wave-23. No hard-stop veto. Threshold (4+/7) exceeded; founder-proxy grounded the call in 4x direct precedent (M5/M6/M7/M8 dispositions) + the compliance-first bet. Full votes: process/waves/wave-23/escalations/board-N-1-roadmap-planning-wave-23.md.
- BOARD caveats carried (non-blocking): (1) M10's ## Success metric is ALSO _TBD by founder — MUST be surfaced to the founder digest concurrently or the loop recreates M9's close-blocker at M10; (2) any future M10 recordkeeping decomposition MUST be fenced ADDITIVE-READ-ONLY over the M2 audit chain (no HMAC-preimage change, no audit_log_entries schema mutation; regime-specific retention params = founder-set config, not hardcoded).
- MILESTONE-INTEGRITY flag (non-blocking, for next roadmap-planning ritual): M10's queue currently holds ONLY 3 unparented V-2/D-3 process follow-ups (auth-hardening 6fe232e3, standing WORM-migration-proof AC fd8f2860, RLS connection-split doc 1a1c5855) — NOT a purpose-authored recordkeeping VERTICAL. Because seed_candidates=3≠0, milestone-decomposer CANNOT legally fire (ritual Step 1.4). wave-24 is therefore a compliance-HARDENING wave (NOT recordkeeping-artifact progress). Next roadmap-planning should author M10's first recordkeeping vertical + re-home/reprioritize the process tasks so a purpose-built bundle can be decomposed.
- N-2 SEED (wave-24): fd8f2860 "Standing AC: populated-DB migration proof for any WORM/audit-table migration" — chosen over oldest-created_at 6fe232e3 under N-2 Action-1 re-order license (Customer-Problem-Stack-Rank scored against M10 compliance scope: fd8f2860 hardens the audit backbone M10's artifacts depend on = most scope-aligned genesis). Single-task bundle (bundled_siblings []). head-next N-2 signoff: APPROVED.
- FOUNDER-GATED PILE-UP (non-blocking, digest): M5 LLM-spend (blocked), M6/M7 #141 email/DKIM (blocked), M9 CRM 345dfbc6 (blocked), M9 _TBD success-metric (product poll, DUE — carried since wave-18), M10 _TBD success-metric (product poll). Unassigned queue: 1 low-value task (b1a0b2ac /health-spec wording) left unassigned.

## [2026-07-07] M10 recordkeeping-integrity TRIPWIRE fired at wave-25 close — BOARD 7/7 disposition (c)
- **Slug:** N-1-M10-recordkeeping-integrity-wave-25 (automatic mode; strategic milestone disposition → BOARD; Tier-3-strict 6+/7 bar applied). Full votes: process/waves/_archive/wave-25/escalations/board-N-1-M10-recordkeeping-integrity-wave-25.md.
- **Tripwire condition (met):** M10 (Advanced compliance & recordkeeping SOX/FINRA, in_progress) has shipped 2 hardening/debt waves (auth-hardening 6fe232e3 w25; WORM-migration-proof standing AC fd8f2860 w24) and its ONLY remaining seed candidate (1a1c5855, RLS connection-split doc + coupled rollback) is ALSO hardening/debt. ZERO purpose-authored SOX/FINRA recordkeeping VERTICAL (retention-policy locks, attestation/certification report generation, recordkeeping exports, regime-review posture) has ever been decomposed. ## Success metric = _TBD by founder_. Seeding 1a1c5855 = a 3rd consecutive hardening wave = milestone drift → REFUSED as "milestone progress" and escalated to BOARD (not mechanically seeded).
- **Decision: BOARD 7/7 UNANIMOUS APPROVE disposition (c)** — surface + bounded-final-seed. No hard-stop veto. Exceeds 4+/7 default and 6+/7 Tier-3-strict.
  1. **Surface to founder (non-blocking):** the recordkeeping SCOPE + _TBD success metric + whether to raise the compliance classification. Rule-17 founder-reserved AND cannot be autonomously decomposed (decomposition ritual Step 1 refuses _TBD). roadmap-planning NOT run autonomously (would stall at its own founder checkpoint).
  2. **Seed wave-26 = 1a1c5855** as the EXPLICITLY-FINAL bounded hardening/debt-closure wave (do not idle). Single-task bundle. Not vanity: RLS runtime(dealflow_app)/owner(migrate) role separation + coupled rollback is the load-bearing substrate under the WORM/audit recordkeeping backbone (architect-reviewer + risk-manager).
  3. **Enforced wave-27 circuit-breaker (binding condition of approval):** after 1a1c5855 ships, M10 = 0 seed candidates + _TBD metric + scope-not-shipped → wave-27 N-1 Action 7 fires decomposition → ritual Step 1 REFUSES on _TBD → incomplete-scope → loop PAUSES (.loop-paused.yaml, decomposition-pending-founder) UNLESS the founder has scoped recordkeeping. Framework-structural, not soft intent. No 4th "one more debt item" may be smuggled in under the hardening rationale.
- **Grounding (founder-proxy):** product-decisions "keep the formal regulatory classification light / the founder can raise the classification later"; M10 ## Success metric _TBD by founder_; live bet "Compliance-first outreach wedge" (Medium confidence; falsifier = whether the pilot design-partner requires this depth). Disposition (a) [autonomously author verticals] = overreach on a founder-reserved call.
- **N-2 seed (wave-26):** 1a1c5855 "Document + AC the RLS connection-split (runtime dealflow_app / migrate owner) + coupled rollback". Single-task bundle (bundled_siblings []). Validated clean (todo/wave_id-NULL/M10/top-level). head-next N-1+N-2 signoff: APPROVED.
- **FOUNDER-GATED PILE-UP (non-blocking, digest — 3rd-recurrence Actions billing added):** M5 LLM-spend (blocked), M6/M7 #141 email/DKIM (blocked), M9 CRM 345dfbc6 (blocked), M9 _TBD success-metric (product poll, carried since wave-18), M10 _TBD success-metric + recordkeeping-scope + compliance-classification-raise (product poll, DUE — now blocking wave-27), GitHub Actions billing (3rd same-day minutes exhaustion in wave-25 C-1 — recommend a PERMANENT CI spending-limit raise). Unassigned queue: 1 low-value (b1a0b2ac /health-spec wording).

---
## 2026-07-07 — M10 compliance posture: KEEP IT LIGHT (founder decision)
**Decision (founder, direct):** "Compliance posture — keep it light for now." M10 (Advanced compliance & recordkeeping) does NOT target a formal named regime (SOX / FINRA / SOC 2) at this stage.
**What it pins:** M10's recordkeeping verticals are built at a pragmatic, light posture on the existing H1 backbone (M2 HMAC audit-chain + M8 workspace RLS). NO formal attestation/certification-report generation and NO named-regime review posture until the founder later raises the compliance classification. Retention windows / export formats / records views are built to sensible light defaults, raiseable later.
**Consequence:** M10 ## Scope tightened + ## Success metric set (below) → the milestone-decomposition ritual can now author a purpose-built recordkeeping vertical → the enforced founder-pause (decomposition-pending-founder) is RESOLVED; the loop resumes at wave-27.
**Deferred to founder (non-blocking):** which vertical FIRST + the exact success bar were left to product-default under the light steer — proceeding with **recordkeeping exports first** (most foundational + immediately valuable + leverages the live audit-chain/RLS). Founder can redirect.

[2026-07-07] M10 (Advanced compliance & recordkeeping): bundle authored — 2 tasks (recordkeeping EXPORTS vertical — firm-admin CSV/JSON export of workspace-scoped audit-log + deal/pipeline activity, HMAC-chain integrity-verifiable, export-action self-audit-logged, RBAC + M8-RLS scoped)
- vertical-slice: seed = firm-admin export endpoint (0d2c5f08-81e7-421c-ac69-cfedea2cc8a8), sibling = firm-admin export page (f331a51c-aa4f-464a-a0a6-c470f4f297a6). Built on M2 (HMAC audit-chain: apps/api/src/modules/audit/*, verifyChain, audit_log_entries WORM) + M8 (workspace RLS: dealflow_app runtime / getDb / workspace_id scoping).
- LIGHT posture (founder 2026-07-07): standard-format, integrity-verifiable, audit-logged, RBAC+RLS-scoped export — NOT a named-regime certification/attestation package. RETENTION policy + records-VIEW correctly NOT bundled (later verticals, per M10 ## Scope build order).
- caller: N-1-next-bundle (resolves the wave-26 decomposition-pending-founder pause now that M10 ## Success metric is SET)
- decomposed by: milestone-decomposer sub-agent

[2026-07-07] M10 (Advanced compliance & recordkeeping): bundle authored — 4 tasks (RETENTION-policy vertical — configurable per-workspace recordkeeping retention window at LIGHT posture; DB config table -> shared-Zod contract -> RLS+RBAC service/API with read-only cutoff surfacing -> compliance-surface settings UI)
- caller: N-1-next-bundle (wave-28 seed authoring; automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Vertical slice: seed = workspace_retention_policy table + additive migration (d3cc1337, mutable config table, workspace-scoped M8 RLS, retention_period_days + light default, populated-DB migration proof per fd8f2860). Siblings (parent d3cc1337): shared-Zod retention-policy contract in packages/shared (b7786c5b); retention-policy service + RBAC-scoped API with read-only cutoff/beyond-window surfacing (ed4945e0, getDb->FORCE RLS->set_config, @Roles(admin) fail-closed, config-change writes APPEND to M2 audit chain); retention settings + cutoff surfacing UI on the compliance surface (ce75c6c6, alongside shipped export page f331a51c, zinc/emerald + lucide-react + 4px grid, WCAG 2.2, NO purge control).
- M10 ## Scope build order: (1) recordkeeping EXPORTS = SHIPPED (0d2c5f08 + f331a51c); (2) RETENTION policy = THIS BUNDLE; (3) records VIEW = later vertical (NOT authored). Formal attestation/named-regime DEFERRED per LIGHT posture (founder 2026-07-07).
- WORM guardrail HELD in authored prose (binding acceptance line on seed + service + UI): audit-chain immutability + the HMAC-SHA256 hash-chain + the audit_log_no_mutate append-only trigger are PRESERVED and integrity-verifiable. "Retention" = retention-WINDOW POLICY config + read-only surfacing (and config-change events APPENDED to the M2 chain) — NOT deletion/mutation of audit_log_entries or any WORM row. Actual retention-DELETE over WORM data is explicitly OUT of scope / DEFERRED (founder/compliance decision under LIGHT posture).
- Seed-candidate guard (Step 1.4): seed_candidates=0 before authoring (all 5 prior M10 tasks done: exports vertical + 3 hardening/debt). ## Success metric SET (not _TBD) -> decomposition legally fired -> decomposition-complete (not the wave-25/26 incomplete-scope pause). Sizing ~2,000-3,500 net LOC, <=~30 files, 1 seed + 3 siblings. Built ONLY on shipped-and-live surfaces (M2 audit, M8 RLS/getDb, M1 RBAC, packages/shared Zod, shipped compliance/export surface + design system). All schema additive (rollback = DROP TABLE); no existing table mutated.

[2026-07-08] M10 (Advanced compliance & recordkeeping): bundle authored — 3 tasks (records-VIEW vertical — firm-admin in-app browse+filter of retained records = audit-log + deal/pipeline activity; READ-ONLY / WORM-preserving; workspace-RLS-scoped; RBAC compliance/admin; no new migration)
- caller: N-1-next-bundle (wave-29 seed authoring; automatic mode)
- decomposed by: milestone-decomposer sub-agent
- Vertical slice: seed = firm-admin Records view page + deal-activity list read API (d573e7bf-30e8-4eb2-9bba-2b1588f69578). Siblings (parent d573e7bf): shared-Zod records-view/deal-activity list filter contract in packages/shared (6f86b594-569c-43fa-87d2-4294833bf7c9); deterministic RLS-isolation + RBAC-deny + WORM-intact tests, authored test-first (770ab1c4-6e22-493c-9184-b63722b24d1b).
- M10 ## Scope build order: (1) recordkeeping EXPORTS = SHIPPED wave-27 (0d2c5f08 + f331a51c); (2) RETENTION policy = SHIPPED wave-28 (d3cc1337 + b7786c5b + ed4945e0 + ce75c6c6); (3) records VIEW = THIS BUNDLE (last light vertical). Formal attestation/certification-report + named-regime (SOX/FINRA) posture remain DEFERRED per LIGHT posture (founder 2026-07-07).
- Gap closed: the audit-log HALF of the browse surface already ships (AuditLogTable -> GET /compliance/audit-log -> listAsActor -> findFiltered via getDb/RLS, READ-ONLY). The milestone defines "retained records" = audit-log + deal/pipeline activity (same two-scope model as the export vertical). Missing = an in-app browse/filter surface for the DEAL/PIPELINE-ACTIVITY half (findDealRowsBounded is export-path-only, tx-scoped) + a unified firm-admin Records page tying both record types under one browse+filter UX. Seed adds the paginated deal-activity LIST read (mirrors the audit-log list pattern) + the unified Records page.
- READ-ONLY / WORM guardrail (binding acceptance lines on seed + tests): NO mutation surface anywhere — no edit, no delete, no purge control (exactly like the wave-27 export vertical); browsing records emits NO new audit_log_entries row (mirrors the listAsActor "MUST NOT call AuditService.append" invariant); the HMAC-SHA256 hash-chain + append-only trigger + audit_log_entries immutability are untouched (verifyChain still ok after a browse). Workspace-RLS-scoped: reads run through getDb/FORCE RLS as the load-bearing tenant guard (NOT app-level workspace filtering); RLS-isolation test proves a foreign-workspace admin sees zero of this firm's records, exercised as the runtime dealflow_app role. RBAC compliance/admin (advisor own-outreach-scoped or excluded, matching the export gate); fail-closed boot assertion covers the new route.
- NO NEW MIGRATION (confirmed + expected): this is a pure read view over already-existing RLS-covered tables (audit_log_entries + pipeline/mandates). No schema authored = the clean-read-only signal per the slice design. NO email send, NO Anthropic/LLM import, NO new external SDK.
- Seed-candidate guard (Step 1.4): seed_candidates=0 before authoring (all 9 prior M10 tasks done: exports vertical + retention vertical + 3 hardening/debt); 0 open tasks. ## Success metric SET (not _TBD) -> decomposition legally fired -> decomposition-complete. Sizing ~1,500-3,000 net LOC, <=~25 files, 1 seed + 2 siblings. Built ONLY on shipped-and-live surfaces (M2 audit/recordkeeping, M8 RLS/getDb, M1 RBAC, packages/shared Zod, shipped compliance surface + design system).

[2026-07-08] M10 (Advanced compliance & recordkeeping): in_progress → done (N-1 wave-29 milestone-closure; LIGHT-posture complete)
- Trigger: wave-29 shipped the records-VIEW vertical LIVE (@8526999, V-block APPROVED). All 12 M10 child tasks now terminal (open_count=0, done_count=12). LLM-judged scope-shipped per closure invariant (roadmap-lifecycle § Invariants #3).
- Light success metric FULLY MET: a firm admin can (1) export a complete workspace-scoped, integrity-verifiable audit-log + deal activity in a portable format (export itself audit-logged) — wave-27; (2) configure a per-workspace retention window with sensible light defaults — wave-28; (3) browse + filter retained records in-app — wave-29. All READ-ONLY / WORM-preserving, workspace-RLS-scoped, RBAC-gated.
- Delivered across 6 waves: 3 hardening/debt (WORM-migration-AC, auth-hardening, RLS-docs) + 3 light recordkeeping verticals (exports w27, retention w28, records-VIEW w29).
- Formal attestation / certification-report generation + named-regime (SOX/FINRA/SOC2) review posture remain founder-DEFERRED (NOT part of the light metric; founder decision 2026-07-07). Raise to a named-regime target only if the compliance classification is later raised.

[2026-07-08] Next-milestone slot after M10 close — BOARD 7/7 APPROVE-PAUSE (scope-exhaustion / strategic-review founder-pause)
- Trigger: M10 closed (done); active milestone slot empty. Whether to promote a next milestone is NOT mechanical (M9 blocked, M11/M12 _TBD-metric-refused) → strategic → BOARD (automatic mode). Decision slug: next-milestone-slot-after-M10-close. Record: process/waves/wave-29/escalations/board-next-milestone-slot-after-M10-close.md.
- Vote: 7/7 APPROVE-PAUSE (exceeds 6+/7 Tier-3 strict). No dissent, no hard-stop veto. There is NO legal autonomously-buildable next seed — every remaining path needs founder input the founder has reserved.
- Founder-gated pile-up: (a) M9 blocked = choose a deal-source vendor + provide account-issued API key (rule-6 consent gate; task 345dfbc6); (b) M11 = set its _TBD success metric + resolve out-of-MVP blocking prereq 2867d087 (decomposition ritual Step 1 refuses _TBD metrics); (c) M12 = set its _TBD success metric (same refusal); (d) roadmap-refresh / strategic review.
- BOARD-weighted founder recommendation: (a) unblock M9 leads (most seats — serves live H1 integrated-platform bet, competitively urgent vs Datasite/Grata + DealCloud native integrations, most immediate advisor value); (d) roadmap-refresh is the alternative / parallel move to sharpen M11+M12 _TBD metrics and prevent a repeat stall.
- Disposition: N-3 pauses the loop via process/session/.loop-paused.yaml + STATUS: BLOCKED (measured pause — this is scope-exhaustion after M10 completed, NOT anticipatory). Mode flag stays automatic (a pause is not a mode change).
- Permanent-Actions-fix to surface: buildable credential-free scope is structurally exhausted; the founder should also decide the durable next-slot direction so the loop has runway beyond a single unblock.

---
## 2026-07-08 — Next direction after M10 close: INTEGRATIONS (M9), vendor = Affinity (founder decision)
**Decision (founder):** After M10 (Advanced compliance & recordkeeping) completed + the scope-exhaustion BOARD pause (7/7), the founder chose **A — Integrations** and vendor **Affinity** (relationship-intelligence CRM, M&A-native). Unblocks M9 (Integrations & insight); the CRM/deal-source adapter (task 345dfbc6) now targets Affinity.
**What it unblocks:** M9 blocked→in_progress. The Affinity DataSourceAdapter (fetch deals/contacts/orgs from Affinity → normalize → upsert into DealFlow's model) is now buildable against Affinity's public REST API. The account-issued Affinity API key is REQUESTED (see updates/) for the LIVE hookup (deploy secret) — the adapter + mocked tests build autonomously first.
**Founder-reserved still-open:** the Affinity API key (account-issued — founder provides via Affinity → Settings → API); M9's _TBD success metric (for eventual M9 close); the permanent GitHub-Actions CI limit raise.

---
## 2026-07-08 — CRM vendor REDIRECT: Twenty (twenty.com), not Affinity (founder directive)
**Decision (founder, via resume mailbox):** "use https://twenty.com/ for crm." The live CRM/deal-source integration targets **Twenty** — a modern OPEN-SOURCE CRM (GraphQL + REST API; self-hostable or cloud). Supersedes the Affinity vendor choice as the LIVE CRM.
**Disposition:** the Affinity adapter (wave-30, 345dfbc6, built + deployed DORMANT) STAYS registered (a valid 2nd connector — the pluggable-adapter architecture + M3's "≥2 sources" metric benefit from both); it is NOT removed. A NEW Twenty DataSourceAdapter is built (wave-31) as the primary/live CRM. Two adapters (Affinity + Twenty) → serves M3 ≥2-sources + M9.
**Account-issued creds REQUESTED (for the live hookup):** the Twenty API key + the instance base URL (twenty.com cloud OR a self-hosted URL). Env secrets (TWENTY_API_KEY + a connection base-URL config) — the founder provides via Twenty → Settings → Developers/API. The adapter + mocked tests build autonomously first; the live-verify is key-gated.

---
## 2026-07-08 — Next-milestone slot after wave-31 (Twenty CRM adapter) — BOARD 7/7 APPROVE-PAUSE (scope-exhaustion founder-pause)
- Trigger: wave-31 shipped the Twenty CRM DataSourceAdapter (M9; done, deployed DORMANT @b1f81d79, key-gated). Active slot = M9 still in_progress but open_count=0 / seed_candidates=0 with no buildable successor. Whether/what to build next is NOT mechanical → strategic transition → BOARD (automatic mode). Decision slug: next-milestone-slot-wave-31. Record: process/waves/wave-31/escalations/board-next-milestone-slot-wave-31.md.
- Vote: 7/7 APPROVE-PAUSE (exceeds 6+/7 Tier-3-strict). No dissent, no HARD-STOP veto. No legal autonomously-buildable next seed.
- M9 disposition: STAYS in_progress (NOT force-closed). Cannot close — ## Success metric = _TBD by founder (requires a LIVE CRM connection) AND both CRM adapters (Affinity wave-30 + Twenty wave-31) are dormant/key-gated (no live verify without founder-issued keys). 19 done tasks ≠ shipped milestone here. Avoids the Hallucinated-Milestone-Completion failure.
- M11/M12: both ## Success metric = _TBD → milestone-decomposition ritual refuses (no acceptance criteria from an undefined metric). M5/M6/M7 blocked. Only unassigned work = 1 tiny /health doc-polish task (b1a0b2ac).
- Founder-gated pile-up (ordered by value): (#1 highest) provide ONE CRM key (Twenty TWENTY_API_KEY+TWENTY_BASE_URL, OR Affinity AFFINITY_API_KEY) → no-new-code live-hookup wave activates a built adapter + verifies real companies flow into sourcing search; (#2) set M11/M12 success metrics + resolve M11 prereq 2867d087 → unblock decomposition; (#3) roadmap-refresh / strategic review; (standing) permanent GitHub Actions spending-limit fix (CI hard-stopped ~7x).
- BOARD notes: architect-reviewer surfaced M11 fail-closed write-path fix 2867d087 as the one non-padding candidate but rejected it (inert under single-tenant + on a _TBD-metric todo milestone). risk-manager + competitive-analyst both re-flagged CI billing fragility.
- Disposition: N-3 pauses the loop via process/session/.loop-paused.yaml (scope-exhausted-pending-founder) + STATUS: BLOCKED (measured pause — scope exhaustion at a wave boundary, NOT anticipatory). Mode flag stays automatic (a pause is not a mode change). Pattern: 3rd consecutive founder-gated edge (w29 paused → w30 Affinity → w31 Twenty → now) — the loop reliably builds what's asked; LIVE activation + new milestones need founder input.

---
## 2026-07-08 — CRM STRATEGY PIVOT: self-host open-source Twenty as our company/contact data store (founder directive)
**Decision (founder):** "We don't want to use Twenty CRM as an API tool, but want to take the open-source version and host it on our end." Scoping answer: **"Private data store, our UI stays"** — self-host the open-source Twenty on OUR infrastructure as the store-of-record for companies + contacts; DealFlow's EXISTING UI stays the interface and reads + writes to it. NOT Twenty's cloud SaaS.
**Rationale:** data ownership / compliance-first — all deal-adjacent data stays on our infrastructure, no third-party SaaS custody.
**Consequences:**
- The wave-31 Twenty adapter (read path) is REUSED, not retired — its base URL (TWENTY_BASE_URL) now points at our SELF-HOSTED instance instead of Twenty's cloud. Same API surface (self-hosted Twenty exposes the same REST API). The earlier "not as an API tool" = not Twenty's CLOUD; a self-hosted instance accessed via its own API is in-scope.
- **UNBLOCKS the loop:** self-hosted Twenty needs NO founder-issued cloud key — the orchestrator provisions the instance + generates its API key internally (open-source + our infra; rule 6/10). The prior key-gated-live-verify (Twenty cloud key + Affinity key) is dissolved for the self-hosted path.
- The wave-30 Affinity adapter: strategy superseded (we're not aggregating external CRMs; we self-host ONE as our store). Stays registered-dormant, de-prioritized; removal deferred.
- **Scope = multi-wave.** Wave-32 (foundational): stand up self-hosted Twenty on our infra (server + Postgres + Redis + worker per Twenty's self-hosting docs) + auto-provision its API key + activate DealFlow's READ connection (reuse the wave-31 adapter) + verify REAL companies flow into the sourcing search. Later waves: the WRITE path (DealFlow creates/updates companies + contacts in Twenty), migrate the company/contact screens to be backed by it.
- **Roadmap note:** this reshapes M9 (Integrations & insight → now includes the self-hosted CRM data platform). M9's success metric is still _TBD (founder-reserved); a roadmap-refresh to formalize the "self-hosted data platform" theme + set its metric is warranted but non-blocking (the directive seeds the work directly).

---
## 2026-07-08 — Next-milestone slot after wave-32 (M9 self-host Twenty deploy package) — BOARD 7/7 APPROVE-PAUSE (scope-exhaustion founder-pause)
- Trigger: wave-32 shipped the M9 self-host Twenty DEPLOY PACKAGE (task 878c3123; done). B-6 APPROVED after a pgvector rework-fix (commit 8e77719: image → pgvector/pgvector:pg16 + CREATE EXTENSION vector across 3 files; d4943b4 records the APPROVE; merge dd4d240 to main). Secret-free docker-compose + provisioning automation + deploy-doc; twenty.adapter.ts reused byte-identical from wave-31 (no DealFlow app-code change, no migration). Artifacts: infra/twenty-selfhost/ + command-center/dev/SDK-Docs/Twenty/twenty-selfhost.md.
- The LIVE stand-up is FOUNDER-GATED on infra provisioning: needs service-creation access NOT in this env (brain has redeploy-only on the existing dealflow service) AND/OR founder consent to a new billable ~5-service stack + an S3-compatible bucket (Cloudflare R2 / MinIO). A cost/consent gate regardless of access. The package is 100% ready to run.
- Whether/what to build next is NOT mechanical (M9 remaining scope founder-infra-gated; M11/M12 _TBD-metric → decomposition refuses; M5/M6/M7 blocked; only unassigned = 1 tiny /health doc task) → strategic transition → BOARD (automatic). Decision slug: next-milestone-slot-wave-32. Record: process/waves/_archive/wave-32/escalations/board-next-milestone-slot-wave-32.md.
- Vote: 7/7 APPROVE-PAUSE (exceeds 6+/7 Tier-3-strict). No dissent on disposition. No PROMOTE, no REJECT. risk-officer raised a HARD-STOP flag (self-host = DealFlow now owns Twenty's full ops lifecycle: upgrades/security-patching/backups/uptime + billable stack — human sign-off required); it REINFORCES the pause (the decision it guards is exactly the founder ask), does not force a build.
- M9 disposition: STAYS in_progress (NOT force-closed). Cannot close — ## Success metric = _TBD by founder (requires a LIVE CRM connection) AND nothing is live-verified (Affinity wave-30 + Twenty-cloud wave-31 adapters dormant; the self-host package is built-but-not-stood-up). 20 done tasks ≠ shipped milestone here. Avoids Hallucinated-Milestone-Completion.
- No manufactured seed: the lone unassigned /health doc-polish task (b1a0b2ac) was REJECTED as padding rather than promoted to dodge pausing (Placebo-Productivity guard; BOARD realist + counter-thinker concur).
- Tech debt logged (counter-thinker, not to bury): task 2867d087 (M11 `?? DEFAULT_WORKSPACE_ID` write-path fail-open — silent cross-firm data misplacement, the P-4 F1 anti-pattern) is inert at one pilot firm but is a MUST-fix-before-2nd-tenant gate. Correctly not-in-scope now (M11 todo/_TBD); surfaced to the founder packet.
- Founder-gated pile-up (ordered by value): (#1 highest, READY-TO-GO) stand up the self-hosted Twenty (turnkey package on main; needs founder infra provisioning consent + access + S3 choice + a cost ceiling; carries monthly cost + ongoing ops burden) → no-new-code live-hookup wave activates the built read-connection + verifies real companies flow into sourcing search; (#2) set M11/M12 _TBD success metrics + resolve M11 prereq 2867d087 → unblock new-milestone build; (#3) roadmap-refresh / strategic review; (standing) permanent GitHub Actions spending-limit fix (CI hard-stopped ~7x).
- Disposition: N-3 pauses the loop via process/session/.loop-paused.yaml (scope-exhausted-pending-founder) + STATUS: BLOCKED (measured pause — scope exhaustion at a wave boundary after wave-32 completed all P→L stages, NOT anticipatory). Mode flag stays automatic (a pause is not a mode change). Pattern: 4th consecutive founder-gated edge (w29 → w30 Affinity → w31 Twenty-cloud → w32 self-host package → now). The loop reliably builds what's asked; LIVE activation + new milestones need founder input.

---
## 2026-07-08 — Founder "you run it": provisioning self-hosted Twenty on Railway (capability confirmed)
**Decision (founder):** "you run it" — go ahead and stand up the self-hosted Twenty myself.
**Capability confirmed:** APP_RAILWAY_TOKEN (project token) CAN create/delete services in project `app-arina-5ywq3s` (env production, id 0e84f0b6-1b1d-469f-91b9-caf4e59c9ba8) — verified via a serviceCreate probe (then cleaned up). So NO founder infra access needed; I provision via the Railway GraphQL API.
**Defaults chosen (technical, rule 17 — founder didn't specify; minimize founder deps):**
- **Storage = MinIO** as a self-contained service in the project (NOT Cloudflare R2 — R2 needs an external account/creds the founder didn't provide; MinIO keeps it fully self-hosted, no external account, aligns with data-ownership).
- **Cost posture:** the smallest instances that run Twenty (Twenty needs ~3 CPU/4.5GB total across services — a modest monthly cost on the existing Railway project; "you run it" = cost consent).
- All secrets orchestrator-generated (openssl/crypto), set as Railway service env vars, NEVER committed.
**Wave-33 = the LIVE stand-up** (executes the wave-32 package): create the Twenty stack services (twenty-db[pgvector] + twenty-redis + twenty-minio + twenty-server + twenty-worker) → configure env + volumes → deploy → bootstrap the API key (GraphQL createApiKey→generateApiKeyToken) → seed sample companies → wire DealFlow's TWENTY_BASE_URL(https)+TWENTY_API_KEY → LIVE-verify real companies flow into sourcing.

---
## 2026-07-08 — Self-hosted Twenty LIVE on Railway (infra done; API-key one-time-manual)
Stood up the full self-hosted Twenty stack on Railway (5 services: pgvector DB, Redis, MinIO, server, worker) — server LIVE at https://twenty-server-production-aee3.up.railway.app. Fixed 4 real deploy issues autonomously (PGDATA, wrong image name twentycrm/twenty, DB-url encoding, https domain). API-key bootstrap hit the documented one-time-manual boundary: Twenty v2.19 disables introspection + exposes no headless first-user signup, so the founder must create the first admin once (a human account action) + generate an API key; everything after (seed, wire, live-verify) is automated. Not a code blocker — an account-creation gate.
