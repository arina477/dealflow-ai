# Wave 38 — P-0 Frame (FIX: prod migrations don't auto-apply on deploy)
## Root cause (diagnosed on the deployed app + code):
1. NOTHING runs migrations on deploy: start command = `node apps/api/dist/main.js` (no migrate step); main.ts bootstrap has NO runMigrations call (it only compares DATABASE_URL vs MIGRATE_DATABASE_URL for the distinctness guard); the build (`pnpm build`) doesn't migrate. → new migrations (0021, rate_limit_hits) never applied to prod (0021 applied MANUALLY this session).
2. drizzle.config.ts `dbCredentials.url = DATABASE_URL` (the dealflow_app role — NOSUPERUSER NOBYPASSRLS) which CANNOT run privileged migration ops (CREATE FUNCTION/ALTER/policies). Migrations must run as the OWNER role = MIGRATE_DATABASE_URL.
## Fix (P-3): add a reliable migrate-on-deploy that runs pending migrations with the OWNER role (MIGRATE_DATABASE_URL) + the shipped migrations folder, BEFORE the app serves. Options for the specialist: (a) migrate-on-boot in main.ts (a runMigrations() using drizzle's migrator + a MIGRATE_DATABASE_URL connection + ./src/db/migrations, run before Nest bootstrap, idempotent via drizzle's journal table, then close that connection + serve as DATABASE_URL); OR (b) a Railway release/pre-deploy command. Prefer (a) migrate-on-boot — self-contained, no platform config. MUST: use MIGRATE_DATABASE_URL (owner), ship the .sql migrations in the runtime image, be idempotent (drizzle __drizzle_migrations journal), fail-loud if a migration errors (don't serve a half-migrated app), and NOT break the assertUrlsDistinct / assertNonSuperuser guards (migrate connection is separate + closed before the app's RLS-scoped pool opens).
## Verify: deploy → confirm pending migrations apply (esp. rate_limit_hits table now EXISTS; 0021 idempotent no-op) + the app boots + serves. This unblocks EVERY future migration.
## design_gap false (infra/backend). D-skip. single-spec. claimed: 7f4d150b.
