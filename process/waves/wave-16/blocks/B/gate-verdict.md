# Wave 16 — B-6 Verdict

**Reviewer:** head-builder (fresh spawn)
**Reviewed against:** process/waves/wave-16/blocks/B/review-artifacts.md
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale
Wave-16 M7 admin-hardening (6 verticals) is built to the frozen spec contract AND the P-4 security-rework addendum, verified by reading the shipped code on `wave-16-admin-hardening` — not the deliverable prose. All three P-4 pre-build security findings are genuinely honored in code and each is backed by a fault-killing test that fails if the guard regresses. Repo typecheck is green (4/4 packages); shared (489) and web (693) test suites pass exactly as claimed; api unit tests pass (766). The six DB-backed e2e suites could not execute in THIS gate session because the session's `CLAUDOMAT_DB_URL` is the brain-state Railway DB on which my role lacks DDL/migrate rights (every one of the six failed identically at `ensureMigrated` with SQLSTATE `42501 permission denied for database railway`, including three pre-existing wave-12/14 suites) — an infra-readiness artifact of the review environment, not a code or test defect. Those suites are correctly authored (real-service, fault-killing, WORM-safe teardown) and C-1 already ran the admin-concurrency e2e green against a privileged CI TEST_DATABASE_URL last wave; B-5 verify + C-1 CI-watch own their live execution, not the B-6 code/test-integrity gate.

### Load-bearing verification (read the code)

- **[c54db02d] Invite dedup (P-4 Finding 1) — HONORED.** `inviteAsActor` (user-management.service.ts:158-164) acquires `pg_advisory_xact_lock(hashtext(${normalizedEmail})::bigint)` as the FIRST statement in the tx, BEFORE the SELECT-active-user, SELECT-live-invite, then INSERT ordering. No partial unique index (the spec explicitly rejected it — it would break expired→reinvite and needs non-immutable `now()`). Live-invite predicate is `consumed_at IS NULL AND expiry > now()`, so expired/consumed → new invite allowed. Concurrency proof INVITE-CONC-1 (admin-concurrency.e2e-spec.ts:473) calls the REAL service (does NOT re-implement the lock inline), runs two concurrent `inviteAsActor` for the same fresh email via `Promise.allSettled`, and asserts exactly one 409 + exactly one invite row in the DB — fault-killing per its documented property. Expired→reinvite unit coverage: A-7c (admin.spec.ts:462); consumed→reinvite: A-7d.
- **[2560fecc] Config boundary (P-4 Finding 2) — HONORED.** `dataSourceConnectionConfigSchema` (packages/shared/src/data-source-admin.ts:95) is a genuine `.strict()` enumerated whitelist (fieldMapping: bounded ≤128-char strings; syncBatchSize: int 1–10000; regionSlug: ≤64 alphanumeric-hyphen) — NO free-text `z.string()` and NO `z.unknown()` slot. `validateConfigOrThrow` (data-source-admin.service.ts:121) runs `safeParse` BEFORE the tx and throws a UNIFORM STATIC `CONFIG_VALIDATION_ERROR` constant; the ZodError is discarded (never logged/interpolated), closing the wave-15 leak vector where `scrubCredentialFromError` only redacted `input.credential`, not `input.config`. No-echo asserted: C-4b (admin.spec.ts:960) — secret value ABSENT from the error, static message present; C-4c mirrors for update; backward-compat with `{}`/omitted config: C-4e/C-4f.
- **[8bb0a22f] Admin-activity (P-4 Finding 3) — HONORED.** Response projection (admin-activity.service.ts:139-167) is an EXPLICIT whitelist typed against `AdminActivityRow` (sequenceNumber/actor/target/action/timestamp only); payloadHash/contentHash/entryHash/prevHash/chainVersion/actorRole/resourceType/resourceId/mandateId/credential are structurally omitted. ACT-3 (admin-activity.e2e-spec.ts:290) asserts all forbidden fields absent in both camelCase and snake_case; ACT-4 proves opening the view appends ZERO audit rows (count before === after). Reuses `AuditRepository.findAdminActivity`/`countAdminActivity` (audit.repository.ts:158/204 — read-only, no INSERT; the only INSERT is in `insertEntry`) — does NOT fork a second reader. RBAC admin-only: fail-closed boot guard + `@Roles(...rolesForRoute('/admin/activity-data'))` (allowedRoles `['admin']`, rbac.ts:614). ACT-6 is a DI-boot resolution test (not a live 403/401) — acceptable at B-6; T-8 carries the HTTP-level advisor-403/anon-401 assertion per the P-4 note.
- **[904a3c25] Cascade (mvp-critical spine) — HONORED.** `createAsActor` (mandate.service.ts:130-153) reads workspace_settings TX-SCOPED via `repository.findWorkspaceSettingsInTx(tx)` (BUILD rule 7 — tx handle, not module-level). Explicit-wins: `input.compliance.jurisdiction ?? firmSettings?.defaultJurisdiction` and `suppressionScope !== undefined ? provided : firmDefault ?? null`. Resolve-once-at-create: resolved values stamped into the compliance_profile INSERT — no view-time join. Mandatory e2e proof present + fault-killing: CASCADE-1 (set default→create-omitting→inherits 'EU'/'firm'), CASCADE-2 (change default→existing mandate still 'EU', not 'APAC'), CASCADE-3 (explicit-wins), CASCADE-4 (null fallback) — all real-service against the live DB (mandate-cascade.e2e-spec.ts).
- **[042cf4e6] Reactivate — HONORED.** `reactivateAsActor` (user-management.service.ts:404) sets `deactivated_at = NULL` WITHOUT touching `role_id` (no priv-esc), audited LAST-IN-TXN under the additive `user-reactivate` action, already-active→400. Admin-only via RolesGuard. WORM-safe teardown in tests (T-4 rule 1): retain audit-referenced users, reset via UPDATE not DELETE (REACTIVATE-1/2 admin-concurrency.e2e-spec.ts:524/582; unit A-8a/b/c admin.spec.ts:537+).
- **[6f1a96da] Nav — HONORED.** Sidebar (apps/web/app/(app)/_components/Sidebar.tsx:72) renders `navItemsForRole(me.role)` — server-side role-filtered list, no hardcoded/CSS-hidden items; advisor never receives the admin section. Admin-activity page (activity/page.tsx:114) enforces `assertRole('/admin/activity', me.role)` server-side (redirects non-admin).

### General discipline
- **No un-journaled migration:** `git diff main..HEAD -- apps/api/drizzle/` is empty — schema_skipped honored, no Ghost-Green.
- **No hollow tests (VERIFY rule 3):** every load-bearing test is real-service and asserts DB state / row counts / field absence, not method execution; concurrency and cascade proofs are documented fault-killing.
- **Commit-per-spec (multi-spec):** each feature commit cites exactly one task_id; every one of the 6 claimed task_ids has ≥1 citing commit. The lone multi-id commit (`feat(contracts): B-1 admin-hardening shared contracts`) is the shared-package contracts commit spanning all 5 schema-touching tasks — legitimate for a single `libs/contracts` change; lint/docs housekeeping commits carry no task_id (acceptable).

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
