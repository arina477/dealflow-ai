# Wave 16 — V-3 Verdict

**Reviewer:** head-verifier (fresh spawn, Phase 1 gate — V-block exit)
**Reviewed against:** process/waves/wave-16/blocks/V/review-artifacts.md
**Wave:** 16 — M7 admin-hardening (SECURITY-SCOPE-TIGHTENED: invite/user-state + config-security + compliance-default cascade)
**Deployed:** LIVE @ `d72d7cb735aff74fcdbdb28bd20f19bf711cc539` (Railway api+web meta.commitHash==d72d7cb; independently re-probed this pass)
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale

Both V-1 reviewers APPROVE against the deployed state and both verdicts are **credible on proof-carrying grounds, not inference**. I did not rubber-stamp the reviewer YAML — I independently re-executed the load-bearing deployed-state probes and read the deployed source for every structural compliance claim. Karen's 8 source-claim confirmations are sound: I re-verified `/health` serves the exact deployed hash `d72d7cb` on its own domain (no Ghost-Green / stale-mirage), all three load-bearing routes (`POST /admin/users/:id/reactivate`, `GET /admin/activity-data`, `GET /compliance/audit-log/verify`) return **401** anon (registered + fail-closed auth-gated), the negative-control `/admin/nonexistent-route-xyz` returns **404** (proving the 401s are real route hits, not a catch-all), web root **307**, HEAD==d72d7cb, and the migration journal max idx = 13 with no stray `0014` (no un-journaled migration). I then read the deployed source and confirmed the three structural compliance invariants that a false-green cannot fake: (1) admin-activity's response projection is a hard object-literal whitelist `{sequenceNumber, actor, target, action, timestamp}` — hash/credential/resource fields are structurally absent, leak-proof by construction (stronger than a response-grep); (2) `dataSourceConnectionConfigSchema` is a genuine `.strict()` object with only bounded non-secret fields (no `z.string()` free-text slot, no `z.unknown()`), and `validateConfigOrThrow` runs `safeParse` BEFORE any DB write, discards the ZodError, and throws a uniform static `CONFIG_VALIDATION_ERROR` (no value echo); (3) `AuditRepository` has exactly ONE `.insert(` path (`insertEntry`), `findAdminActivity`/`countAdminActivity` are `.select()`-only, and `audit-log/verify` is a pure read (`verifyChain()`) behind a fail-closed boot guard — so the new `user-reactivate` action cannot corrupt the append-only hash chain, and C-2 confirms the chain verifies `ok:true` (entriesChecked 324→328) after it shipped.

**Karen's C-2-single-sourcing disclosure is sound, NOT a blocking gap.** Three fully-authed *runtime response-body* assertions (activity-data 200 row-set, reactivate live 400/403/200, verify {ok:true}) could not be re-executed at V-1 because no prod admin credentials live in the repo. Karen disclosed this honestly and did not claim to have run them. This does not block because: (a) she independently proved the deployed *enforcement scaffolding* for each (route live + auth-gated + source-level structural guarantee); (b) C-2 head-ci-cd executed them against a live authed session with a **positive control** (advisor `/mandates`→200 proves the 307/403 responses are role-gating, not blanket auth-fail) and documented concrete per-check results (status codes, exact row-key sets, row counts, grep results); (c) every claim is source-corroborated. The residual weakness — V-1 cannot *independently* re-run the deepest authed body checks — is a real systemic tooling gap, correctly filed by Karen as a recommendation (populate gitignored prod fixtures), not a wave blocker. The deployed invariants are proven structurally at source; the authed body checks are corroborating evidence, not the sole basis.

**jenny's APPROVE is credible.** 0 spec-drift across all 6 blocks; the P-4 SECURITY REWORK (advisory-lock-not-partial-index, config no-echo + no-free-text-slot, drop-vacuous-firm-scope) is faithfully deployed — I confirmed the advisory-lock-not-index and the `.strict()` no-free-text schema at source. `disclaimer_template_id` being DERIVED from the resolved jurisdiction (rather than independently inherited) is the correct security posture — it prevents a user/firm injecting an arbitrary disclaimer FK — is documented in the docstring, and is a derived consequence of the jurisdiction cascade, not drift.

**V-2 triage is correctly ZERO-blocking; no load-bearing claim was downgraded.** The four load-bearing invariants (race-safe advisory-lock invite, cascade-inherits-with-no-retroactive-mutation, admin-activity read-only/no-secret, config no-echo) are all confirmed TRUE at deployed source AND backed by fault-killing tests — they *pass*, they were never findings to downgrade. **The G-3 non-blocking judgment is correct under scrutiny.** `fieldMapping: z.record(z.string().max(128))` is bounded-length but free-value, so a ≤128-char secret could technically be stored as plaintext in `config.fieldMapping`. This is a genuine residual soft spot but does NOT defeat the encrypted-at-rest invariant enough to block: (a) the *dedicated* credential path is AES-256-GCM (`v1:iv:tag:ct`) and structurally intact — the encrypted-at-rest invariant *for credentials* holds; (b) P-4 Finding 2's literal requirement was "no *dedicated* free-text slot," which is met (`fieldMapping` is a bounded provider-field-name dictionary with a legitimate purpose, not a free-text config blob); (c) exploiting it requires an operator to *deliberately misuse* a field documented as non-secret — it is not an attacker-reachable bypass and no legitimate flow takes that path; (d) the hardening fix (a value-format regex on `fieldMapping`) is correctly routed to next-wave P-2. This is a spec-anticipation gap, not a compliance-gate bypass — blocking on it would be a triage-discipline failure against a spec-compliant deployment. G-1 (hashtext 32-bit collision — throughput-only, correctness unaffected because the SELECT-live-check under the lock filters the exact email) and G-2 (sequenceNumber cursor exposed to admins — a monotonic audit index on an admin-trusted surface, not secret/PII) are likewise correctly non-blocking.

**No compliance-gate bypass, credential leak, or audit-chain break was missed.** The config gate is non-bypassable (runs before every DB write, fail-closed, uniform static, ZodError discarded). Admin-activity cannot leak a secret (structural whitelist). The audit chain is append-only with a single INSERT path and a pure-read verifier; it verifies `ok:true` (324→328) after the new `user-reactivate` action, and the direct-SQL prod-cleanup correctly did NOT touch the app chain. **Deferral honesty holds:** M7 milestone and all 6 tasks remain `in_progress` (not falsely closed); sending-domain #141 is deferred and absent from the deployed diff (absent-not-half-built), per the spec head + P-3.

Fast-fix queue is EMPTY (V-2 `fast_fix_queue: []`) → Phase 2 skipped. Phase 1 emits APPROVED.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
