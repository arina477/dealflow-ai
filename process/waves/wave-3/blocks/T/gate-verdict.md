# Wave 3 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-wave3-t9)
**Reviewed against:** process/waves/wave-3/blocks/T/review-artifacts.md + findings-aggregate.md
**Attempt:** 1  (1 = first gate, 2+ = post-rework)

## Verdict
APPROVED

## Rationale
The wave's load-bearing security surface — per-route RBAC enforcement on a compliance-first platform — is genuinely tested and independently LIVE-verified, not coverage theater. RBAC is proven at two orthogonal layers: unit (compliance.rbac.spec exercises guard→DB-role with a mocked AuthRepository per-role 200/403, plus fail-closed on empty @Roles and DB-authoritative-over-stale-claim as concrete behavioral assertions) AND live at C-2 against the deployed 935b847 hash with fresh unique cookie-jar users minted via POST /auth/invite — compliance→200 `{pendingCount:0,items:[]}`, advisor→403 `{message:Forbidden,statusCode:403}` with no resource or role leak, unauth→401. This is the both-directions assertion I require (compliance-allow AND advisor-deny AND unauth-reject), not the tautological inverse, and RolesGuard reads the role from the server-verified SuperTokens session claim re-verified against the app-DB, not client input — closing the stale-privilege window. Every compliance invariant is covered: deny-with-no-leak (403 body carries no data/role), fail-closed empty-@Roles (guard throws + boot-time assertion; the non-empty-@Roles compliance-controller boot-assert did not throw live), DB-authoritative role, allowlist opt-in (/auth/*,/health ungated so login is not broken, verified live), and the nav⊆RBAC invariant contract-tested by construction across all four roles (no nav item can point at a route the role would 403 on). The real-browser E2E (7/7 PASS on live Railway with Chrome confirmed installed — chromium-1208 via pw-compat shim, non-zero executed-test count, no silent skip false-PASS) exercises real behavior: role-aware nav sets genuinely diverge in-browser (advisor sees Mandates, compliance does not; both see Compliance per rbac.ts), login lands on `/` and is NOT bounced, unauth `/`→307→/login — corroborated independently by C-2 ("compliance nav ×9 vs advisor Mandates nav differ — not a static shell"). T-5 was honest about the stale wave-2 `/dashboard`→`/` assertions: it correctly classified them as spec-staleness from the intentional wave-3 route move (documented with root cause, the failing wave-2 specs left visible rather than deleted), while the new wave-3 spec asserts the correct `/`; the inline test-bug corrections (strict-mode `.first()`, URL regex) are legitimate spec-level fixes, not product-masking. T-7 skip is justified (AppShell+RBAC, no large-data path; the single extra guarded-request DB read is negligible). The 2 low findings (lucide icon-cast typing; guard 1-DB-read/request accepted as correctness>micro-perf) are correctly non-blocking and 0 critical; the 1 test-maintenance item was fixed. The only caveat — T-6's non-fatal `aria-current="page"` active-item assertion (production-SSR `x-invoke-path` limitation) and the content-area-bg test-locator precision artifact (source confirms `#fcfcfd`) — is cosmetic, documented, tracked for a future client-side usePathname enhancement, and touches no RBAC/compliance invariant. No silently-skipped layer, no zero-execution E2E, no layout-only false-PASS, no untested compliance invariant. This clears the APPROVED bar.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
