# Wave 37 — B-6 APPROVE
## Phase 1 head-builder APPROVED: all 8 P-4 security invariants verified in code — server-minted workspace_id (gen_random_uuid in SECURITY-DEFINER 0021, no client param, .strict() schema) + atomic+compensate-delete-Core-user + grant-admin reuses assignRoleAsActor (admin/ untouched, no bypass) + M8 RLS holds + additive migration (CREATE FUNCTION+GRANT only) + rate-limited (3/60s+5/hr fail-closed-soft) + frontend-correct + no-regression. The 7 aligned web RBAC tests = legitimate wave-36 admin-read correction (rbac.ts untouched), NOT a masked regression.
## Phase 2 /review SHIP (no P0/P1): 4 crown jewels hold (server-minted-unsteerable, fresh-isolated-workspace-per-firm-no-collision, grant-admin-untouched, atomic+compensate). Duplicate firmName → separate isolated workspace (correct; workspaces.name no unique constraint). Self-serve ONLY creates NEW firm (never joins existing; invite-JOIN path untouched). 1 P2 nit FIXED: create-firm page dead 409 branch + misleading firm-name-unique copy → corrected to a 400 email-may-be-registered message.
## Final: APPROVE. → C-block.
