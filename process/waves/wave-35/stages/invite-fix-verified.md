# Wave 35 — pilot-readiness: teammate-invite FIXED + verified (deployed E2E)
Root cause (deployed logs): POST /auth/invite had no SessionGuard → no workspace context in ALS → RLS-scoped invite INSERT threw 500. Fix (dc0f9c3, merged 3f0554f): @UseGuards(SessionGuard,RolesGuard)+@Roles(admin) → the WorkspaceInterceptor populates the ALS → INSERT succeeds; invitedBy set from the admin session; cross-workspace safety preserved (workspace from ALS, not body). Also closed the unguarded-endpoint SECURITY gap.
VERIFIED E2E on the deployed app: (1) admin invites → 201 {token,expiry}; (2) non-admin (advisor) invite → 403; (3) new teammate signs up with the token → 201, email-from-invite + assigned role (analyst), pre-auth invite lookup via SECURITY DEFINER resolve_invite (RLS-exempt); (4) new teammate logs in → OK. A pilot firm can now add its people.
## Remaining pilot-readiness follow-ups (noted, non-blocking): role-fragmented read screens (admin can't read sourcing/pipeline); rate_limit_hits table missing on prod (rate-limit degrades to in-process — a real hardening item).
