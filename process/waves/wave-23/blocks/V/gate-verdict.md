# Wave 23 — V-3 Verdict

**Reviewer:** head-verifier (fresh spawn, V-3 Phase 1 gate)
**Reviewed against:** process/waves/wave-23/blocks/V/review-artifacts.md
**Attempt:** 1  (first gate)
**Deployed tip:** 6c229197f4dfb12352e766e1754502a9f76b51e9 (@6c22919); checkout HEAD 9df066b (post-C+T deliverables, [skip ci])

## Verdict
APPROVED

## Rationale

Both V-1 reviewers APPROVE against the deployed SHA and both verdicts survive independent re-verification against observable deployed-state artifacts — I did not accept either on inference. **Karen (7/7 TRUE, 0 blocking)** and **jenny (8 MATCHES / 0 DRIFTS)** each traced their load-bearing claims to the deployed tip, not the working tree; the live-authed per-mandate deferral is acceptable because the determinism + cross-firm proof is CI-authoritative and CI ran non-skipped on the exact deployed SHA. **(1) Pure-determinism proof is SOUND — Done-Theater avoided.** `git show 6c22919:…scorer.ts` confirms zero `Date.now()`/`Math.random()`/`new Date(` in executable code (all 12 hits are "NOT Date.now()" disclaimer comments); the scorer takes plain arrays + a caller-supplied ISO `referenceInstant` and returns an object, using only deterministic `Date.parse`. This is genuinely pure code, AND the CI-layer proof is real: `seller-intent.scorer.spec` (26/26) asserts byte-identical output on repeat, a time-invariance snapshot that mutates `referenceInstant` to prove it is *used*, and a comment-stripped source-grep that fault-kills any future `Date.now()`/`Math.random()` regression. Determinism is proven at both the code and CI layers, not merely claimed. **(2) Cross-firm isolation proof is SOUND and CI-authoritative (non-skipped).** `seller-intent-isolation.e2e` @6c22919 instantiates the REAL `SellerIntentService` via `workspaceAls.run` under `SET ROLE dealflow_app` (drops superuser → FORCE RLS) — not re-implemented SQL. SIT-1 asserts WS_B mandate IDs fully absent from WS_A results with an independent superuser-SQL cross-check; SIT-3 is fault-killing (no-ALS `getList()` throws fail-closed). Live 2-workspace testing is structurally impossible against the single prod firm, so the CI e2e is the correct authority — and `gh api runs/28858565829` returns `conclusion: success`, `head_sha: 6c229197…` == deployed tip, `event: push` — a real green on the exact deployed artifact, no Ghost-Green. **(3) Both reviewers credible.** Independent source reads confirm: repository routes every query through `getDb(this.db)` (never a raw handle) with SELECT-only queries and no `AuditService` (read-only intact, audit chain undisturbed — `/compliance/audit-log/verify` anon 401 live); service is fail-closed (`getWorkspaceId() === null → throw`); controller has a boot-time RBAC fail-closed guard + `SessionGuard`+`RolesGuard` and exposes only `@Get()` (no write endpoint / no self-approval surface applicable to a read-only wave). No leak, RBAC, read-only, purity, or tieBreak-bypass vector was missed. **(4) V-2 triage is CORRECT — 0 blocking, no load-bearing item downgraded.** The three non-blocking items are handled properly: `breakdown-Zod-.passthrough` (LOW → next-P-2) is a uniform codebase READ-shape convention with SI1 holding by construction — no code path emits `tieBreak`, so `.strict()` is defensive hardening not a live defect; `referenceInstant`-dormant-cooling (LOW spec-gap → founder-confirm, bundled with the metric poll) is a stated + boundary-tested product semantic, correctly a founder call not a code defect; `_TBD` success-metric poll → N-block/founder is the standard M9-close obligation. **The NaN-seed fix is genuinely IN the deployed tip, not just a test:** `git show 6c22919:…scorer.ts` shows the recency reducer seeded with `completed[0]`'s effective timestamp (never `''`, avoiding `Date.parse('')===NaN`) and chronological `Date.parse` comparison — the fix content shipped. Karen's V-1-karen.md:52 phrasing ("commit 854bad5 reachable from 6c22919") is imprecise — `git merge-base --is-ancestor` returns false because the wave was squash-merged (C-1 `merge_strategy: squash`), so `854bad5` is a pre-squash commit outside the linear ancestry — but her load-bearing conclusion (the fix is deployed) is correct and independently confirmed by inspecting the deployed tree directly. This is a documentation imprecision in one reviewer note, not a load-bearing defect, and does not affect the verdict. Fast-fix queue is empty; Phase 2 skips.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
