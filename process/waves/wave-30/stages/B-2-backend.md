# Wave 30 — B-1/B-2 (Affinity DataSourceAdapter)
Commit 4ee349c. SDK-research-FIRST honored:
- B-1 SDK doc command-center/dev/SDK-Docs/Affinity/affinity.md (live-researched api-docs.affinity.co): auth (HTTP Basic, key-as-password, base64(:+key)), pagination (page_size=500 + page_token loop, next_page_token=null termination), rate-limits (900/user/min, 429 + X-Ratelimit-Limit-User-Reset), error shapes, normalize map (org id/name/domain + persons -> NormalizedSourceRecord), Zod response types (.passthrough). Registry row added.
- B-2 affinity.adapter.ts (providerKey=AFFINITY, registered in createDefaultRegistry): env AFFINITY_API_KEY graceful-no-op-if-absent (returns [], app boots + fixture search works); INTERNAL pagination (all org pages via page_token); 429 backoff (X-Ratelimit header + exponential); 5xx/network retry (max 3); timeout (AbortController 30s); boundary-Zod (malformed->logged skip, no crash); partial-failure (returns collected); normalize (org + <=10 persons/org). NOTE-2: no healthCheck/withRetry (inlined). Workspace-scoping inherited from ingestion (adapter workspace-agnostic).
- Tests (13, all mocked vi.stubGlobal fetch, NO live): multi-page-pagination(3 pages, orgFetchCount=3), 429-backoff-retry, 5xx-retry, timeout, normalize, absent-key-noop, partial-failure, boundary-Zod, domain-fallback, no-domain, person-Zod-skip, auth-header, providerKey. Full sourcing suite 144/144.
- typecheck 4/4, biome clean, NO key committed (grep verified). Deviations: (1) NOTE-1 env=AFFINITY_API_KEY [adapter reads its own env var self-contained; process.env[providerKey] is conceptual]; (2) contact-title undefined [Affinity v1 no title field, deferred]; (3) person-cap 10/org [rate-limit]; (4) no-Retry-After [uses X-Ratelimit, verify at live-hookup].
- LIVE-verify deferred to C-2 (needs founder AFFINITY_API_KEY; adapter wired+ready, setting the Railway env var triggers the live fetch).
new_files: SDK-Docs/Affinity/affinity.md, affinity.adapter.ts, affinity.adapter.spec.ts
