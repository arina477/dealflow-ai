# Wave 29 — B-3 (deal-activity records browse UI)
Commit d2983f1. Scope/tab on /compliance/audit-log: ScopeToggle (role=tablist/tab, deal-activity tab conditional on canSeeDealActivity) + RecordsPanel (activeScope state) + DealActivityTable (mirrors AuditLogTable shell/filters/pagination/states/tokens EXACTLY — thin table since AuditLogTable is tightly coupled to audit columns; design reused not rebuilt). Columns: Date/Counterpart-Seller/Stage/DealSourceType-pill/CreatedBy. Fetch apiFetch /compliance/records-deal-activity-data proxy (rid anti-csrf) + dealActivityBrowseResponseSchema validate; pagination limit 25 offset; filters (type/from/to/mandate). RBAC DOUBLE-GATED: canSeeDealActivity=compliance/admin server-side (advisor SSR-fetch skipped + tab hidden + panel guards mount). READ-ONLY (no edit/delete/send/export affordance). a11y (tablist/tab/aria-selected, role=status/search, aria-live, htmlFor/id, focus rings, no color-only). typecheck 4/4, lint exit 0, web 989 pass (38 new incl advisor-isolation + read-only-boundary). Deviation: thin DealActivityTable vs parametric AuditLogTable reuse (justified — AuditLogTable audit-column-coupled; pattern/tokens reused exactly). D-SKIP satisfied.
