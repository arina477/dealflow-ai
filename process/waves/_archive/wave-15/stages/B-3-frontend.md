# Wave 15 — B-3-frontend
3 admin pages (users/settings/integrations per designs); credential form WRITE-ONLY (password/never-prefilled/hasCredential-badge); last-admin-409 handled; AppShell placeholders (/matches,/outreach) + TopBar fix (x-pathname middleware); RBAC role-reverify test; SSR + /admin/*-data proxies + rid; NO send/AI (670ecc0)
