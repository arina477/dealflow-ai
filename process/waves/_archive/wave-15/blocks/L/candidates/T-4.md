1. In a real-DB integration suite, never hard-delete a row FK-referenced by an append-only immutable table.
   Why: An immutable-table mutation trigger rejects the cascading update, breaking teardown and CI.
