# Wave 13 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-w13-t9)
**Reviewed against:** process/waves/wave-13/blocks/T/review-artifacts.md + findings-aggregate.md
**Attempt:** 1  (first gate)

## Verdict
APPROVED

## Rationale

All four cardinal compliance invariants of the M6 recordkeeping-export vertical are covered by REAL, non-hollow assertions executed against deployed prod state — not inferred from a green badge. (1) **Tamper-evident HMAC hash-chain integrity** is proven LIVE: `GET /compliance/audit-log/verify` → 200 `{ok:true, entriesChecked:309}` is the real `AuditVerifier.verifyChain` shape computed over the accumulated 309-entry production chain (not a stub, not a fixture). (2) **Export atomicity / audited-last-in-txn** is proven LIVE by the 309→310 delta (verify before = 309, verify after = 310, `export_generated` newest action) with unit-level rollback-on-audit-fail coverage — exactly-one-row holds even for the zero-entry scope. (3) **SoD/RBAC** is proven LIVE server-side (advisor export 403 + advisor verify 403 via RolesGuard, anon 401), not merely a hidden button. (4) **Read-only-zero-audit + M2 input-validation** are proven LIVE (advisor read emits 0 audit rows; `?limit=999999`→400 clamp, `?mandateId=notauuid`→400 BadRequest at the boundary, never 500/unbounded) with unit backing. **AC-STRIP** holds on the DEPLOYED authed HTML for BOTH roles with assertions on concrete DOM data (data-testid counts, byte-size distinguishing the real 179 KB/31.6 KB authed page from the ~10 KB login shell, zero edit/delete/send/AI affordance) — this is behavioral DOM-content verification, not layout-only container-render false-PASS. The negative-test matrix is present (advisor-403, M2-400, illegal input, rollback-on-audit-fail), so this is not happy-path-only coverage. CI provenance is audited to the exact SHA (run 28777726235, 5/5 jobs incl `pnpm audit --audit-level=high`, headSha=5293045 == post-push main HEAD; deploy target 2ec4953 = 5293045 + docs-only chore) — not gating on a bare badge. Ephemeral invite→signup sessions were used (isolated test-seed, not elevated long-lived prod tokens). The single MEDIUM finding (DEV-2, mandate-derivation real-DB coverage) is a functional-completeness gap on one FILTERED export path, not an untested or bypassed cardinal invariant — it is carried to V-2 with the reasoning and a strong remediation directive below. The wave's compliance payoff is demonstrably real, so I approve the T-block and route DEV-2 as a surfaced finding per the T-block→V-2 findings contract.

### Playwright-binary infra-readiness check (my non-negotiable gate) — DOES NOT FIRE

No Playwright/browser E2E run was claimed this wave. T-5 (e2e) and T-6 (layout) were satisfied via C-2 live authenticated HTTP + HTML-content probes that produced **non-zero, specific assertions** against the deployed UI (compliance page 200/179 KB with `export-panel`=1/`export-cta`=1/`integrity-badge`=1; advisor page 200/31.6 KB with `export-panel`=0/`export-cta`=0/download=0; live read/verify/export; advisor-403; M2-400). The ESCALATE trigger targets a Playwright run that "passes" with **zero executed tests** because the Chrome binary is absent (silent exit-0 false-PASS) — that failure mode did NOT occur here: the deployed UI was genuinely exercised with content-level assertions, so the executed-verification count is non-zero and the UI-change coverage requirement is met in substance. **Residual (LOW, non-blocking):** live-browser cross-viewport visual-obstruction verification (z-index/overlay/mobile-overflow of interactive controls) was not performed; acceptable for a read-only recordkeeping/export surface with no interactive approval control whose obstruction would carry compliance consequence. If a future wave adds an interactive compliance-action control on this page, a live-browser layout pass (ui-comprehensive-tester) becomes required once the binary is installed.

### DEV-2 (mandate-scope derivation SQL) — the central judgment, carried to V-2 as MEDIUM

The load-bearing mandate-scope derivation SQL (8 per-`resource_type` branches resolving a mandate's audit rows) is covered ONLY by a mocked-repo unit test (asserts the service forwards `mandateId`), `/review` structural verification (all 8 branches, casts, no-double-count, injection-safe, plus the H1 honesty fix documenting the deliberate gate-evaluate exclusion), and live proof that the read/verify/export MACHINERY works — but NO real-DB test seeds multi-producer audit rows for a mandate and asserts the scoped export captures ALL derivable producers AND correctly excludes gate-evaluate. The mocked-repo-only coverage of the load-bearing SQL is, strictly, the over-mocking anti-pattern (assertion proves the mock received the arg, not that the query returns the right rows), and this is a REGULATOR-FACING package where a wrong derivation silently drops/duplicates rows — high consequence when the filtered view is used.

I weighed this as a potential REWORK and concluded MEDIUM carry-forward, not a hard block, for five reasons: (a) all four CARDINAL compliance invariants are tested LIVE — DEV-2 is a completeness gap on a filtered VIEW, not a bypassed or untested invariant; (b) the PRIMARY regulator export — the full-chain export with no mandate filter — captures all producers and is proven live (entryCount:309, all shipped producers), so the untested path is the narrower optional scoped-filter, not the canonical package; (c) `/review` structurally verified all 8 branches including no-double-count and injection-safety, and the joins are simple `mandate_id` FK lookups (low logic-complexity); (d) the H1 honesty fix means the package does NOT overclaim — the gate-evaluate exclusion is documented as a known limitation, so the failure mode "regulator misled about completeness" is mitigated; (e) B-6 itself classified DEV-2 as MEDIUM accepted-debt and the T-block→V-2 findings contract makes finding-blocking classification V-2's decision, not the T-gate's. **Directive to V-2 (strong):** author a recordkeeping mandate-derivation real-DB integration test (`T-4` layer — reuse the wave-12 race-safe shared migrate helper) that seeds multi-producer audit rows (outreach + pipeline + mandate events) under one mandate, then asserts the mandate-scoped export INCLUDES all mandate-derivable producers and EXCLUDES gate-evaluate (per the documented H1 limitation), BEFORE the scoped-export view is relied upon for an actual regulator request. This is a fillable, well-scoped coverage gap (hence not ESCALATE) and should not ship as permanently-mocked coverage on a compliance-correctness path.

### Standing observation (not a wave-13 blocker)

No Stryker/mutation score is reported for the recordkeeping suite. For this vertical the LIVE probes (real chain verify, real 309→310 append delta, real 403/400 status codes, real DOM-content greps) supply the assertion-strength evidence mutation testing would otherwise provide, so I do not block on its absence — but a mutation gate on the recordkeeping domain modules would harden the mocked-repo unit layer flagged in DEV-2 and is recommended as future test-infrastructure work.

## Rework instructions  (only if REWORK)
n/a — APPROVED.

## Escalation  (only if ESCALATE)
n/a — the Playwright-binary infra trigger did not fire (real non-zero live verification, no zero-executed-test Playwright run); DEV-2 is a fillable coverage gap routed to V-2, not a structural gap exceeding the wave's risk budget.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 2
- phase1_head_tester_verdict: APPROVED
- carried_to_v2: [DEV-2 (MEDIUM — mandate-derivation real-DB e2e; over-mocking on load-bearing compliance-correctness SQL), test-cred-registry (LOW carry-forward w11/12), producer-side-gate-mandate-attribution (INFO follow-on)]
- cardinal_invariants_live_verified: [hmac-chain-verify, export-one-audit-last-in-txn, sod-rbac-advisor-403, read-only-zero-audit + M2-validation, ac-strip-dom-content]
- playwright_binary_escalate: NOT_FIRED (no zero-executed-test Playwright run; T-5/T-6 satisfied by real live HTTP+DOM-content assertions)
