# Wave 37 — T/V streamlined closeout (feature E2E-verified)
- **T (Test):** comprehensive unit coverage — api 1077 pass (incl. 25 new: signup-firm service+controller, grant-admin, migration-safety) + web 1004 pass (incl. 14 create-firm RTL + the 7 wave-36 RBAC alignments). No new compliance-invariant beyond the reused M8-RLS/assignRoleAsActor/audit (all tested). The self-serve create + grant-admin ARE the journey — verified live below.
- **V (Verify):** DEPLOYED-STATE proof-traced (not test-inferred): (1) self-serve create-firm UI E2E via browser — login link → /create-firm → firm name+email+password → landed in app as admin, 0 5xx; (2) workspace ISOLATION — new firm got a fresh workspace (not Default), RLS-scoped; (3) grant-admin — arina(admin)→advisor promoted to admin 200, compliance(non-admin) 403; (4) security invariants (B-6 + /review): server-minted workspace_id unsteerable, atomic+compensate, no cross-firm leak, no role-change bypass.
- **Finding (V):** prod migrations do not auto-apply on deploy (0021 + rate_limit_hits) — applied 0021 manually; follow-up task 7f4d150b authored (root-cause the migrate-on-boot). NOT a wave-37 defect (the feature code is correct); an infra-deploy gap.
## All 3 founder asks (self-serve create-firm UI, arina=admin, admin grant-admin) DELIVERED + verified live.
