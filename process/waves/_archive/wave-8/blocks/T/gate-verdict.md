# Wave 8 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-w8-t9)
**Reviewed against:** process/waves/wave-8/blocks/T/review-artifacts.md + findings-aggregate.md
**Attempt:** 1  (1 = first gate)

## Verdict
APPROVED

## Rationale
The M4 first-bundle metric — an advisor creates a sell-side mandate with buyer criteria and a compliance profile — is genuinely delivered and PROVEN live, not merely asserted. The C-2 FINAL re-verify (46642e7) exercised the create-via-UI payoff in a real headless chromium against the post-hydration DOM and URL bar (not HTTP status): full form submit → POST /mandates-data 201 → URL redirects to /mandates/e3dbadef-… → detail page renders the created mandate (seller name, jurisdiction US, server-derived disclaimer fe1c504d, draft status, 3 deferred placeholders) → list shows exactly one row (retry-duplication path closed by the redirect firing). This clears the layout-only-false-PASS and CI-blindness anti-patterns: the assertions read exact DOM data and the URL, not container visibility.

Compliance-invariant coverage is adequate and non-theatrical across the layers. Every load-bearing invariant is tested at unit/integration AND live-confirmed: one-txn 3-table atomicity with audit-last-in-txn rollback; actor-id = app users.id via getUserWithRole (not the raw SuperTokens id — the wave-5 FK lesson, regression-tested); disclaimer derive server-side with valid→FK / no-match→400 (not a null-FK 500) / ambiguous→409 backed by the 0007 partial unique index (one active template per jurisdiction, verified applied with 0 dedup needed); the 3 acknowledgments required (400 otherwise, persisted and audited); active-mandate lock (draft→active 200, edit-while-active 409, DB-confirmed sellerName unchanged — enforced, not cosmetic); and the RBAC matrix (advisor/admin create, analyst read-only 200 GET + 403 POST, anon 401). The HMAC-SHA256 audit chain was live-verified (GET /compliance/audit-log/verify entriesChecked 57→58 with chain ok:true), satisfying the untested-compliance-invariant guard with an actual cryptographic-continuity check, not UI clicking.

The W8-2 hardening is sound and source-verified (not taken on report). The independent service-level guard uses strict identity `!== true` on all three acknowledgment booleans (mandate.service.ts:104-106), with an in-code comment documenting that this rejects the string "true" and number 1 that a truthy `!value` check would have let through — exactly the Zod-bypassing defense-in-depth gap the T-block found. Bypass-shape regression tests exist at BOTH boundaries as independent describe blocks (schema-boundary: false-ack, omitted-ack, absent-object, "true"-string-no-coercion, 1-number-no-coercion; service-boundary: false/undefined/absent/string-"true" each asserting BadRequestException). This is the real-boundary discipline — the guards are tested where they live, not through a mock that only proves invocation. Treating the T-5 screenshot as a timing artifact is correct: the normal API path was always schema-guarded by z.literal(true), C-2 live-verified acks→400, and the service guard is legitimate belt-and-suspenders on the independent boundary, not the primary enforcement.

The C-2 fix-cycles are genuinely closed with no Done-Theater. Each of the four surfaced defects (detail-route /mandates/:id afterFiles collision, jurisdiction-form/seed mismatch, advisor-readable jurisdictions endpoint, flat-Mandate create-response parse) was root-caused by a named specialist and independently re-verified live in the DOM on the fixed deploy — head-ci-cd rejected two prior C-2 attempts (c278f7d, 7b33598) rather than fabricating a green on a passing API while the user-facing vertical was dead, then confirmed the payoff on 46642e7. No silently-skipped invariant or false-green: the Playwright Chrome binary was genuinely present (chromium-1208 via the PLAYWRIGHT_BROWSERS_PATH compatibility shim; playwright.config.ts explicitly sets NO channel:'chrome' because host Chrome is absent — the correct handling, not the missing-binary false-PASS trap), 63/64 tests executed (non-zero, the single failure a documented pre-wave sourcing data-dependency, not introduced by this wave). T-7 skip is legitimate (mandate CRUD with indexed queries, 1:1 compliance reads, no realtime/heavy surface, pilot scale). The three findings are correctly dispositioned: W8-2 hardened both layers, W8-3 fixed (userRole prop + canCreate conditional in MandateListClient, source-confirmed), W8-4 a genuine low cosmetic carry-forward (TopBar title). It is acceptable that the T-block acks-hardening ships on the next deploy: the live-proven enforcement path (the Zod schema) already rejects on the normal API route and C-2 live-verified acks→400; the pending service guard is a redundant independent-boundary defense, so no compliance invariant is unenforced on the current live deploy. This residual (hardening commits a061c57 + 1312fb4 on main @ 1312fb4, CI green, awaiting deploy) is correctly a V-block / next-deploy concern and does not gate T-9.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
