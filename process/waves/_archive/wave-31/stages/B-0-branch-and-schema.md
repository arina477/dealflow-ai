# Wave 31 — B-0 Branch & schema
Branch wave-31-twenty-adapter. 1 task (1eb63a40). Schema SKIP (no migration; config schema UNTOUCHED). New secrets TWENTY_API_KEY + TWENTY_BASE_URL (env, requested from founder — NOT committed). D-SKIP (no UI).
```yaml
branch: wave-31-twenty-adapter
schema_skipped: true
migrations: []
new_secret_env: [TWENTY_API_KEY, TWENTY_BASE_URL] (requested; not committed)
config_schema_change: false
```
