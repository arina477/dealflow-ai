# Wave 31 — P-3 Plan (M9 Twenty CRM DataSourceAdapter) — mirrors wave-30 Affinity
## Approach — SDK-research-first, then the adapter (mirror affinity.adapter.ts)
### Action 1 — Deliverables
1. **[SDK-research-FIRST] command-center/dev/SDK-Docs/Twenty/twenty.md** (Twenty OPEN-SOURCE — full docs at twenty.com/developers + GitHub): base URL (per-instance — twenty.com cloud OR self-hosted, resolved from TWENTY_BASE_URL env), API version (REST — pick REST over GraphQL for a read adapter, document why: simpler, no query-shape mgmt), **auth** (API key as Bearer token: `Authorization: Bearer <key>`), **endpoints** (GET /rest/companies [list + paginate], GET /rest/people; the company/person fields), **pagination** (Twenty uses cursor pagination — `starting_after`/`ending_before` + `limit`, or the `pageInfo` cursor; document the exact model + termination), **rate limits** (+ 429 handling), **error shapes**, the **normalize map** (Twenty company {id, name, domainName} + people → NormalizedSourceRecord {sourceRecordId=twenty company id, name, domain, contacts[{name,email,title}]}). + registry.md row. Research from Twenty's PUBLIC docs (WebFetch twenty.com/developers or the REST API docs; Twenty is open-source so the docs are complete; mark any assumption "to verify at live-hookup").
2. **The Twenty adapter** (apps/api/src/modules/sourcing/adapters/twenty.adapter.ts, providerKey='TWENTY', registered) — MIRROR affinity.adapter.ts: fetchCompanies(connection): read TWENTY_API_KEY + TWENTY_BASE_URL from env; **graceful no-op if EITHER absent** (return [] + log; app boots, fixture/Affinity search works); **https-validate TWENTY_BASE_URL** (scheme=https + valid URL — SSRF/misconfig guard); INTERNAL cursor pagination (all pages) + 429/5xx backoff + retry (bounded) + timeout (AbortController); boundary-Zod each Twenty response; NORMALIZE Twenty company (+ associated people) → NormalizedSourceRecord; **[P2-a FOLD] safeParse the OUTPUT against normalizedSourceRecordSchema (skip+log invalid — match fixture.adapter.ts)**; register 'TWENTY' in createDefaultRegistry.
3. **Secret/config:** TWENTY_API_KEY + TWENTY_BASE_URL via env ONLY (NEVER committed; requested from founder). Config schema (dataSourceConnectionConfigSchema) UNTOUCHED (wave-16 secret-sink boundary). .env.example: both name-only.
4. **Tests:** mirror affinity.adapter.spec — MOCKED-HTTP (vi.stubGlobal fetch): multi-page cursor pagination (all pages), 429→backoff→retry, 5xx-retry, timeout, normalize, absent-key-noop + absent-URL-noop, boundary-Zod (malformed input→skip), **OUTPUT-validation (a normalized record failing normalizedSourceRecordSchema → skipped+logged)**, base-URL-https-validation (non-https/malformed → no-op+log). NO live HTTP.
### Action 2 — Data model: NONE (reuses sourcing model + ETL). No migration.
### Action 3 — API: none new (feeds the existing sourcing search via registry).
### Action 4 — Deps: native fetch + the existing test HTTP-mock (from wave-30). Secret: TWENTY_API_KEY + TWENTY_BASE_URL (env).
## Plan (by B-stage)
**B-0 Schema:** SKIP. **B-1:** reuse the interface + shape; Twenty response Zod types (internal). **B-2** (backend-developer, external-sdk-integration-rules): the Twenty SDK doc FIRST → then twenty.adapter.ts (mirror affinity.adapter.ts + base-URL-from-env + P2-a output-validation) + registry + graceful-no-key/URL + mocked tests. **B-3:** SKIP (no UI). **B-4/B-5/B-6:** head-builder polices SDK-doc-first + mirror-wave-30 + base-URL-from-env-https-validated (config untouched) + output-validation (P2-a) + robustness-tested + secret-env-never-committed + graceful-no-key/URL.
### Action 6 — Specialist: backend-developer. Serial. Action 8 — Self-consistency CLEAN. C-2 live-verify key-gated.
```yaml
deps_new: []
schema_change: false
new_secret: true   # TWENTY_API_KEY + TWENTY_BASE_URL (env — requested from founder, NEVER committed)
new_sdk: twenty (REST, open-source — SDK doc authored)
specialists: [backend-developer]
compliance_invariants: [sdk-doc-first, mirror-wave-30-affinity-adapter, base-URL-from-env-https-validated (config schema UNTOUCHED - wave-16 boundary), reuse-DataSourceAdapter-interface, robust-pagination+backoff+retry+timeout+boundary-Zod, OUTPUT-self-validation-vs-normalizedSourceRecordSchema (P2-a fold), secret-env-never-committed, graceful-no-op-if-key-or-URL-absent, mocked-http-tests]
hard_boundaries: "the Twenty DataSourceAdapter behind the EXISTING interface (mirror affinity.adapter.ts) + Twenty SDK doc (research-first) + base-URL+key from ENV (TWENTY_BASE_URL https-validated + TWENTY_API_KEY; config schema UNTOUCHED - wave-16 boundary) + INTERNAL pagination/backoff/retry/timeout/boundary-Zod + OUTPUT-self-validation (P2-a) + normalize + register TWENTY + graceful-no-op if key/URL absent + mocked tests. MVP core companies/people; opportunities/custom-objects/write-back/webhooks/incremental-sync/multi-instance-per-firm-config-URL = LATER. NO migration/UI/config-schema-change. LIVE-verify key-gated (founder Twenty key + instance URL)."
design_gap_flag: false
self_consistency: clean
```
