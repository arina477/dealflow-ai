# Wave 31 — B-1/B-2 (Twenty CRM DataSourceAdapter)
Commit ad16247. SDK-research-FIRST (from Twenty OPEN-SOURCE server code) + mirror wave-30 + P2-a fold:
- B-1 SDK doc command-center/dev/SDK-Docs/Twenty/twenty.md: Bearer auth (Authorization: Bearer key), base URL from TWENTY_BASE_URL env (per-instance, never hardcoded), GET /rest/companies?depth=2 (inline persons), cursor pagination (starting_after + pageInfo.hasNextPage/endCursor, limit default 60/max 200), normalize map (company.id/name/domainName.primaryLinkUrl + inline people). Registry row.
- B-2 twenty.adapter.ts (providerKey=TWENTY, registered createDefaultRegistry): env TWENTY_API_KEY + TWENTY_BASE_URL lazy-read + graceful no-op if EITHER absent (app boots); HTTPS guard (new URL + protocol=https: else no-op+warn); INTERNAL cursor-pagination (all pages); 429/5xx backoff+retry (MAX_RETRIES 3, exp from 1s); timeout (AbortController 30s, clearTimeout finally); boundary-Zod inbound (malformed->log+partial); [P2-a FOLD] OUTPUT normalizedSourceRecordSchema.safeParse (invalid->skip+warn — closes wave-30 gap, mirrors fixture.adapter:85); depth=2 contacts (no per-company person calls). Config schema UNTOUCHED.
- Tests (18 mocked vi.stubGlobal, NO live): multi-page-cursor-pagination(3->6), 429-backoff, 5xx-retry, timeout, normalize, absent-key-noop, absent-URL-noop, non-https-noop, malformed-URL-noop, boundary-Zod, [P2-a] output-validation, auth-Bearer, partial-failure, domain-extraction. api 1048 pass.
- typecheck 4/4, lint exit 0, NO key/URL committed, NO config-schema/migration. Deviations (8, all documented): Bearer(vs Basic), base-URL-env, cursor-pagination, depth=2, domain-composite, P2-a-output-validation, jobTitle-inline, TWENTY_BASE_URL(vs Twenty canonical TWENTY_API_URL — both doc'd).
- LIVE-verify deferred C-2 (founder Twenty key + instance URL; adapter key-gated, graceful [] without).
new_files: SDK-Docs/Twenty/twenty.md, twenty.adapter.ts, twenty.adapter.spec.ts
