# Wave 31 — T-block review artifacts
**Wave topic:** M9 Twenty CRM DataSourceAdapter (dormant, key-gated live path). | **Block exit gate:** T-9 | **Status:** gate-passed (T-9 APPROVED — coverage genuine, key-gated E2E deferral legitimate)
## Layer dispositions (adapter-only backend wave — mirrors wave-30 Affinity T-block):
- **T-1 Unit:** DONE — 18 mocked-HTTP tests (twenty.adapter.spec.ts): multi-page cursor-pagination(all), 429-backoff, 5xx-retry, timeout, normalize, absent-key-noop, absent-URL-noop, non-https-noop, malformed-URL-noop, boundary-Zod, [P2-a] output-validation, auth-Bearer, partial-failure, domain-extraction. api 1048 pass. GENUINE (not tautological — /review confirmed the multi-page + output-validation tests exercise real behavior).
- **T-2 Integration / T-3 Contract:** N/A — no new API endpoint (the adapter feeds the EXISTING sourcing search via the registry interface — its contract [DataSourceAdapter] is unchanged + unit-covered); no new DB (config schema untouched, 0 migrations).
- **T-4 E2E / T-5 Journey-map:** DEFERRED (key-gated) — the LIVE Twenty fetch → sourcing-search E2E is NOT runnable without TWENTY_API_KEY + TWENTY_BASE_URL (founder-gated). The dormant path (adapter registered, returns [] , app boots, fixture/Affinity search works) is C-2-verified (boot-clean). Same disposition as wave-30 Affinity (a legitimate key-gated deferral, NOT a coverage gap). No new user-facing journey (no UI change).
- **T-6 Layout / T-7 Perf:** N/A — no UI; no perf-sensitive path (the adapter is an off-request ETL-style fetch, internally bounded [pagination cap-by-cursor, timeout, retry-bounded]).
- **T-8 Security:** SATISFIED — secret handling (TWENTY_API_KEY + TWENTY_BASE_URL env-only, NEVER committed — grep-clean; graceful-no-key/URL), SSRF guard (https base-URL validation), config-schema-untouched (wave-16 secret-sink boundary respected). Already adversarially verified at B-6 /review (no P0/P1/P2; secret-leak CLEAN, SSRF-guard PASS, config-untouched confirmed).
## T-9 Journey: head-tester gate (coverage adequacy for a dormant key-gated adapter + confirm the key-gated E2E deferral is legitimate).
## LIVE Twenty E2E = founder-gated (TWENTY_API_KEY + TWENTY_BASE_URL). The dormant deploy is verified; the live path defers to the live-hookup follow-up.
