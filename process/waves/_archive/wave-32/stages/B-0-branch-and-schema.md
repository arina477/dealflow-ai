# Wave 32 — B-0 Branch & schema
Branch wave-32-selfhost-twenty. 1 task. Schema SKIP (NO DealFlow migration — Twenty self-hosts its own DB). New secrets (all env, orchestrator-generated, NEVER committed): APP_SECRET + Twenty-PG + Redis + S3 + TWENTY_API_KEY + TWENTY_BASE_URL. D-SKIP (no DealFlow UI). INFRA wave → deployment-engineer.
