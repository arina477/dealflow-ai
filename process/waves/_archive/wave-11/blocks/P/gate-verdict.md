# Wave 11 — P-4 Verdict

**Reviewer:** head-product (fresh spawn, agentId head-product-w11-p4)
**Reviewed against:** process/waves/wave-11/blocks/P/review-artifacts.md
**Attempt:** 1  (1 = first gate, 2+ = post-rework)
**Phase:** 1 (head-product)

## Verdict
APPROVED

## Rationale
This is the most compliance-critical bundle since M2, and the spec earns the gate on every load-bearing axis. The three moat invariants are each specified as binary, observable, server-side contracts — not aspirations. Version-binding is asserted in the SERVICE via an `isUsableForSend(version)` predicate (approved AND approved_content_hash == current content_hash), mirroring the verified-live M2 disclaimer approval-version binding: editing an approved template mints a NEW pending version and leaves the approval bound to the old hash, so an edited-but-unapproved template is provably not send-eligible — a real compliance breach the design closes, checkable at the pre-send gate. The non-bypassable pre-send gate correctly REUSES the established M2 `ComplianceGateService.evaluate` (the sole send-eligibility authority) EXTENDED with the version-binding + outreach-SoD checks, and explicitly REJECTS forking a second gate as a bypass risk — I verified the service exists, is tx-scoped, audits in-txn, is fail-closed, and its own header comment already names the M6 outreach send path as its intended caller, so the reuse is the pre-established architecture, not a wave-11 invention. SoD (sender != the approver of the template version) is server-side (composer != approved_by → 403, audited), and grant/reject is compliance-role-only with an explicit negative test (non-compliance role → 403). Both hard boundaries are honest: NO LLM/AI-drafting (deferred to a later M6 bundle behind M5's blocked founder-spend gate; drafting here is manual/structured) and NO transactional-email SDK / send-path (this slice produces a send-ELIGIBLE gate-passed record, not an actual send) — `new_sdk: false`, `new_secret: false` confirm no scope smuggle. Reuse discipline holds: required-compliance-block FK → M2 disclaimer_templates (no new disclaimer store), audit → AuditService, content_hash → the M2 keyless SHA-256, additive migration 0010 journal-registered when > 0009's 1783555200000 (BUILD rule 4) — every reuse anchor verified present in the codebase. Scope is right-sized (multi-spec, ~3,500-4,500 LOC, under the 5,000 split ceiling and above the 2,500 multi-spec floor), all three P-0 reframers aligned PROCEED, mvp-thinner found nothing peelable under the moat rule, and the wave-4..10 lessons (actor-id, DrizzleError-unwrap, journal-when, tx-scoped-reads rule 7, read-passthrough rule 5, SSR-hydrate, page-route-collision-avoidance, one-txn + audit-last-in-txn) are embedded in the P-3 self-consistency sweep. The foundation-first sequencing (versioned approved template → composer → non-bypassable gate → SoD, send deferred) is the correct compliance-first slice for the T1 outreach wedge. One cosmetic nit for Phase 2: a P-3 alt-path reference cites `compliance-gate/evaluators/content-hash.ts` whereas the file lives at module root as `content-hash.ts` (the evaluator is `evaluators/content-hash.evaluator.ts`) — both exist and B-block resolves via VALUE imports, so this is a path-precision note for Karen's claim-check, not a Phase-1 defect. Every P-4 stage-exit checkbox ticks; no compliance, audit-log, version-binding, or RBAC/SoD acceptance criterion is left un-checkable.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 2

---
## Phase 2 — Karen + jenny + Gemini (SECURITY-SCOPE-TIGHTENED: version-binding + non-bypassable gate + SoD + approval + audit → ≥2 Phase-2 iterations)
**Karen:** APPROVE — all 9 load-bearing claims VERIFIED (M2 ComplianceGateService.evaluate:83 = real non-bypassable authority, extend-not-fork feasible; disclaimerTemplates:213; content-hash/sod/disclaimer/suppression evaluators; matchCandidates shortlist:222; AuditService.append; getUserWithRole:154 + AuthModule exports; migration 0010 slot; NO Anthropic/email SDK in package.json; rules 4/5/7 real). 2 BUILD notes (enum-name-distinct outreach_approval_status; SoD approved_by-mirror) + 1 T/V-CRITICAL (gate must be CALLED by compose, not merely callable — the M2 header's own M6 dependency) — all carried to spec.
**jenny iter-1:** APPROVE — honest faithful tightly-scoped compliance foundation; all 3 moat invariants MATCH (version-binding [mirrors M2 decision #4 + content-hash.evaluator], non-bypassable gate REUSES M2 [not fork], SoD [mirrors sod.evaluator sender≠approver+compliance-only]); boundaries honest (no-send/no-LLM share real deferred gates); M6 NOT claimed complete; reuse genuine. 1 Low hardening (composer send-affordance + AI-draft STRIP — plan-only, needs spec-pinning per the wave-10 provenance precedent) + 2 T/V notes.
**Hardening (folded):** AC-STRIP (B-3 strips the design Send/Schedule-Send buttons + WORM-on-send + AI-Drafting modals → surfaces gate-verdict/send-eligible; B-6 greps composer/templates .tsx; a live send/AI-draft CTA = REWORK) + the T/V gate-called structural test + the enum-name.
**jenny iter-2:** APPROVE — all 3 RESOLVED on the enforceable surface; no residual; compliance-foundation fully honest (5 invariants enforceable).
**Gemini:** UNAVAILABLE (HTTP 429 — degraded, non-blocking).

## Phase 2 Verdict: PASS (Karen APPROVE + jenny APPROVE iter-2 + Gemini UNAVAILABLE; 2 Phase-2 iterations on the compliance-critical wave).
**P-block gate: PASSED.** design_gap_flag=false → next block B.
### B-block notes carried:
1. **B-0 (migration 0010, journal when > 0009):** outreach_templates + outreach_template_versions (version_number monotonic, content_hash, **outreach_approval_status enum [distinct name — karen]**, approved_content_hash, approved_by, disclaimer_template_id FK→M2 disclaimer_templates) + outreach (template_version_id FK, gate_verdict, status compose|send_eligible|blocked). .down.sql.
2. **B-2:** TemplateService (create/draftNewVersion[new version + content_hash]/requestApproval[required-block]) + **isUsableForSend version-binding** + grant/reject (compliance-role SoD, sets approved_content_hash + approved_by); **OutreachService.compose ALWAYS CALLS the M2 ComplianceGateService.evaluate (extended: isUsableForSend + outreach-SoD [composer != approved_by] + M2 rules) → send_eligible|blocked, fail-closed, audited-in-txn** (T/V proves compose has NO path to send_eligible without evaluate). one-txn + actor-id + tx-scoped-reads (rule 7) + DrizzleError-unwrap.
3. **B-3 (MANDATORY AC-STRIP):** templates-library + outreach-composer pages SSR-hydrated; STRIP the design Send-Immediate/Schedule-Send buttons + WORM-on-send copy + AI-Drafting modals → surface gate-verdict + send-eligible (NO false send/AI capability — CODE-OF-CONDUCT); /outreach-data + /outreach-templates-data proxies; apiFetch rid; read-passthrough. + a minimal compliance-queue grant/reject view.
4. RBAC advisor/analyst-draft + compliance-approve + advisor-compose (+ SoD sender≠approver) + NAV; nav⊆RBAC. Reuse M2 ComplianceGateService/evaluators/disclaimer/audit/content-hash + M1 + M5-shortlist. No new dep/SDK/secret.
