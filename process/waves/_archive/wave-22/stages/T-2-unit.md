# Wave 22 — T-2 unit/integration
outreach-activity-rls.e2e ran 9/9 (not skipped) with the SCOPED audit assertions (WHERE workspace_id=$1) on postgres:18 as dealflow_app — the flake-fix verification. Stable (green first try) + fault-killing (exact +1 / exact verb).
```yaml
test_pattern: ci-verified
findings: []
```
