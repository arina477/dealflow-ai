# Wave 10 — P-4 Verdict

**Reviewer:** head-product (fresh spawn, agentId head-product-w10-p4-phase1)
**Reviewed against:** process/waves/wave-10/blocks/P/review-artifacts.md
**Attempt:** 1  (1 = first gate, 2+ = post-rework)
**Phase:** 1 (head-product)

## Verdict
APPROVED

## Rationale
The deterministic match spine bundle (M5's first, load-bearing slice) passes the P-4 gate. Every acceptance criterion in the multi-spec contract (seed 47ed7ddd, siblings fb82d339 + f74dce45) is falsifiable and describes observable external behavior — persisted match_run + ranked match_candidates with integer fit_score 0-100 and score_breakdown jsonb, the submit-guard (400 when the buyer universe is not submitted), idempotency (one run per universe via a buyer_universe_id UNIQUE), the ranked page, accept/reject/flag → shortlist, the ready-for-outreach handoff status, audited-last-in-txn mutations, and actor = app users.id — not internal algorithm prose. Both load-bearing boundaries are drawn cleanly and are honestly held in spec AND plan: (1) deterministic-vs-LLM — the spec explicitly bans Anthropic/Claude/LLM/BullMQ/rationale-text/spend, fit_score is a pure rule-based integer, and the LLM ranking+rationale is correctly deferred to a later BOARD-gated bundle (SDK-rules + LLM gateway + zero-retention DPA + load-test + Tier-3 spend); no AC leaks LLM/SDK/spend scope, and the /matches-shortlist UI is required to frame the score as "rule-based fit", NOT an AI-generated rationale; (2) M5/M6 — the bundle stops at persisting a ready-for-outreach shortlist status that M6 reads and does NOT perform outreach. I verified the load-bearing reuse claims directly against the branch: buyer_universe/buyer_universe_candidates carry NO score/rank column (the M4/M5 boundary holds; scoring correctly lives on the new match_candidates), the companies table exposes only name/domain/sector/contacts (geo/size/deal_type genuinely absent — confirming the thin-data premise), mandate_buyer_criteria carries all four dims (sector/geo/sizeBand/dealType) so only 2 of 4 are backable by company data, the migration journal ends at 0008 (when=1783468800000) so migration 0009 must register when > that per BUILD rule 4, and AuditService/getUserWithRole/RolesGuard all exist for reuse with no pre-existing LLM/BullMQ dependency on the branch. The problem-framer's key concern — meaningful discrimination on thin M3 data — is not left as aspiration: the contract makes weights + tie-breaks a required, machine-checkable property of a pure unit-testable scoreCandidate function (graded contact-completeness + exact-sector-match base + deterministic tie-break so ties don't collapse), with unsupported dims scored-where-present plus score_breakdown provenance rather than crashing or producing a uniform ranking. Compliance-adjacent discipline is intact: RBAC advisor-primary with analyst read-only and explicit anon-401/unauthorized-403 negative constraints, every mutation audited last-in-txn with rollback-on-audit-fail, cross-run-scoped PATCH (404 on match_run_id mismatch), and the accepted-count semantic-predicate guard on handoff (BUILD rule 6). Schema is additive-only, and wave-5..9 lessons (actor-id, DrizzleError-unwrap, journal-when, idempotent-container, semantic-predicate-guard, read-schema-passthrough, SSR-hydrate, page-route-collision-avoidance) are embedded in the plan. Every claimed_task_id traces back to the P-0 bet; no scope smuggle, no orphaned edge case, no implementation leakage rising to a rework.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 2

---
## Phase 2 — Karen + jenny + Gemini
**Karen:** APPROVE (with 1 MANDATORY B-3 condition) — all reuse/schema VERIFIED (M4 buyer_universe no-score-column + 'submitted' status; M3 companies.sector-ONLY [no geo/size/deal] + contacts; mandateBuyerCriteria dims; AuditService.append; getUserWithRole + AuthRepository export; migration 0009 slot [when > 1783468800000]; wave-5..9 lessons; deterministic backend CLEAN — no Anthropic/BullMQ in package.json, no rationale column). **MANDATORY: design/matches-shortlist.html carries FALSE AI-framing (:619 AI-Match-Analysis, :607/:687/:702 Explainability-Engine/rationale-generated, :679 fabricated cross-client) — B-3 MUST strip + reframe as rule-based; head-builder B-6 greps the .tsx.** (carried to spec)
**jenny:** APPROVE — deterministic-HALF honest (LLM/rationale correctly deferred to the gated bundle, never claimed done/present); M5/M6 boundary held (handoff not outreach); reuse genuine (match_candidates FK→M4, score inputs M3/M4, scorer weights/tie-breaks specified for discrimination); the karen AI-strip is the correct CODE-OF-CONDUCT-compliant disposition + embedded as enforceable B-3 condition. 3 non-blocking flags: route-anchor (D6 link at B-3/T-9), scorer-discrimination (verify at V via unit tests), literal-grep (B-6 add a semantic eyeball pass for paraphrased AI-capability language).
**Gemini:** UNAVAILABLE (HTTP 429 — degraded, non-blocking).

## Phase 2 Verdict: PASS (Karen APPROVE + jenny APPROVE + Gemini UNAVAILABLE).
**P-block gate: PASSED.** design_gap_flag=false → next block B.
### B-block notes carried:
1. **B-3 MANDATORY (karen):** STRIP the AI-framing from design/matches-shortlist.html (:607/:619/:679/:687/:702) — reframe as "rule-based fit score"/"score breakdown"; NO AI-generated-rationale affordance, NO fabricated cross-client signal. **B-6 GREP** `grep -i "AI Match|rationale is generated|explainability engine|improve model|similar mandates"` .tsx → ZERO + a SEMANTIC eyeball pass (jenny flag 3) for paraphrased AI/model/generated capability language.
2. Schema (B-0, migration 0009, journal when > 0008): match_run (mandate_id FK + buyer_universe_id FK UNIQUE [idempotent, wave-9] + ready_for_outreach bool) + match_candidates (buyer_universe_candidate_id FK, fit_score int CHECK 0-100, score_breakdown jsonb, disposition enum). .down.sql per 0000-0006.
3. MatchingService (B-2): PURE deterministic scorer (weights/tie-breaks for discrimination on sector+contact-completeness; unsupported dims graceful+provenance; unit-tested, NO LLM/randomness) + one-txn create-run/disposition/handoff + audit-last-in-txn + actor-id + submit-guard (400 if universe not submitted) + accepted-count handoff-guard (BUILD rule 6) + cross-run-scoped PATCH + DrizzleError-unwrap. NO Anthropic/BullMQ.
4. Web (B-3): /matches-shortlist SSR-hydrated (ranked list + fit_score + score_breakdown RULE-BASED-framed + accept/reject/flag + handoff); /matches-data non-page-colliding proxy; apiFetch rid; read passthrough. Confirm the mandate-detail D6 "Ranked Candidates" anchor links here (jenny flag 1).
5. RBAC advisor-primary (+analyst read) + NAV_MATCHES. V-block: verify scorer discriminates on real fixture data (jenny flag 2).
