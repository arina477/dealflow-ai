# Wave 4 — B-6 Verdict

**Reviewer:** head-builder (fresh spawn, agentId head-builder-w4-b6-p1)
**Reviewed against:** process/waves/wave-4/blocks/B/review-artifacts.md
**Attempt:** 1  (1 = first gate, 2+ = post-rework)

## Verdict
APPROVED

## Rationale

This is the M2 compliance backbone — the load-bearing tamper-evident audit log — and every compliance invariant was verified in CODE, not accepted on claim. **(1) DB-layer immutability:** migration 0002 emits both controls — `REVOKE UPDATE, DELETE, TRUNCATE` + `GRANT INSERT, SELECT` for the app role (Control 1) AND a `BEFORE UPDATE OR DELETE` trigger (`audit_log_block_mutation`, `SECURITY DEFINER`) that unconditionally `RAISE EXCEPTION`s for every connecting role including owner/superuser (Control 2), with honest inline comments that the grant is a no-op against an owner and the trigger is the load-bearing control for that case; the down-migration is additive-reversible (drops only trigger→function→table, existing tables untouched); `sequence_number` is `GENERATED ALWAYS AS IDENTITY` so the app cannot supply it. **(2) Tamper-evidence:** the HMAC-SHA256 keyed chain uses a length-delimited, version-gated canonical serialization with an explicit `\x00null` sentinel and `prev_hash` appended last; the golden-vector test pins a hardcoded HMAC (`228e6ee1…`) which I independently recomputed from first principles and it matched exactly — a genuine cryptographic pin, not a self-referential rubber-stamp — and it throws on any unsupported `chain_version`, satisfying P-4 item-1 (chain_version pins BOTH key and serialization order). The verifier walks in sequence order checking gap→link→content in that order, selecting the key per each entry's `chain_version` (rotation-safe), asserting the genesis anchor, and the detection matrix test builds a REAL chain then tampers each axis (content_hash, actor_role, prev-link, genesis-link, mid-gap, missing-genesis) asserting the correct `firstBreakAt` + `reason` + count. **(3) Key handling:** the keyring fail-fasts at construction via Zod `min(1)` (missing/empty key crashes module init → API refuses to boot → no unsigned entry is ever writable), reads env only, holds the key in a private `#field`, and the leak test proves `toString`/`JSON`/enumeration never expose it. **(4) Write-atomicity:** `append(input, tx)` requires a caller tx (composes atomically so an audited action cannot commit without its entry); `pg_advisory_xact_lock` is the FIRST statement (serializes concurrent appends, covers the empty-log→genesis race); the predict-then-force sequence (`tail+1` under lock, `OVERRIDING SYSTEM VALUE`) makes stored `sequence_number` == hashed `sequence_number`, and the service test proves the resulting chain re-verifies `ok:true`. **(5) RBAC:** the endpoint resolves `@Roles` from the shared `roleRoutes` single source with a boot-time non-empty fail-closed assertion; the per-role test exercises the REAL guard (compliance/admin→200, advisor/analyst→403, anon→401) including the DB-authoritative stale-claim case (claim=admin, DB=advisor→403). The screen is compliance-only (`assertRole` + `NAV_AUDIT_LOG` compliance-only, admin correctly redirected), a legitimate SoD nuance vs the endpoint's compliance+admin — nav⊆RBAC preserved. **(6) Threat-boundary honesty:** tail-truncation is explicitly NOT claimed detectable (documented accepted boundary with the high-water-mark future path); the service/verifier are honestly labeled standalone-this-wave with real call-sites at M6+ — nothing is falsely claimed audited. **(7) Test discipline:** the 347 tests are real, not hollow — golden vector, full verifier detection matrix, keyring fail-fast+leak, per-role RBAC, and 42-assertion frontend RBAC+persistence tests. The one gap — no live-DB immutability integration test, and migration 0002 not yet applied — is honestly documented as a C-2 deferral (postgres-pro proved the SQL via a rolled-back txn on the same PG 18.4 engine; live-apply + runtime immutability check are correctly routed to C-2 with the app postgres). This is the accepted migration-not-live-applied boundary, transparently labeled, NOT a false-green. The compliance-grade broken-chain UI is `role="alert" aria-live="assertive"`, persistent and non-dismissible per spec. **(8) Commit discipline:** every claimed task_id has ≥1 citing commit and no commit mixes unrelated spec-block files; two commits cite multiple task_ids (`b0eed89` B-1 shared contract → 3 consumers; `54ff1b8` B-2 service+verifier as one cohesive module) which is the recognized shared-contract/cohesive-module carve-out — splitting a single Zod contract file per consumer would be artificial and would violate the single-source-of-truth this wave exists to establish. Recorded as a MEDIUM accepted-note below; it does not gate. The failure mode my role must never let pass — a state mutation without an immutable audit entry, or an unsigned/forgeable chain — is structurally prevented here.

## Accepted notes (non-gating, carry to C-2 / Phase 2)

- **MEDIUM — multi-task commits (accepted, shared-contract carve-out):** `b0eed89` cites `ec1f279d,e6a4cbfe,031d79fc` and `54ff1b8` cites `a8b2b5a2,e6a4cbfe`. Legitimate: B-1 authors the single shared Zod contract consumed by three tasks; B-2 service+verifier are one backend module sharing the hash core. No cross-spec file bleed. Do NOT rebase-split.
- **HARD C-2 GATE (must-verify at deploy):** migration 0002 must be applied to the app postgres (dealflow-postgres) and a runtime immutability check (app-role UPDATE/DELETE rejected + privileged trigger RAISE) plus a live append+verify must pass before ship. `AUDIT_LOG_HMAC_KEY` (+ `_VERSION`) must be set in Railway env (boot fail-fast will otherwise refuse to start — this is the desired behavior). head-ci-cd must not pass C-2 on a health-check mirage; require observed migration-applied + live verify `ok:true`.
- **LOW:** `sequence_number` uses Drizzle `mode:'number'` (safe to 2^53) — documented upgrade path to `bigint` mode noted in schema; fine for pilot volumes.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 2
