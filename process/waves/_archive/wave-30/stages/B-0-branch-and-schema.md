# Wave 30 — B-0 Branch & schema
Branch wave-30-affinity-adapter. 1 task (345dfbc6). Schema SKIP (no migration — the adapter feeds the existing sourcing ingestion/search). New secret AFFINITY_API_KEY (env, requested from founder — NOT set/committed here). New dev-dep (HTTP mock) OK. D-SKIP (no UI).
```yaml
branch: wave-30-affinity-adapter
schema_skipped: true
migrations: []
new_secret_env: AFFINITY_API_KEY (requested; not committed)
```
