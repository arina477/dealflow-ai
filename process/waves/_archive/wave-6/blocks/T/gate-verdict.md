# Wave 6 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-w6-t9)
**Reviewed against:** process/waves/wave-6/blocks/T/review-artifacts.md + findings-aggregate.md
**Attempt:** 1  (1 = first gate)

## Verdict
APPROVED

## Rationale
The load-bearing hard part of this data-pipeline wave — the DEDUPE-CORRECTNESS test map — is genuinely tested and LIVE-verified against the deployed `918dbf0` artifact, not coverage theater. C-2 inspected the live Postgres (via a temporary, now-deleted TCP proxy) with concrete row-count assertions rather than truthiness or mock-invocation checks: cross-source `acme.com` collapses to exactly ONE canonical with TWO `company_provenance` rows (distinct raw_company_ids from both sources) and Alice case-normalizes (`alice@acme.com`==`ALICE@ACME.COM`) to ONE contact with TWO `contact_provenance` rows (principle-3 lineage). The worst-case negative invariant — NO false-positive merge — is asserted at two layers: the engine unit test ("Acme Co"≠"Acme Inc"; `normalizeName` does not strip "co"; name-only never auto-merges, only domain agreement) and the live 4-companies=4-distinct-`normalized_domain` (1:1) check. Idempotency is proven by a live re-sync returning `{ingested:0,updated:5}` with every count identical before/after, backstopped by the `companies_normalized_domain_partial_unique`, `dedupe_candidates_raw_matched_pending_unique`, and `company_provenance_company_raw_unique` indexes. The ambiguous→review-queue + audited-atomic-resolve path was legitimately NOT live-exercised because this deterministic fixture yields zero pending candidates (all dups are exact-domain Priority-1 auto-merges) — and critically, head-ci-cd REFUSED to fabricate a synthetic candidate via raw DB writes, correctly preferring engine-layer coverage (T-2 test g candidate-path-idempotent + name+domain-conflict→review + the /review CRITICAL-2 fix) over testing a fake path; this is the right call, not a gap. All four fix-cycles are closed with live-confirmed final state: false-positive-merge (CRITICAL-1, 4 distinct domains no wrong merge), lost-contact-provenance (CRITICAL-2, 2 contact_provenance rows via shared human/auto impl), import-type DI boot (fixed 96179b0, now guarded by the `SourcingModule di-boot` regression test — the correct guard for a tsc-invisible defect class, since `tsc` passes on `import type`), and fixture-asset (fixed 918dbf0, sync now 201, fixture in dist/ at 1290 bytes). The T-8-LITE scope call is correct: wave 6 ingests external-party company/contact data, not user auth/payments/sessions/end-user-PII, so the P-4 security-scope-tightened gate does not trip; the right lite probes ran — secret-handling (provider_key stores the env credential NAME e.g. `GRATA_API_KEY`, NO secret column, schema-asserted + grep-clean; adapter resolves `process.env[key]` at runtime) plus the data-correctness invariants live-verified — with auth/CSRF/RBAC inherited from waves 3/5 and re-confirmed fail-closed live (analyst 200/201, advisor/compliance 403, unauth 401 across all 4 sourcing endpoints). The real-browser E2E is real and non-fabricated: chromium-1208 via the pw-compat shim executed a non-zero test count (6 new + 2 regression guards; full suite 40/40; 9.7s new / 44.0s full — not a zero-execution false-PASS), with behavioral DOM assertions (headings, aria-labels, filter chips, empty-state copy) and real RBAC-deny redirects (advisor→`/`, unauth→`/login`); the empty-state-only limitation is honestly disclosed (prod tables purged post-C-2; connection-create needs DB creds unavailable to the harness) and the populated path is live-proved at C-2 — this is correct handling, not a layout-only false-PASS. The Playwright-binary escalation trigger does NOT fire: the C-2 note about an absent Chrome binary refers to the C-block DEPLOY container, whereas the T-5/T-6 harness demonstrably ran a functional browser with non-zero executed tests and real DOM assertions. T-7 skip is legitimate (indexed dedupe lookups on normalized_domain/name, small pilot-scale fixture, on-demand synchronous ETL, pg_trgm noted for future scale). The sole finding — TopBar title renders "Dashboard" instead of "Companies" (recurring Next.js 15 `x-invoke-path` SSR propagation issue, now on 4 screens) — is correctly non-blocking: it is content-cosmetic, touches no data-integrity or compliance invariant, and is already tracked as polish task d7f716b4 routed to B per Iron Law. Every T-9 stage-exit checkbox is tickable from a concrete artifact traced to the deployed hash, live DB query, or CI run; no invariant is silently skipped and no green is fabricated.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
