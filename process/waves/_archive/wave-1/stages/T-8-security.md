# Wave 1 — T Security

**SKIPPED.** no auth / payments / sessions / cookies / CSRF / rate-limit surface this wave. NOTE: supply-chain security IS covered — pnpm audit --audit-level=high is a required CI check (caught + fixed the multer advisory at C-1).

```yaml
test_pattern: skipped
skipped: true
skip_reason: "no auth / payments / sessions / cookies / CSRF / rate-limit surface this wave. NOTE: supply-chain security IS covered — pnpm audit --audit-level=high is a required CI check (caught + fixed the multer advisory at C-1)."
findings: []
```
