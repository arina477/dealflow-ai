# Wave 13 — B-2-backend
 GET /compliance/audit-log/verify already handled by wave-4 AuditLogController (registered first→priority; both call same verifyChain — no behavioral diff; new one shadowed) — flag for B-6
