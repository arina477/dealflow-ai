# Wave 11 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-w11-t9-attempt1)
**Reviewed against:** process/waves/wave-11/blocks/T/review-artifacts.md + findings-aggregate.md (T-1..T-8), C-1-pr-ci-merge.md, C-2-deploy-and-verify.md, B-6 gate-verdict.md, and independently re-verified against CI run 28740703914 raw logs + apps/api/test/outreach-gate.e2e-spec.ts + .github/workflows/ci.yml + git provenance af5b5d9←8d7ed8b
**Attempt:** 1  (1 = first gate)

## Verdict
APPROVED

## Rationale

The most compliance-critical wave since M2 clears every load-bearing compliance invariant with real, non-hollow tests, and I independently verified the single load-bearing claim rather than trusting the deliverable. **The headline: the send_eligible-reachability proof is GENUINE, not skip-collected.** B-6 Phase-2 caught the C-1 defect (approval writes never bridged to `compliance_approvals` → gate always `blocked` → send_eligible unreachable in prod) — a defect the mocked unit tests missed entirely — and deferred the true end-to-end proof to a CI real-DB e2e conditioned on `TEST_DATABASE_URL`. I confirmed from the **raw CI artifact** (run 28740703914) that this condition is now satisfied: `.github/workflows/ci.yml` provisions a `postgres:18` service, creates `dealflow_test`, and sets `TEST_DATABASE_URL=…/dealflow_test` on the test job; the log shows `test/outreach-gate.e2e-spec.ts (6 tests) 1707ms` with per-test timings and case A explicitly logged (`grantApproval writes compliance_approvals → real gate returns allowed=true … 429ms`). A skipped suite reports 0ms/skipped — this executed. The test itself is **un-mocked and adversarial**, not tautological: it instantiates the REAL ComplianceGateService + ComplianceGateRepository + AuditService + AuditKeyring (zero `vi.fn()`), inserts the C-1-fix `compliance_approvals` bridge row, and asserts `verdict.allowed===true && verdict.blocks.length===0` (case A), then `no-approval`/`sod:sender-is-approver`/content-drift blocks on adversarial fixtures (cases B/C/D), plus M-2 ConflictException and C-2 versions-embed. Rollback-sentinel tx isolation prevents shared-state contamination; self-migrating `beforeAll` makes it infra-independent; the `insertTestDisclaimer active=false` fix-cycle proves the disclaimerEvaluator path genuinely executed. **Provenance is unbroken:** deployed `af5b5d9` = CI-green `8d7ed8b` + a docs-only chore (touches only two process/*.md files, no code delta) — the e2e ran green against the exact code live on Railway. No Ghost Green.

**Compliance-invariant map — each invariant has a real, non-hollow test:** (1) non-bypassable gate → send_eligible only via passing evaluate(): structural single-assignment code (B-6) + CI e2e case A + no-bypass outreach.spec tests 24/25 — ADEQUATE; (2) SoD + compliance-only approve: e2e case C + dual server-side enforcement (@Roles + defensive 403) + C-2 live analyst/compliance 403 — ADEQUATE; (3) version-binding: e2e case D content-drift + template.service unit + hash provenance code-verified vs real M2 schema — ADEQUATE; (4) AC-STRIP/provenance: C-2 self-grep of DEPLOYED authed HTML (76KB/22KB/22KB real pages, not login shell — zero Send/Schedule/AI CTAs, honest 'Run Compliance Gate & Create Record' CTA, 'No email has been sent' correctly post-compose-gated) + web assert-absent unit tests — ADEQUATE; (5) audit chain last-in-txn: audit.spec + real AuditService in the e2e gate build — ADEQUATE; (6) RBAC + CSRF/origin: outreach.spec RBAC + C-2 live (anon 401) + T-8 CORS-allowlist reflects only allowlisted origin + antiCsrf rid enforcement — ADEQUATE. I specifically credit the C-2 gatekeeping: the spawned deployment-engineer produced two FALSE findings (spurious POST 401 from omitting `rid:anti-csrf`; false AC-STRIP-FAIL from grepping the /login shell) and head-ci-cd independently re-verified and corrected both — that is distrust-every-green discipline, not fabricated green.

**Known gaps weighed honestly and judged non-blocking for THIS wave.** Finding #1 (MEDIUM, empty deployed test-credential registry) is a tester-workflow/tooling gap, not a product defect: C-2 achieved deployed-authed RBAC + AC-STRIP coverage via the invite→signup workaround, so the compliance surface WAS verified live; correctly flagged as cross-wave carry-forward for V-1. Findings #2/#3 (LOW): the interactive UI-click gate-verdict panel and authed visual screenshots were not captured, but the verdict COPY is present in authed HTML and web-unit-tested (the CI log shows `OutreachComposerClient — gate verdict rendered (send_eligible | blocked)` green), and a full live happy-path send_eligible requires the entire wave-10 cross-user SoD sourcing chain — relying on the CI real-DB e2e (6/6) against the exact deployed commit is STRONGER evidence for the invariant than a hand-assembled live smoke, so this reliance is acceptable and does not warrant REWORK. Findings #4/#5/#6 (LOW/INFO): auth backbone untouched this wave (session hardening inherited from wave-2), invite-only signup-500 is correct-by-design, and the Playwright chrome-channel absence is infra. **On the Playwright false-PASS wire specifically:** T-5/T-6 do NOT claim a green interactive Playwright E2E — they explicitly disclose the interactive click-through as a bounded LOW residual and substitute unauth ui-comprehensive-tester + authed-HTML structural verification + 431 green web component tests. There is no silent zero-execution false-PASS masquerading as a green E2E; the gap is disclosed and deferred, which is honest coverage accounting, not coverage theater — so the hard-ESCALATE condition does not fire. 0 critical / 0 high findings; risk budget intact.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
