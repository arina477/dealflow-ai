# Wave 27 — P-3 Plan (M10 recordkeeping EXPORTS) — REWORKED per P-4 Phase-2 (all 3 reviewers REJECTED the inverted premise)
## CORRECTED PREMISE (the crux — was factually inverted):
- **audit_log_entries HAS FORCE RLS** (workspace_isolation policy, 0014:279/380 + 0017:57). A Drizzle select on audit_log_entries via getDb (dealflow_app runtime, NOBYPASSRLS) is ALREADY workspace-filtered by RLS — IDENTICAL to the deal/pipeline tables. RLS is the PRIMARY + SUFFICIENT guard for the export DATA.
- The RLS-EXEMPT read (read_audit_chain_rls_exempt) is ONLY the global integrity WALK for verifyChain → it is FORBIDDEN in the export PAYLOAD path (boolean/summary verify only; NEVER serialize its rows).
- An explicit workspace_id filter MAY be added as LABELED defense-in-depth, sourced from getWorkspaceId() (ALS/session) — NEVER a request param — but it is NOT "mandatory because no RLS."
## REFRAME (security-auditor F3): EXTEND the existing recordkeeping export, NOT greenfield.
- Existing (ships): apps/api/src/modules/recordkeeping/{controller,service,repository}.ts — POST /compliance/audit-log/export (RBAC compliance+admin), exportAsActor (RLS-scoped via getDb, audit-logs export_generated, verifies chain, returns a manifest with tailHash/entryCount), exportScopeSchema/exportManifestSchema (packages/shared/src/recordkeeping.ts), the /compliance/audit-log/export roleRoutes row.
- **REUSE the RLS-scoped repository pattern verbatim.** New deltas ONLY: (1) CSV serialization (currently JSON-manifest); (2) deal/pipeline-activity scope (existing is audit-log-only); (3) a row-cap + explicit truncation; (4) the firm-admin export UI page. If a distinct route is chosen, it MUST route through the identical getDb-RLS repository pattern (no read_audit_chain_rls_exempt in the payload).
## SEC B-BLOCK OBLIGATIONS (folded from Phase-2 F1-F10 — BINDING):
- SEC-1 [CRIT] audit-export reads via getDb/RLS (dealflow_app); FORBID read_audit_chain_rls_exempt in the payload path (verify=boolean only). RLS is the load-bearing guard.
- SEC-2 [HIGH→confirm] workspace is SERVER-resolved (session GUC via getWorkspaceId); the export query/param Zod schema is .strict() with NO workspace_id/firmId/tenant field; a request carrying workspace_id → 400/ignored + export still session-workspace-only (negative test).
- SEC-3 [HIGH] EXTEND the existing recordkeeping export (reuse RecordkeepingService/Repository RLS-scoped path); new = CSV + deal/pipeline + cap + UI page. Do NOT build a second, less-hardened export surface.
- SEC-4 [HIGH] BOUNDED export: a required-or-defaulted date-range AND an explicit max-row cap; on cap-hit → FAIL LOUDLY or emit an explicit manifest truncation warning (truncated:true, rowsReturned, rowsAvailable) — NO silent partial "complete" file (that is itself a compliance-integrity bug). Prefer streaming CSV. Test: over-cap → 4xx-with-guidance OR truncated:true, never a silently-short complete file.
- SEC-5 [MED] CSV-injection escape (AC): any cell whose first char is = + - @ OR tab(0x09)/CR(0x0D)/LF(0x0A) → prefix a single quote; THEN RFC-4180 quote (wrap cells with " , CR LF in double-quotes, double internal "). Test =SUM(1+1), +1, -1, @x, \tfoo, =cmd|'/c calc'!A1, embedded-newline.
- SEC-6 [MED] the global sequence_number is a cross-tenant SIDE-CHANNEL (global monotonic identity → reveals other firms' activity volume/timing). Export a FIRM-LOCAL ordinal (1..N over the firm's own exported rows); keep prev_hash/entry_hash for offline linkage verification (per-entry HMACs leak no volume signal); MASK/drop the raw global sequence_number.
- SEC-7 [MED] RBAC: add the new route to packages/shared/src/rbac.ts roleRoutes = ['compliance','admin'] (RECONCILE with the existing /compliance/audit-log/export precedent — compliance+admin, NOT admin-only; the P-0 "admin-only" was under the wrong premise). Boot-fail-closed (rolesForRoute non-empty assertion). Matrix: firm-admin/compliance of A → 200 (A rows only); advisor/analyst → 403; anon → 401; A-admin with any firm-B identifier → still A-only.
- SEC-8 [HIGH] fault-killing export-isolation e2e (does NOT exist yet): seed audit + deal/pipeline rows in firm A + firm B; invoke the REAL export service as dealflow_app inside workspaceAls (app.workspace_id=A) — reuse the analytics-isolation.e2e harness; assert the exported payload (CSV + JSON, audit + deal scopes) has ZERO firm B rows + count == firm A seeded; assert the export path does NOT call read_audit_chain_rls_exempt for the payload. Run as dealflow_app (NOT postgres — else FORCE RLS is bypassed = false-green, the 0016 trap).
- SEC-9 [LOW] reuse the existing export-audit-log pattern: log scope/format/count/range ONLY (never the exported DATA); attribute actor.id (app users.id, not raw SuperTokens id); append last-in-txn. Add format + scope to the metadata.
- SEC-10 [LOW] no cross-firm joins: the deal/pipeline export joins ONLY RLS-covered tenant tables; any join to a global table (roles/workspaces/app_meta) selects only the caller's own non-sensitive lookup fields. Assert in the isolation test.
## D-BLOCK (design_gap TRUE): D-1 brief (firm-admin export page) → D-2 variants → D-3 review. THEN B.
## Plan: B-0 SKIP (no schema). B-1 (backend-developer): extend exportScopeSchema (format csv|json, scope audit|deal|both, date-range, cap) .strict(). B-2 (backend-developer): extend RecordkeepingService/Repository (CSV serializer + deal/pipeline scope via getDb/RLS + cap+truncation + SEC-6 firm-local ordinal) + SEC-8 isolation e2e + the export-audit-log. B-3 (nextjs-developer): the firm-admin export page (adopted design). B-4/B-5/B-6: head-builder polices SEC-1..10 (esp SEC-1 getDb-not-RLS-exempt, SEC-4 truncation, SEC-8 real-isolation-test-as-dealflow_app).
```yaml
deps_new: [minimal-CSV-serializer (injection-safe)]
schema_change: false
specialists: [backend-developer, nextjs-developer]
compliance_invariants: [audit-export-via-getDb-RLS-not-rls-exempt, no-client-workspace-id, extend-existing-export, bounded-with-explicit-truncation, csv-injection-escaped, firm-local-ordinal-no-global-seq-sidechannel, rbac-compliance+admin-fail-closed, fault-killing-export-isolation-e2e-as-dealflow_app, export-audit-logged-no-data, no-cross-firm-joins]
hard_boundaries: "EXTEND the existing recordkeeping export (getDb/RLS path — audit_log_entries HAS RLS); new=CSV+deal/pipeline+cap+UI; FORBID read_audit_chain_rls_exempt in payload; bounded+explicit-truncation; firm-local ordinal (mask global seq); .strict() no-client-workspace-id; RBAC compliance+admin fail-closed; fault-killing export-isolation e2e as dealflow_app; NO retention/records-view/certification; NO schema."
design_gap_flag: true
security_scope_tightened: true
self_consistency: clean
