# Wave 4 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-w4-t9)
**Reviewed against:** process/waves/wave-4/blocks/T/review-artifacts.md + findings-aggregate.md
**Attempt:** 1  (1 = first gate, 2+ = post-rework)

## Verdict
APPROVED

## Rationale

Every compliance invariant of the tamper-evident audit log is genuinely tested and LIVE-verified — not coverage theater — and I confirmed the load-bearing claims by reading source, not by trusting the deliverable prose. **(1) Immutability** is enforced by two independent DB-layer controls in migration `0002` (REVOKE UPDATE/DELETE/TRUNCATE + `GRANT INSERT,SELECT` from `CURRENT_USER`, plus a `BEFORE UPDATE OR DELETE` FOR-EACH-ROW trigger and a `BEFORE TRUNCATE` FOR-EACH-STATEMENT trigger). The trigger — not the REVOKE — is correctly identified as the load-bearing control because the app role owns the table (REVOKE-from-owner is a no-op), and C-2 proves this against the real DB: UPDATE, DELETE, and TRUNCATE were all rejected by the trigger with row count unchanged at 3. **(2) Tamper-evidence** is real and non-tautological: `audit.verifier.spec.ts` builds a genuine N-entry chain through the real `AuditService` + real `AuditKeyring` + real HMAC, then tampers each threat axis and asserts the EXACT `firstBreakAt` and `reason` (content-hash-mismatch, prev-hash-mismatch, sequence-gap, plus genesis-edge cases) — and C-2 reproduced this live (flip seq-2 content_hash → `{ok:false,firstBreakAt:2,reason:content-hash-mismatch}`, restore → `ok:true`). The golden-vector in `audit.hash.spec.ts` pins a concrete 64-hex HMAC constant with an explicit rotation contract (any serialization-order/field-set drift flips the vector and fails the suite), plus null-vs-"null" discrimination, keyed-not-bare-hash, and per-field tamper sensitivity. **(3) The /review CRITICAL (chain didn't verify against a real DB) is genuinely closed** — encoded as the `pg timestamptz round-trip` regression test that rewrites stored `createdAt` into pg wire-text form and asserts `ok:true` post-fix, with an explicit no-op guard (line 164 asserts the representation actually changed and matches the pg microsecond `+00` pattern) — AND live-confirmed at C-2 where 3 real entries appended through `AuditService.appendStandalone()` (real HMAC, real tx, real timestamptz read-back, NOT hand-crafted INSERTs) verify `{ok:true,entriesChecked:3}` both in-process and over the live HTTP endpoint. **(4) Key handling** is env-only with boot fail-fast (`audit.keyring.spec.ts`: constructor throws on missing/empty key, unknown-version throws, key never leaks via toString/JSON/enumeration), and the wave-4 secret-grep is clean (only env refs + a vitest dummy; the real 64-char key set via Railway pre-deploy, value withheld). **(5) RBAC** exercises the REAL `RolesGuard` against REAL `@Roles()` metadata resolved from the shared `roleRoutes` map and is DB-authoritative — the stale-claim test (claim=admin, DB=advisor → 403) is exactly the SoD-adjacent hardening required — matching the C-2 live matrix (compliance 200 / advisor 403 / anon 401). **(6) Real-browser E2E (7/7, chromium-1208)** ran against LIVE prod with a non-zero executed-test count and asserts DOM data (emerald "all entries verified" pill, entries-checked=3, and the intercepted `/compliance/audit-log/verify` XHR returning 200 `{ok:true,entriesChecked:3}`) — proving the B-6 same-origin-proxy fix works in a real browser and that RBAC gates (advisor denied nav + server-side redirect on direct navigation). This is NOT the missing-binary false-PASS failure mode: the binary is present, tests executed, and assertions read data, not just container visibility. **(7) T-7 skip is legitimate** — the verifier is an O(n) in-order walk at pilot scale with a single-row append; no perf-heavy surface this wave. **The one open low finding (TopBar shows "Dashboard" not "Audit Log")** is correctly non-blocking: the load-bearing `<h2>Audit Log Integrity</h2>` heading renders correctly, the design spec's §Integrity-Validation does not mandate the TopBar title, and the test-maintenance getByText ambiguity (#1) is already fixed. No silently-skipped compliance invariant, no untested audit path, no false-green: the deployed SHA is triple-matched (CI-green == deployed meta == merge HEAD), and every green traces to a concrete executed-and-asserted artifact. Proceed to journey regen (this is a UI wave).

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
