# Wave 7 — T-9 Verdict

**Reviewer:** head-tester (fresh spawn, agentId head-tester-w7-t9-p1)
**Reviewed against:** process/waves/wave-7/blocks/T/review-artifacts.md + findings-aggregate.md
**Attempt:** 1  (1 = first gate, 2+ = post-rework)

## Verdict
APPROVED

## Rationale

The M3 metric — search across ≥2 connected sources — is genuinely delivered and LIVE, not coverage theater. The workspace renders at `/sourcing` (analyst 200, not the old redirect), search filters the canonical universe (`?q=Acme` → 1 of N), and the ≥2-source facet is backed by REAL data: at C-2 (23e5372 re-verify) two fixture connections (A + B) were synced (each `{"ingested":5}`) and GET `/sourcing/companies` returned 4 canonical companies each carrying `connectionIds` populated with 2 real sources (badges, NOT the `'—'` empty state), with cross-source companies showing both connections — the ≥2-source metric proven on real rows, not mocked. Every fix-cycle is genuinely closed and live-confirmed: B-6 badges-hollow (connectionIds now populate) + providerKey-unvalidated (unknown → 400, not 500) both verified live; C-2 migration-0005 Ghost-Green (journal `when` older than 0004 tripped drizzle's coarse timestamp guard → 0005 silently no-op'd) resolved and PROVEN by direct DB probe (6 `__drizzle_migrations` rows + UNIQUE(display_name) present, not by trusting the preDeploy log line); and the C-2 DrizzleQueryError-500 (the wrapper's `.code` is undefined; real 23505 lives on `.cause.code`, so the 409 branch was dead) resolved — dup → 409 Conflict live at 0fe63de, data integrity intact (2nd insert blocked, exactly 1 row). The S2 orchestrator clarification is SOUND and I concur: the E2E browser POST returned 409, not 401 — a ConflictException is only reachable AFTER SessionGuard + RolesGuard pass and the INSERT hits the UNIQUE constraint, whereas a broken header-not-cookie session fails closed at the guard with 401 and never reaches the repository. 409 therefore proves the session authenticated and the constraint fired; S2 is a test-data collision (a leftover display_name reused across runs), correctly classified as test-maintenance (per-run-unique name), NOT a product bug and NOT a B route. The authoritative evidence is C-2's live 201 (unique) + 409 (dup) driven end-to-end through the web-origin proxy with a real analyst cookie-jar session and the VIA_CUSTOM_HEADER rid — which supersedes the T-5 body's original "Real product bug / routes to B" classification. The dup→409 regression is genuinely hardened: the unit mock now throws a DrizzleQueryError-cause.code-shaped object (not a bare `{code:'23505'}`), which is precisely the guard for the green-CI/broken-runtime class — the prior bare-code mock is exactly what masked the 500 in production, and the new shape kills that mutant. Connection-create is real and compliance-sound: RBAC fail-closed (analyst 201 / advisor 403 / compliance 403 / anon 401 live), audited in-transaction with the actor translated to app `users.id` via getUserWithRole (not raw SuperTokens id — the wave-5 lesson), providerKey validated against the adapter registry (400 unknown), and the HMAC-SHA256 audit hash-chain verified intact live (entriesChecked 38→40, verify ok:true) — the tamper-evident invariant appended new rows without breaking the chain. No silently-skipped invariant and no false-green: every T-layer traces to a concrete artifact (CI green ~928 tests with real assertions, live HTTP statuses, DB probes, audit-chain verify). T-7 skip is legitimate — reuse-heavy page over indexed canonical queries with a bounded per-company connectionIds subquery at pilot scale; no new heavy path introduced. The 2 open findings are correctly non-blocking: F1 (S2 test-data collision) is test-maintenance with the feature live-proven, and F2 (TopBar renders "Dashboard" not "Sourcing") is a recurring cross-screen page-title-propagation cosmetic already tracked as a polish task — neither is a compliance-invariant gap, a broken existing journey, nor an untested illegal transition. E2E ran in a real chromium-1208 browser (binary present, 8 tests executed, non-zero), so the missing-binary silent-false-PASS class does not apply. T-block passes to V.

## Footer
- verdict_complete: true
- rework_attempt_cap_remaining: 3
